# 呼吸器（resp）作り直し 引き継ぎ

**参照実装は循環器**。工程・不変条件・踏んだ罠の一般論は
`CLAUDE.md`「既存科目のHTMLを作り直すときの不変条件」と
メモリ `project_circ_rebuild_pipeline.md` が正本で、ここには**呼吸器に固有の事実だけ**を書く。

| | |
|---|---|
| prefix / 総数 | `resp` / **全9章 511問**（2026-09-01 に 506 → 511） |
| PDF | `MEC問題文pdf/MEC臓器別講座・呼吸器_問題（表紙2026）.pdf`（341ページ・1冊だけ） |
| 章別HTML | `_archive/呼吸器/ch01〜ch09.html`（**到達不能・編集対象外**）。正本は `questions_resp.json` |
| JSON の整形 | `json.dumps(indent=1)` を CRLF にしたものと**往復一致する**（実測）。書き戻しはこの形で |

## 工程の進み

| | 工程 | 状態 |
|---|---|---|
| ① | 欠落5問（NO.282〜286）の復元 | ✅ 2026-09-01 `1656a57`（sw v277） |
| ② | qt整形・バッジ4種・rate_text | ✅ 2026-09-01（sw v278） |
| ③ | 画像の全数作り直し（帰属と並びの検収） | ✅ 2026-09-01（sw v279） |
| ④ | 原文照合（設問文の書き換え・連問ステム欠落・正答率の入れ替わり） | ✅ 2026-09-01（sw v280）。詳細は下の「④」 |
| ⑤ | 解説を後期章水準へ底上げ（ep/ee/em/ept） | 🟡 **進行中。ch01〜ch03 完了＋ch04 が NO.200 まで（authored 205 / 511問）。次は NO.201 から** |

## 道具

| 道具 | 置き場 | 役割 |
|---|---|---|
| `anstable.py` | `_work/`（**Git管理下**） | 巻末解答一覧表を12列で読む。バッジ・疾患名・正答率の**唯一の正本**。`python _work/anstable.py resp --dump` |
| `qt_style.py` | `_work/`（**Git管理下**） | qt を整形外科式へ変換する**正本**。循環器と共有＝**科目固有の分岐を持たせない** |
| `resp_pdf.py` | `_work/`（**Git管理下**） | `pages` / `text` / `qtdiff` / `images` / `save` |
| `pdfchoice.py` | `_work/`（**Git管理下**） | PDF の1問から ａ〜ｅ の**原文**を切る。**行単位で切らず span の座標と文字サイズで切る**。版面の実測値（本文 sz9.2／添字 sz5.5・ベースラインが4.7pt下がる／行送り14.2／正答率欄 x516.5 sz8.5／縦組みの柱 x568.7 sz10.6）はこのファイルが持つ |
| `stem_pdf.py` | `_work/`（**Git管理下**） | 連問の共通ステム（宣言文〜最初のアンカー）を原文のまま取り出す |
| `orig_audit.py` | `_work/`（**Git管理下**） | ④の検査4本（ans / choice / qt / stem）。`python _work/orig_audit.py resp --dump` |
| `fix4_1.py`〜`fix4_4.py` | `_work/_resp_tmp/`（gitignore） | ④ を questions_resp.json へ当てる。**冪等**・`--dry-run` あり |
| `apply_qt_badges.py` | `_work/_resp_tmp/`（gitignore） | ② を questions_resp.json へ当てる。**冪等**・`--dry-run` あり |
| `fix_qt_manual.py` | `_work/_resp_tmp/`（gitignore） | ② が構造上さわれない NO.62 / NO.260 の手当て |
| `restore5.py` | `_work/_resp_tmp/`（gitignore） | ① の復元本体 |
| `img_reconcile.py` / `img_survey.py` / `img_declared.py` / `img_order.py` | `_work/_resp_tmp/`（gitignore） | ③ の4検査。**4本を突き合わせて初めて実害が絞れる**（下記） |
| `apply_imgs.py` | `_work/_resp_tmp/`（gitignore） | ③ を questions_resp.json へ当てる。**冪等**・`--dry-run` あり |
| `fix_map.txt` | `_work/_resp_tmp/`（gitignore） | ③ で検収した「直すぶんだけ」の map。`resp_pdf.py save --map` に食わせる |

⚠️ 循環器は `_work/build_circ_ch{NN}.py` という生成器を持っていたので ast で `Q()` の引数を
書き換えたが、**呼吸器に生成器は無く questions_resp.json が唯一の正本**なので JSON を直接書き換える。

## 呼吸器に固有の事実（実測）

### 解答一覧表（`anstable.py resp`）

```
読めた行 511（NO.1〜511）
★156  CBT104  必修107  一般173  臨床338  正答率あり502  疾患名あり481
⚠ 一般と臨床が両方 or どちらも無い行: なし
```

- **一般173 ＋ 臨床338 ＝ 511**＝全問がちょうど一方を持ち、必修はそれと直交するフラグ（循環器と同じ）。
- **疾患名あり481問**＝③の画像の読みを裏取りできる（`resp_pdf.py` 側でも `--dis` 相当が使える）。
- 正答率が空の9問がある（`rate_text` は JSON 側の値をそのまま使っている）。

### 連問（「次の文を読み、N と M の問いに答えよ。」）

- **PDF に 33 グループ 81問**。JSON の `qt-context` は 79問で、差の {282, 283} は
  ①で復元した2問（最初から整形外科式で書いた）＝**欠落は1つも無い**。
  循環器で {260, 311} が欠落していたのとは違い、呼吸器はここが健全。
- `br`（連問バッジ）は 71問にしか付いておらず 102/103/104/107/111/112/116/117 で欠けていた。
  **②で `br` ごと廃止した**（宣言文が qt の先頭に出るので二重になる／循環器と同じ）。

### ② で機械が触れなかった 12問

| 種類 | NO. | 扱い |
|---|---|---|
| 計算問題（`解答：` 欄があり「設問文＝qtの末尾まで」が成り立たない） | 14, 20, 45, 55, 58, 75, 95, 98, 100, 260 | `qt_style.Skip` で素通し。**`<strong>` を持たない**（循環器の計算3問と同じ扱い） |
| ステム途中に `<strong>` があり2つになる | 62 | `fix_qt_manual.py` がステム側を `<span class="kw">` へ |
| 計算問題かつ連問＝`qt-context` が1つだけ残る | 260 | `fix_qt_manual.py` がステムだけ整形外科式へ（設問文は囲まない） |

⚠️ 計算問題のうち **NO.55 / NO.58 / NO.75 は PDF の折り返しが失われて2文が繋がっている**
（`1 秒量1,920ml1 秒率を求めよ。` `呼気予備量0.35ℓ全肺気量を求めよ。` `HCO3－ 25mEq/L肺胞気-…`）。
これは整形の問題ではなく**④の原文照合の仕事**。

### ⚠️⚠️ `qt_style.py` の冪等性バグをここで踏んだ（直してある）

変換後の qt は末尾が `。</strong>` になるため、**1回目には無かった「。」の切れ目が2回目に現れる**。
`_find_question_span` がそこで止まり、設問文が空・`<strong>` が閉じない壊れ方をした
（NO.2 / 10 / 81 / 313 / 315 / 444 の6問）。**残りがタグと空白だけになる切れ目は数えない**
ようにして解決済み。`_work/_resp_tmp/verify_from_head.py` が
「直したせいで1回目の結果まで変わっていないか」を HEAD と突き合わせて確かめる（差0で確認済み）。

## ③ 画像の作り直し（2026-09-01 完了）

**結論: 368枚中 直したのは 39枚。うち 31枚は「並びの逆転」で、8枚は「欠落していた図」。**
`pdf_audit.py resp` は着手前から 0件で、そこからは1件も出てこなかった＝
**「監査0件だから画像は正しい」がやはり言えない**ことを呼吸器でも確認した。

### 使った検査（4本・`_work/_resp_tmp/` に置いてある）

| 道具 | 何を見るか | 結果 |
|---|---|---|
| `img_reconcile.py` | 現物 ↔ PDF埋め込み xref の**同一性**（ahash で候補12件 → **ncc>0.90 で確定**）と、その矩形の直上のアンカー | OK 360 / ATTR 7 / VEC 1 |
| `img_survey.py` | `resp_pdf.attribute()` が出す**設問ごとの矩形集合** vs JSON の imgs | 食い違い 12問 |
| `img_declared.py` | **設問文が宣言する枚数** vs imgs の枚数 | 食い違い 15問 |
| `img_order.py` | 複数枚の問題の**並び**を図ラベル(A/B/C)で検証 | OK 99 / **BAD 14** / NOLABEL 6 |

⚠️ **4本を突き合わせて初めて絞れる。** 単独ではどれも当てにならない——
`img_survey` の12問のうち **8問が機械の帰属ミス**（図が次の設問のアンカーより下に組まれる紙面）、
`img_declared` の15問のうち **12問が数え方の限界**（ABGの「結果を示す」＝本文／
「胸部単純CT を示す」＝3スライスの合成図／マンモグラムの語彙漏れ）だった。
**残った実害は3問だけで、それを見つけたのは `img_survey` と `img_declared` の重なり**。

### 直した1: 並びの逆転 14問（31枚）

**`_1` が右の図・`_2` が左の図**になっていた（循環器で100問中21問だったのと同じ壊れ方）。
図ラベルは画像の外に組まれるので切り出した画像には写らず、**枚数チェックでは絶対に出ない**。

```
119C-36 115D-58 119D-17 115D-46 119A-37 119A-62 119D-32 115D-27
113A-29 113A-66 110A-34 116A-20 115A-22 111I-49
```

⚠️ **`111I-49` は「右乳房(A) / 左乳房(B) / 左乳房拡大(C)」で、狂うと左右を取り違える。**
⚠️ **直し方はファイルの中身を入れ替える**（`imgs` のリストは並べ替えない）。
   連番と図ラベルの対応が崩れると後から読めなくなる。
⚠️ **位置で並べ替えて済ませないこと**——`119A-62` は紙面が `B / A C` の順で、
   y座標だけで並べると必ず間違える。**矩形の直下のラベル語**が正本。
⚠️ ただしラベルが**図の上**に組まれる紙面がある（`119C-40` の ①〜⑤、`108E-49` の C①〜⑤）。
   A/B/C の版面は必ず下、丸数字の版面は上。`img_order.py` は下だけを見るので、
   丸数字の問題は NOLABEL で目視に落ちる（6問とも並びは正しかった）。

### 直した2: 欠落していた図 3問（8枚）

いずれも**連問の共通ステムが宣言した図が、1問目に付いていなかった**。

| NO. | 国試 | 枚数 | 中身 |
|---|---|---|---|
| 102 | 111G-66 | 1 → **3** | 既存の1枚は C（冠状断）だった。A ポータブル胸部Xp・B CT水平断を追加 |
| 107 | 120B-41 | 0 → **2** | A 頸部の写真・B 模式図。**📷バッジ(bi)も無かったので足した** |
| 298 | 113F-77 | 2 → **6** | 既存はフローボリューム曲線 E/F だけ。CT の A〜D が丸ごと落ちていた |

### ③ で見つけた ④ 行きの宿題 → **④で全部片付けた**（下の「④」参照）

宿題は「連問ステムの切り詰め5件」「NO.120 の表の破損」だったが、
④で全問を機械照合したところ**ステムの切り詰めは19問**あり、
さらに**AIが書いた要約への置き換えが4問**見つかった。
⚠️ **画像の作業で見つけた宿題は氷山の一角だと思っておくこと。**

### 残っている「食い違い」は全部が偽陽性（確認済み・追わなくてよい）

| 検査 | 残 | 理由 |
|---|---|---|
| `img_survey` | 132/133・267/268・348/349・416/417 | 図が**次の設問のアンカーより下**に組まれる紙面。JSON が正しい |
| `img_reconcile` | 106G-64（VEC） | **ベクター描画**（1秒率×%肺活量の図）でPDFに埋め込み raster が無い。現物は正しい |
| `img_declared` | 20/55/58/75/95/98/100/120 | ABG・肺機能の「結果を示す」は**本文の数値**で図ではない |
| 〃 | 46・285 | 「気管支内視鏡像を示す」「胸部単純CT を示す」＝**複数コマの合成図を1文で宣言** |
| 〃 | 442 | ステムの宣言は 441 のもので、441 が2枚を持っている |

## ④ 原文照合（2026-09-01 完了）

**結論: 解答一覧表との照合で 159問、PDF 原文との照合で選択肢 82肢・qt 25問を直した。**
検査は `python _work/orig_audit.py resp --dump`（ans / choice / qt / stem の4本）。
**4本を突き合わせて初めて実害が絞れる**のは③の画像4本と同じ。

### 直した1: 解答一覧表（anstable）が正本のもの

- **正答率 157問**。実害は2種類——**連問の中で入れ替わっていたもの**
  （102/103・116/117・132〜134・135/136・154/155・174/175・260/261・310〜312・
  420/421・432/433・434/435・468/469 ほか）と、**`rate` が -1 のまま `rate_text` に
  だけ数字が入っていて難問フィルタに乗らなかったもの**。
  形は循環器に揃えた（`rate_text` は `"N%"` か `""`、`rate_cls` は ch>=80 / cm 60-79 / cl <60）。
  ⚠️ 旧 `（参考）`／`（参照）`／`（超難問）` の表記は廃止。表に正答率が無い9問は `""`。
- **NO.13（111A-59）の正解が a,d,e → c,d,e**。表は**章の表(p325)と巻末の索引(p337)の2か所**で
  c,d,e。内視鏡像B（①の腔内）は**分岐の見えない長い管腔＝左主気管支**なので「①は右主気管支」は誤り。
  解説（ee/ei/em）も書き直した。**画像を自分で見て裏を取った**（循環器と同じ教訓）。
- 復元5問（NO.282〜286）だけ選択肢ラベルが全角 `ａｂｃ` だった（他495問は半角）→ 半角へ。
- **正解肢に付いていた迷子の `<` 36個**を除去。

### 直した2: PDF 原文が正本のもの（選択肢 82肢 / 61問）

| 種類 | 例 |
|---|---|
| **肢の入れ替わり** | NO.31（a↔b）・NO.77（②③の吸気/呼気） |
| **数値の誤り** | NO.11 は5肢とも要約されたうえ a の労作後が 66（正しくは76） |
| **誤字** | NO.6 予防→併発予防／NO.59 並走→並行／NO.109 白板症→白斑症／NO.13 腫瘤性→腫瘍性 |
| **末尾の脱落** | NO.103 `活性化プロテイン` → `活性化プロテインC` |
| **要約・省略** | NO.44・148・168・222・229・274・275・278・279・404 ほか |
| **原文に無い補足** | NO.5（Stridor）・12（Carina）・27（FRC）・198・209・257 ほか |
| **単位の脱落** | NO.67 d に `/L` を補った |

### 直した3: qt（25問）

- **連問の共通ステムの切り詰め 19問** — `現　症：意識は清明。` の直後で切れ、バイタル以降と
  `検査所見：` ブロックが丸ごと落ちていた。**図はあるのに宣言文が無い**状態で、
  `qtdiff` の字数比には出ない（兄弟全員が同じだけ短くなるため）。
  NO.102 は 325字 → 1014字、NO.300 は 526字 → 1003字。
  対象: 64/65・102〜104・107/108・169/170・271/273・298〜300・420/421・432・434・463/464
- **AI が書いた要約への置き換え 4問** — NO.273「急速悪化。JCS II-10。BP 70/40（ショック）。
  SpO₂ 測定不能。JVD↑。」／NO.299「生検の結果：肺腺癌（多発転移あり＝Stage IV）。」／
  NO.421／NO.469。**国試の原文ではない箇条書きの略記**に置き換わっていた。
- **NO.120（118D-50）は表が壊れて解答不能だった**。`％VC の 104/114` と `FEV1％ の 68/72` が
  失われ、残っていたのは FEV1 の 2.12/2.66 だけ。`<table class="tb">` へ起こした。
- NO.89 の設問文の頭の迷子の `↘`／計算問題5問（14/45/55/58/75）の `<br/>`／合字 `ﬁ`(31) `ﬂ`(4)。

### ⚠️ ④で踏んだ罠（他科目でも必ず踏む）

1. **組版のゆれを畳まずに比べると実害が埋もれる。** 畳む前は選択肢だけで500件超が出た。
   畳む対象は 〈〉/（）・`PaO₂`/`PaO2`・リーダー線の長さ・`10L/ 分` の追い出し空白・
   `気　胸` の中付き空白・末尾の「。」・異体字（囊/嚢・瘙/掻）・合字（ﬁ/ﬂ）・大小文字。
   ⚠️ **組版は PDF へ戻さない。** 呼吸器の house style は実測で
   〈〉0件 / （）67件 / 末尾「。」6件 / 添字 `₂` / リーダー線 `—`（choices）、
   qt は `SpO<sub>2</sub>` だけ添字で `PaO2`/`HCO3－` は素のまま。**科目ごとに実測して合わせる。**
2. ⚠️⚠️ **付着物を文字列で落とす保険を置かないこと。** 「JSON の全文で始まり余りが英数字だけなら
   同一とみなす」を入れていたら、NO.103 の `活性化プロテインC` の脱落を
   **「C は図ラベル」と解釈して握りつぶしていた**。落とすのは幾何（span の座標と文字サイズ）で。
3. ⚠️ **PDF のテキストを行単位で切ってはいけない。** `ａ　動脈血pH` の次行 ` 7.40` は
   行頭数字の判定に当たって肢の一部が落ちる。**ラベルの出現位置でスライスする。**
4. ⚠️ **右端を x だけで切ってはいけない。** 本文も右端 x517 まで組まれ（`（B）` sz9.2）、
   添字は x534 まで出る（sz5.5）。x だけで切ると**図の宣言 `（A）（B）` と添字が消える**。
   正答率の欄は **x≧512 かつ sz≒8.5**、縦組みの柱は **x≧545**。
5. ⚠️ **添字はベースラインが 4.7pt 下がる**ので、span を y で素朴に並べると**行末へ飛ぶ**
   （`安静時PaO 96Torr、労作後PaO 76Torr22`）。**先に y で行へ束ね、束ねてから x で並べる。**
6. ⚠️ **兄弟が足した文と共通ステムを混ぜない。** 連問の2問目以降は独自の追記を持つ
   （`入院後適切な治療が行われ…`）。共通ステムで上書きすると、その追記が消える。
   `fix4_3.py` は**設問文の切れ目を今の `<strong>` の中身で決め、組み直した qt のタグを
   剥がした文字列が PDF と完全一致することを検査**してから書いている。

### 残る食い違い4件は意図的（追わなくてよい）

| NO. | 理由 |
|---|---|
| 284 | **表が選択肢**。列見出し（平均肺動脈楔入圧／平均肺動脈圧）は肢の側に埋め込むのが正（CLAUDE.md の規約）。PDF 側は `高　値高　値` としか取れない |
| 176 | **図が選択肢**。グラフの軸の文字（`（％）` `100` `①②`）が肢の行へ流れ込む |
| 298 a | `V̇25` の上点がテキストに出ない（PDF は `V` `・` `25` の3 span） |

## ⑤ 解説の底上げ（2026-09-02〜・進行中）

着手前（2026-09-01 実測）: `ans_sub` を持つのは 78問 / 511問、`eg` は全問持つが
`ept`+`em` の2ブロックだけが 211問と最も多く、**循環器の後期章水準（4ブロック）には
全く届いていなかった**。循環器と同じく章単位で刻んで進める。

### 進み

| 章 | 状態 |
|---|---|
| ch01 呼吸器の基本（NO.1〜72・72問） | ✅ **完了**（`93c3f7e` `6a67e76` `108fb76` `58da8b1` `58d75a8`） |
| ch02 呼吸不全（NO.73〜105・33問） | ✅ **完了**（`4fd36c5` `1d6015c`） |
| ch03 閉塞性疾患（NO.106〜170・65問） | ✅ **完了**（`815b4b0` `b64e730` `f6ff86a` `2054bf5` `807eae2`） |
| ch04 間質性疾患（NO.171〜244・74問） | 🟡 **NO.200 まで完了**（`1f74a7c` `e0553a3` `b7d2814` `ae91d71` `90536bd`）。次は **NO.201 から** |
| ch05 以降 | ⬜ 未着手 |

`authored 205 / 511問`（ch01 の72問＋ch02 の33問＋ch03 の65問＋ch04 の30問＋①で復元した NO.282〜286 の5問）。数え方は `apply_auth.py` が毎回出す
「authored 合計」＝`eg` の cls 並びが `ep/ee/em/ept`（計算問題は `ep/em/ept`）のもの。

### 書き方（`_work/_resp_tmp/`・Git 管理外）

```
python _work/_resp_tmp/ch_dump.py 1 42 48       # 章・[lo,hi) を dump_ch01.txt へ（6問ずつ）
                                                # 出力は Read ツールで読む（Bash の cp932 では潰れる）
（ch{NN}_auth_{x}.py に DATA = {NO: dict(sub/patho/why/deep/point)} を書く）
python _work/_resp_tmp/apply_auth.py ch01_auth_h --dry-run
python _work/_resp_tmp/apply_auth.py ch01_auth_h
```

- 1バッチ6問・平均 6,700〜7,900字。`apply_auth.py` が `<br>`/`</br>` とタグ開閉を検査し、
  **why は全選択肢ぶん必須**（欠けると落ちる）。**計算問題は選択肢が無いので `why={}`＝3ブロック**。
- `patho` は `('🫁 見出し', 本文)`、`deep` は `('🔍 見出し', 本文)`、`point` は本文だけ
  （見出しは常に `🎯 国試ポイント`）。`kw`＝橙の小見出し／`kw2`＝青の締め1行／
  `kw3`＝緑／`kw4`＝赤（警告）。
- 当てたら `python _work/pdf_audit.py resp --no-image`（0件のまま）と
  `node _work/test_card_render.js` を通す。

### 🔑 旧解説は解答と矛盾していることがある（ch01 だけで6件見つかった）

**画像は必ず自分で開き、`ans_label` と突き合わせてから書くこと。**

| NO. | 旧解説の誤り |
|---|---|
| 43（111I-1） | `ee` が「b が正しい」、解答は d。**`ans_label` にも「（誤り：PaO₂<55mmHgが基準）」という矛盾する注記**が付いていた（`108fb76` で除去）。⚠️ 同種の注記を持つ 24問のうち、**肯定形の設問はこの1問だけ**——他23問は「誤っているのはどれか」型なので注記は正しい |
| 46（110G-13） | `ei` が5つの選択肢をそのまま「正しい対応」として並べていた（＝全部が正解という自己矛盾） |
| 48（109B-41） | 「Brinkman指数1600」＝誤り（20本×40年＝**800**） |
| 53（106H-1） | `em` が「**呼気**時に最強となる喘鳴の病変部位」＝設問は吸気時 |
| 57（104H-7） | `ee` が声音振盪を「**無気肺で増強**」＝気道が閉塞した無気肺では減弱する |
| 64（102C-28） | `ans_sub` が「JVD＋肝腫大が最も緊急度の高い徴候」と、解答（d チアノーゼ）と正面から矛盾 |

### 🔑 ch02 でも同じ型の矛盾が4件あった

| NO. | 旧解説の誤り |
|---|---|
| 73（120A-65） | `ep` が「淡水」＝本例は**海水**（高張なので肺胞へ水を引き込む） |
| 77（118C-37） | `ei` の②と③が図・選択肢に対して逆 |
| 79（112A-41） | 病変を「左」としていたが、胸水も再膨張性肺水腫も**右**（CTで下行大動脈が画面右＝患者左／来院時写真で胃泡が画面右下、の2点で裏取り） |
| 83（111I-46） | `ans_sub` と `ee` が「NIPPVでCO₂を排出」＝解答（a 気管挿管）と正面から矛盾。意識障害があるのでNPPVは禁忌 |
| 91（117A-32） | `ans_sub`/`ep`/`ee` が「シャント率上昇が誤り」＝解答（e 代償性呼吸性アシドーシス）と矛盾。pH 7.26 は代償されていない |
| 96（113C-46） | `ep` が **NO.83（111I-46）の数値**（pH 7.17・PaCO₂ 102・呼吸数12/分）をそのまま書いていた。本問に血液ガスの記載はなく呼吸数は8/分 |
| 102（111G-66） | `em` が「人工呼吸中」「縦隔気腫＝陽圧換気による圧損傷」＝本例はリザーバー付マスク6L/分。「両肺の浸潤影＝ARDS」も誤りで実体は誤嚥性肺炎 |
| 104（111G-68） | `ep` が本例を「重症ARDS」と断じていた（連問 NO.102/103 と同じ誤嚥性肺炎） |

🔑 **連問は「同じ症例を別の角度から3回書く」ことになるので、角度の割り当てを
先に決めてから書く。** ch02 の NO.102〜104 は 画像所見／抗菌薬の選択／呼吸生理の指標、
と役割を分け、NO.105 のために「高炭酸ガス血症の全身への影響」を空けておいた。

🔑 **計算問題（`why={}`）は構造上4,000〜4,300字にしかならない。** ch02 では NO.75・95・
98・100 がこれで、混ざるバッチは平均が6,400字前後まで下がる。**目安（6,700〜7,900字）を
満たそうとして水増ししないこと**——選択肢のあるものが7,400〜7,900字あれば十分。

🔑 **章内で patho / deep の角度が重なりやすい。** ch01 では喫煙指数が3問（44/47/48）・
呼吸筋が2問（49/52）・気道の解剖が3問（46/56/59）・チアノーゼが2問（54/71）・
打診が3問（57/68/72）・副雑音が2問（60/62）あり、**同じ話を別の角度から書く必要があった**。
書く前に既出の見出しを確認して角度をずらすこと。
ch02 では酸素投与器具が4問（80/92/94/101）・A-aDO₂ の計算が4問（75/95/98/100）・
NPPV が4問（78/82/83/90）・ARDS が4問（73/76/89/97）・CO₂ナルコーシスが3問（74/96/105）
あり、**章の全問を見渡して角度を先に割り当ててから**バッチを書いた。

### 🔑 ch03（NO.106〜170・65問）で見つけた旧解説の誤り

| NO. | 旧解説の誤り |
|---|---|
| 145（111E-51） | 画像2枚（胸部エックス線・CT）に**一行も触れていなかった** |
| 148・150・151・153・156 | 画像の解説が無いか一行だけ |
| 163（109A-59・**正答率19%**） | 頸静脈怒張とBNP上昇を「急性肺性心」と一言で片づけ、**3枚の画像に一行も触れず**。誤答の主因は「心不全と読んで治療を選び直す」ことなので、見かけの心不全所見を1つずつ解体する形へ書き直した |
| 166（108E-49） | ⚠️`ep` が「**③がこのパターンを示す**」と書いているのに正解は **e ⑤**＝**解説の中で答えが食い違っていた**。③は実際にはプラトー型（上気道閉塞） |
| 169（107H-35） | 胸部エックス線に一行も触れていなかった（**正常であること自体が、過敏性肺炎・肺炎・心不全を否定する情報**） |

🔑 **図が選択肢の問題は、5本とも実測して1本ずつ同定する。** NO.166 のフローボリューム
曲線5本は、①ピーク11・FVC 4.2・直線／②ピーク9.4・FVC 5.9・直線／③ピーク5.5→**約4の
プラトー**・FVC 4.5／④ピーク7.3・**FVC 2.0・形は保たれる**／⑤ピーク4.6・**急落して
低い尾**・FVC 2.0、と数値を読んでから分類した。⚠️**④と⑤はFVCが同じ2.0Lで、大きさでは
区別できない**——この種の図は「形の名前」ではなく**読み取った数値**を解説に残すこと。

🔑 **ch03 は同じ話題が繰り返し出るので、章の全65問に角度を先に割り当てた。**
COPD増悪の治療が5問（136/137/163/167/168）・喘息発作の初期対応が4問（106/117/154/170）・
喘息の診断が3問（120/139/169）・咳の鑑別が4問（121/125/141/152）・ICSが3問（143/153/165）・
呼吸機能検査が4問（111/128/140/166）あり、**同じ疾患を10回近く別の角度から書くことになる**。
バッチを書く前に「既出の `ep`／`em` 見出し一覧」を作って空いている角度を探すのが速い。
実際に採った角度の一覧はコミットメッセージ（`815b4b0`〜`807eae2`）に残してある。

🔑 **「選ばない選択肢」を外す理由は1つではない。** NO.167 は BAL（そもそも検査）・
胸腔ドレナージ（所見がない）・侵襲的人工換気（順番が先）と**3つとも違う理由**で外れる。
「不適切」で片づけず、**理由の型を書き分ける**と解説が厚くなる。

### 🔑 ch04（NO.171〜・進行中）で得たこと

⚠️⚠️ **`apply_auth.py` のタグ開閉検査に `u` を足した**（それまで span / b / div の3つだけ）。
足した瞬間、**適用済みの NO.171・173・174・179 に余分な `</u>` が5箇所**見つかり
`e0553a3` で直した。**live に出ていた**＝検査に無いタグは黙って壊れたまま出荷される。
形はいつも同じ——**`<u><b>X</b></u>——<b>Y</b></u>` の先頭の `</u>` が余る**。
リード文の `<span class="kwN"><b>…<u>…</u>…</b></span>` を書くときに最も出やすい。

🔑 **1問1ファイルで書いて `cat` で継ぎ足す。** 大きな日本語を bash のヒアドキュメントへ
流すと `unexpected EOF while looking for matching '''` で**黙って0バイト追記**になる。
`_cNNN.txt` を書き、`head -n -2` → `cat` → `printf` で `}` を戻す手順で継ぐ。
**継ぐたびに `--dry-run`** を通せば、どの問でタグが壊れたかがその場で分かる。

🔑 **`why` と `point` は放っておくと薄くなる。** `patho` / `deep` は自然に2,400〜2,800字に
なるが、`why`（5肢で3,600〜4,000字）と `point`（1,800字）は意識しないと半分で終わり、
**1問8,000字台へ落ちる**。書き終えたら**1問ずつ実測し、足りない側だけを増補する**
（NO.185〜188 の4問がこれに当たった）。

🔑 **角度の衝突は `_work/_resp_tmp/_authored_heads.txt`（`ep` と `em` の見出し一覧）を
引いて避ける。章を書き終えたら追記する。** ch04 では**縦隔気腫（NO.102 deep）・
好酸球性肺疾患の並べ方（NO.149／NO.151 deep）・副雑音の分類（NO.57/60/62/68）・
KL-6（NO.147 deep）**が既出で使えず、その都度別の角度へ振り直した。

🔑 **ch04 でも旧解説の誤りが出た。** NO.176 は4象限の対応が**4つとも逆**で
`ans_label` の注記まで誤っていた（`label` キーで訂正）。NO.179 は `em` が「②がこのパターン」
と書いているのに正解は **c＝③**。NO.174 は `ep` が夏型過敏性肺炎としていたが、
症例は田植えを続けて軽快しており正解は「治療は必要ないことを説明する」＝**普通感冒**。

### 🔑 ch04 NO.189〜200（2026-09-03）で得たこと

🔑⚠️ **旧解説の「他問からの混入」は ch02 だけの現象ではなかった。** NO.189 の末尾 `em` が
**「抗癌薬投与後のびまん性肺陰影。」で始まっていた**——これは **NO.188（抗癌化学療法中の
薬剤性肺炎）の本文**で、NO.189 の被疑薬は **NSAIDs と抗菌薬**、抗癌薬は一度も出てこない。
**隣り合う2問が同じ疾患のとき**に起きるので、**連続する同一疾患のペアは疑って読む**。

🔑⚠️ **ch04 で目立つのは「矛盾」ではなく「画像への無言」**（ch03 と同じ型）。
このバッチ12問のうち **NO.190・192・193・195・197・198 の6問が、画像に一行も触れていなかった**。
とくに次の3つは、**画像が答えそのもの**を持っている:

| NO. | 画像に写っているのに書かれていなかったこと |
|---|---|
| 190（100F-18） | **右上肺野の大陰影の中に空洞**——設問「頻度の高い合併症」の答え（肺結核）が写っている |
| 192（118D-53） | **心筋生検の非乾酪性肉芽腫と多核巨細胞**——設問が「診断はどれか」で、**組織像こそが根拠** |
| 195（117D-75） | **造影CTの結節状・全周性の胸膜肥厚**——「胸膜生検」を選ぶ根拠。ここを読まないと、なぜ肺生検（d）ではないのかが言えない |

🔑 **中皮腫が3問続く（193・195、および 204・206・211・213・214・217〜219・233・243）ので、
角度を先に割り振らないと必ず衝突する。** 使った順は
**193＝縦隔が偏位しない大量胸水／職業歴の聴き方**、**195＝造影CTの胸膜肥厚／組織診断が難しい理由**。
既出の **NO.110 deep（石綿関連疾患の一覧）** と **NO.181（カルレチニン／救済制度）** は
どちらも使えないので、**残りは「胸水の読み方」「病期と治療」「予後説明」あたりへ振ることになる**。

🔑 **サルコイドーシスも同様に多い**（192・196、以降 203・216・224・229・231・237・242・244）。
使った順は **192＝心筋生検の組織像／壁厚と内腔の2軸表**、**196＝不整脈の機序／心臓のFDG-PETの前処置**。
**NO.180（治療適応／見つけにいく）も既出**なので、残りは
**検査所見（ACE・sIL-2R・Ca）／気管支鏡所見／高Ca血症の機序／眼病変** へ振れる。

🔑 **放射線肺炎は ch04 に5問ある**（198・200、以降 212・215・226）。**NO.177 deep が総論
（照射野と線量分布図・急性の肺臓炎から線維化まで）を既に使っている**ので、
このバッチでは **198＝誤答肢4つ（乳び胸・上肢浮腫・反回神経麻痺・心肥大）の解剖**、
**200＝放射線生物学（急性期反応は分裂の速い組織、晩期反応は血管）** へ逃がした。
残る3問は **経過観察という判断／IPFがリスク因子／照射野に一致する陰影の読み** が空いている。

🔑 **rate が高くても解説が薄いとは限らない、が、rate 98〜99% の問題は「なぜその肢か」より
「なぜ他が違うか」に紙幅を割ると厚くなる。** NO.195（98%）・198（98%）・199（99%）は
いずれも**誤答肢の側に本体を置いた**（検査の目的3分類／手術合併症の解剖／感染症の伝播経路）。

🔑 **タグ検査が拾った崩れは1件**（NO.192 patho の余分な `</b>`）。形は ch04 batch a/b の
`</u>` と同じで、**リード文の `<span class="kwN"><b>…⚠️<u>…</b>——<b>…</b></u></b></span>` を
書くときに `<u>` の直後の `<b>` を落とす**。**`<u>` を開いたら必ず `<b>` を開く**と決めておくと出ない。

🔑 **CACHE の bump は push の直前に1回。** `questions_resp.json` を変えているので必須
（2026-09-02 に v280 → **v281**）。バッチごとに bump しない。

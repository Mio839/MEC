# 神経（neur）作り直し 引き継ぎ

**参照実装は循環器→呼吸器**。工程・不変条件・踏んだ罠の一般論は
`CLAUDE.md`「既存科目のHTMLを作り直すときの不変条件」と `_work/呼吸器_引き継ぎ.md` が正本で、
ここには**神経に固有の事実と進捗だけ**を書く。

| | |
|---|---|
| prefix / 総数 | `neur` / **全11章 594問**（NO.1〜594・uid の欠番なし） |
| PDF | `MEC問題文pdf/MEC臓器別講座・神経_問題（表紙2026）.pdf`（368ページ・1冊だけ） |
| 正本 | `questions_neur.json`（生成器なし＝JSON を直接書き換える） |
| JSON の整形 | **`json.dumps(indent=2, ensure_ascii=False)`・LF・末尾改行なし**で往復一致（2026-09-15 実測。呼吸器の indent=1+CRLF とは違う） |
| 作業スクラッチ | `_work/_neur_tmp/`（gitignore 済み） |

## 章（NO. の範囲）

| 章 | 題 | 問 | NO. |
|---|---|---|---|
| 1 | 神経の基本 | 70 | 1〜70 |
| 2 | 脳神経 | 67 | 71〜137 |
| 3 | 脳血管障害 | 106 | 138〜243 |
| 4 | 高次脳機能障害 | 26 | 244〜269 |
| 5 | 認知症とParkinsonism | 74 | 270〜343 |
| 6 | 運動ニューロン障害と脱髄 | 37 | 344〜380 |
| 7 | 末梢神経・NMJ・筋 | 62 | 381〜442 |
| 8 | 神経感染症 | 47 | 443〜489 |
| 9 | 機能性疾患 | 41 | 490〜530 |
| 10 | 外傷性頭蓋内出血 | 29 | 531〜559 |
| 11 | 脳腫瘍 | 35 | 560〜594 |

## 工程の進み（ループはこの表の「次」を1単位ずつ進める）

| | 工程 | 1イテレーションの単位 | 状態 |
|---|---|---|---|
| ① | 欠落問題の確認・復元 | 1回 | ✅ 2026-09-15。欠番なし・**国試番号 594/594 一致**（`_neur_tmp/kid_check.py`）。正解肢も NO.480 以外一致（479 は表の折り返しで anstable が誤読しただけ＝b で正しい） |
| ② | qt整形・バッジ4種(bh/bc/bip/brn)・rate_text を "96%" へ | 1回 | ✅ 2026-09-15（sw v420）。`_neur_tmp/apply_qt_badges.py`（冪等・`--dry-run`）。qt 594問・strong 594・qt-context 0／rate 32件を表に合わせ rate_cls・rate_text も再計算／**bm（N択）は qt の「Nつ選べ」から作り直した**（旧は単一正解66問に↗N誤読の 2択/3択/4択 が付いていた）／br廃止。rate_index.js・qmeta.json 再生成 |
| ③ | 画像の全数作り直し（帰属と並び・4検査＋目視） | 2〜3章ずつ | 🟡 **ch01〜ch06 済**（2026-09-15・sw v422）。次は ch07〜ch09 |
| ④ | 原文照合（ans/choice/qt/stem の4検査） | 1回 | ⬜ |
| ⑤ | 解説の底上げ（ep/ee/em/ept・patho/why/deep/point） | 6問ずつ | ⬜ |

**次：③ ch07〜ch09（NO.381〜530）。** 着手前に下の「連問の兄弟ごとの図」一覧を流すこと。

## ③ 画像の作り直し

道具: `_work/neur_pdf.py`（resp_pdf.py の PDF/JSON/画像パス差し替え・Git管理）／`_neur_tmp/img_reconcile.py`・`img_survey.py`・`img_declared.py`・`img_order.py`（全594問を一度に流す・数秒）／
`_neur_tmp/sheet.py OUTDIR NO...`（PDFページ描画＋JSON画像を1枚に並べる目視用シート）。

⚠️ **`img_declared.py` は 2026-09-06 規約（連問ステムの図は兄弟全員）へ直してある。`img_survey.py` は旧規約のまま**
＝連問の2問目以降の「⚠集合が違う」（例 81/82・123/124・142・190・219/220）は偽陽性。
⚠️ 神経の版面は**1問＝1枠**で読みやすいが、**①〜⑤の図が選択肢の問題は丸数字が画像に焼き込まれて保存されており、ファイルの連番が xref 順（＝丸数字順ではない）**になっていた。`img_order.py` は丸数字を拾えず NOLABEL に落ちる → **NOLABEL は全部シートで目視**すること。
⚠️ **同じ xref を2ファイルに保存した重複**（`_1` と `_3` など md5 は違うが同じ図）が多い。`img_survey` の「PDF 1枚 / JSON 2枚」がそれ。

### ch01〜ch03 で直したもの（`_neur_tmp/apply_imgs_ch01_03.py`・一回きり・済み印あり）

| 種類 | NO. |
|---|---|
| 並びの入れ替え（ファイル中身） | 12（①③②⑤④）・14（②①）・37（④①②⑤③）・51（②①③⑤④）・103（④⑤逆）・152（A/B逆）・155（B,A,C）・201（②③①）・237（A/B逆） |
| 重複を imgs から外す | 13・70・145・149・155・200・214 |
| 連問182〜184（114C-66〜68）へステムの図 A/B/C を配布 | 旧: 182=A だけ・183=無し・184=B,A。C は p103 xref500 を新規保存。183 に bi を追加 |

- 目視で正しかった（検査の偽陽性）: 53・76・86・102（合成図1枚）・111・141・144・174・213・235。
- 🧹 **③の最後に削除するファイル**: 110H-2_3・94A-17_2・117B-39_3・117F-60_2・112A-31_4・108E-58_2・105C-17_2・114C-68_1・114C-68_2・111I-45_1・111I-45_2（pdf_audit が「参照されていない」で出すのはこれ）。

### ⚠️ 連問の兄弟ごとの図は `img_survey` では見つからない（旧規約のため）

連問ごとに兄弟の imgs を並べ、**DIFF の群はステムのページを描画して「図の宣言がステムにあるか、兄弟個別の設問文にあるか」を決める**。
ステムなら兄弟全員へ配り、個別なら DIFF のままが正しい（神経では 148/149・204/205・232/233・280・335〜337・344〜346 が個別）。
一覧は ch04〜06 のメモ（下）にあるスニペット＝`qt` 先頭の「次の文を読み、N」で群を作り、imgs の集合が揃っているかを見るだけ。

### ch04〜ch06 で直したもの（`_neur_tmp/apply_imgs_ch04_06.py`・一回きり）

| 種類 | NO. |
|---|---|
| 並びの入れ替え | 246（D,A,B,E,C）・248（①④②⑤③）・255（同）・273（A/B逆）・279（A/B逆） |
| 図の無い設問から外す | 319（付いていたのは次の連問320の SPECT 2枚。bi も外した） |
| 連問ステムの図を兄弟全員へ | 251〜253（109B-59_1,2）・320〜322（110B-53_1,2） |
| 連問の図の取り違え | **351〜353** は MS の FLAIR（xref952）が付くべきところ無し → `112C-63_1` を新設／**354〜356** は 351 の図が付いていた → p211 xref959/960（大腿MRI A/B）を `111B-53_1,_2` として保存し直して配布 |

- 目視で正しかった: 269（A＋視野②枚）・310（本文の表）・377（合成図1枚）・306〜308。
- ④の宿題: **320〜322・351〜353・354〜356** も qt に「連問（112C-64）。」等の原文に無い文字が入り、ステムが切れている（182〜184 と同型）。**④では「連問（」を全問 grep すること。**

### ③で見つけた ④ 行きの宿題

- **NO.148〜150（117F-59〜61）**: 兄弟の qt が「共通ステム＋自分の続き」になっておらず、150 に 148 の検査値以降が混ざる等、ステムの切り方が兄弟ごとに違う。
- **NO.182〜184（114C-66〜68）**: qt のステムが「現症：意識は清明。」で**切れている**（PDF p103 の神経診察・検査所見・「頭部MRI の T2* 強調水平断像（A ～C）を示す。」が丸ごと無い）。さらに 183 に「連問（CGA7）。」、184 に「連問（114C-68）。」という**原文に無い文字**が入っている（183 は設問文「高齢者機能評価簡易版〈CGA7〉の…」自体も欠落）。

### ① で見つかったもの（② で直した）

- **NO.480（106I-29）公式解答が「b,c or b,e（複数の選択肢を正解とする）」**。採点エンジンは「or」を表せないので
  ok は b,c のまま、`ans_label` に b,e も正解と注記し `bk 複数正解` バッジを付けた（hbp_ch04_q259 と同じ流儀）。
- **正答率（`rate`）の食い違い 32件**（解答一覧表の rate 列が正本）:
  - 隣と**入れ替わり**: 276↔277・280↔281・296↔297・388↔389・505↔506、344/345/346 は3つ巡回（正: 344=88・345=96・346=65）
  - JSON が `-1`（表には値あり）: 89=86・90=91・91=61・131=58・132=94・148=97・149=93・150=98・165=65・166=78・
    320=80・321=93・322=48・335=98・336=34・337=98・433=90・502=18・591=81
  - ⚠️ `rate` を直したら `rate_cls`（ch ≥80 / cm 60-79 / cl <60 の既存規則を確かめてから）と `rate_text` も揃える。
    再検査は `python _work/_neur_tmp/kid_check.py`（ANS 479 は既知の誤読）。

## 着手前の実測（2026-09-15）

```
anstable.py neur: 読めた行 594（NO.1〜594）
★205  CBT87  必修118  一般222  臨床372  正答率あり544  疾患名あり558
⚠ 一般と臨床が両方 or どちらも無い行: なし
```

- 一般222＋臨床372＝594（循環器・呼吸器と同じ構造）。
- `rate_text` は `正答率 79%` 形式が525問・空が69問 → ② で `79%` 形式へ。
- 画像あり 222問・延べ383枚。qt-context（旧連問書式）あり。
- eg の cls: em 593 / ep 519 / ept 123 / ee 89 / ec 8。**解説の中央値 443字・ブロック2個以下が77%**（全科目で最も薄い）。
- 旧スクリプト（`enhance_neuro.py`・`neuro_enhance_progress.json`・`fix_neur_*.py`・`audit_neuro_images.py`）は過去の部分修正の残骸。**信用せず、呼吸器の道具で作り直す**。

## 道具（呼吸器から流用）

- `_work/anstable.py neur`（PDFS に neur を追加済み）
- `_work/qt_style.py`（科目固有の分岐を持たせない）
- `_work/resp_pdf.py` → `_work/neur_pdf.py` を作る（PDF パスだけ差し替えで動くか先に確かめる）
- `_work/pdfchoice.py` / `stem_pdf.py` / `orig_audit.py`（sid 引数対応を確認）
- ⑤ は `_work/_resp_tmp/` の `ch_dump.py`・`apply_auth.py`・`_chk.py`・`_findswap.py`・`_fixswap.py`・`_fwd.py`・`_peek.py` を `_neur_tmp/` へ写して使う

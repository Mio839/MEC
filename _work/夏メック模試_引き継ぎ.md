# 2026年度 夏メック模試（第121回）— 引き継ぎ

自己採点ツール（`mock.html` / `mock.js` / `mock_data/m121s.js`）を作るときに読んだ
`MEC問題文pdf/2026年度 夏メック模試_解説書.pdf` の構造を残す。
**次にやるのは解説HTMLの作成**なので、そこで必要になる事実を先に書いてある。

⚠️ PDF は `.gitignore` 済み（`MEC問題文pdf/`）＝リポジトリには入っていない。
別マシンでは PDF を置かないと再抽出できない。生成物 `mock_data/m121s.js` は追跡されている。

---

## 1. 全体の構成

| | |
|---|---|
| ページ数 | **792ページ** |
| 構成 | A 各論(p7-176) / B 必修(p177-256) / C 総論(p257-386) / D 各論(p387-562) / E 必修(p563-646) / F 総論(p647-772) / 出題内容一覧表(p773-782) / 科目別出題内容一覧表(p783-791) |
| 問題数 | **400問**（A75 B50 C75 D75 E50 F75） |
| 配点 | **500点**（A75 B100 C75 D75 E100 F75） |
| 版元 | 株式会社メック・2026年6月17日発行 |

⚠️ **B と E が必修ブロック**。一般1点×25 ＋ **臨床3点×25** ＝ 100点という構造は
第115回以降の国試の必修そのもの。合否は「必修80%（200点中160点）」と
「一般・臨床の相対基準」の二本立てで、**Bだけ入力した日は必修100点満点で見る**
（`mock.js` の `score()` が入力済みブロックだけを母数にするのはこのため）。

⚠️ **禁忌肢は全体で17問**（A6 B1 C2 D4 E0 F4）。国試は**4問以上で不合格**。

---

## 2. 抽出の入口（座標と罠）

再抽出は `python _work/build_mock_m121s.py`（`--check` で現物との一致だけ確認）。
**独立した情報源どうしを突き合わせて検算している**ので、紙面が変わったら黙って通らずに落ちる。

| 取るもの | 出どころ | 罠 |
|---|---|---|
| 正解・配点区分・禁忌肢の印 | 各ブロックの解答表（p8/178/258/388/564/648） | **A/C/D/F は3列・B/E は2列**で x が違う。禁の字は問題番号セルの**左**に置かれる |
| 科目・出題テーマ・一般/臨床 | 科目別出題内容一覧表（p784-791） | **見開きでページごとに左右へずれる**。ヘッダ `A～F` の x で基準を合わせる |
| 設問の在りか | 各設問ページ左肩の大きな番号 | 右端が **x≒97.8** で揃う。単問は y≒54、**連問は「41〜42」の帯で y≒65** |
| どの肢が禁忌肢か | 選択肢考察 | 禁忌肢だけ ○/× ではなく **`□ｃ`** が付き、左に 禁 の字が置かれる |
| 必要選択数 | 設問文 | 「2 つ選\nべ。」と**語の途中で改行する紙面がある**（F63）。`つ\s*選\s*べ` で拾う |

### 検算に使った独立な3つ（全部一致した）

1. 解答表の配点区分の合計 ＝ 表下の記載（A 一般15/臨床60 …）
2. 解答表の禁の印 ＝ 科目別一覧表の禁忌列（**17問で完全一致**）
3. 設問文の「Nつ選べ」 ＝ 正解の個数（**400問すべてで一致**）

⚠️ **この3つの一致は「読み取りが壊れていない」ことの証明として残す価値がある。**
`_work/build_mock_m121s.py` の `verify()` と `_work/test_mock_score.js` の §1〜§3 が両方で見張る。

---

## 3. 解説HTMLを作るときに要る事実

### 3-1. 1問ぶんの解説の構造（実測の出現数）

```
設問文＋選択肢
出題ポイント          370問（＝全問）
鑑別診断へのプロセス   220問（症例問題だけ）
確 定 診 断           219問
画 像 診 断           103問
英 文 和 訳             5問（英語問題）
選択肢考察            370問（＝全問。○/×/□ ＋ 肢ごとの解説）
正解：ｄ
check point           370問（＝全問。まとまった知識のブロック）
```

⚠️ 「370」は**設問ページの枚数**であって設問数ではない（連問は1ページに複数の設問が載る）。

既存の `questions_*.json` の `eg` 配列へ落とすなら、素直な対応は:

| 解説書 | `cls` |
|---|---|
| 出題ポイント | `ept`（国試ポイント） |
| 鑑別診断へのプロセス／確定診断 | `ee`（鑑別） |
| 画像診断 | `ei`（画像所見） |
| 選択肢考察 | `em`（選択肢別解説） |
| check point | `ep`（病態） |
| 【計算】の式 | `ec`（計算） |

⚠️ **独自クラス名を作らないこと**（`study.css` の6色に意味が焼き込んである）。

### 3-2. 各設問がPDFのどこにあるか

`mock_data/m121s.js` の各問が **`pdf: [開始ページ, 終了ページ]`（1-origin）** を持っている。
解説HTMLを起こすときはここから読めばよく、ページを探し直す必要はない。

```js
window.MecMockData.m121s.questions.find(q => q.uid === 'm121s_A_q27').pdf  // → [57, 58]
```

### 3-3. 画像 —— 解説書PDFに入っている（別冊は要らない）

⚠️⚠️ **「別冊No.○を別に示す」と書かれていても、この解説書PDFでは設問文のすぐ後に
その図が挿入されている。** 本番では別冊に載る図が、解説書では本文に組み込まれている。
**したがって画像は別冊を待たずにこのPDFから抽出できる**（2026-09-08にユーザーが指摘・実測で確認）。

**→ 解説HTMLを作るときは、画像問題の図をPDFから抽出して実装すること。**

| | |
|---|---|
| 図を参照する設問 | **122問**（A32 / B6 / C20 / D35 / E9 / F20）。うち連問が24問 |
| 図の種類 | (ブロック, 別冊No) で **131種**・延べ158参照 |
| データ | `fig: ["13A","13B"]`。⚠️ **別冊No. はブロックごとに1から振り直される**ので、ブロックをまたぐ一意キーは `{ブロック}-{No}` |
| 在りか | **その設問の開始ページ（`pdf[0]`）に埋め込みラスタとして載っている**。連問はグループの先頭ページ（兄弟は同じページ範囲を持つ） |
| 抽出 | `page.get_images()` → `fitz.Pixmap(doc, xref)`。ページ全体では解説書に351枚・230ページぶんある |

⚠️ **4問だけラスタではなくベクター描画**（`B2` ROC曲線 / `C1` 生存数曲線 / `C37` グラフ /
`F33` 聴覚器のシェーマ）。`get_images()` では取れないので、**ページの該当領域を
300dpi で描画して切り出す**こと（`page.get_pixmap(clip=rect)`。他科目で確立している方法と同じ）。

⚠️ **図ラベル（A/B/C）は画像の外に組まれた文字なので、切り出した画像には写らない。**
複数枚の問題は並びが狂っても枚数チェックでは見つからない——循環器の作り直しで
**100問中21問**が狂っていた前科がある。**矩形の直下のラベル文字を拾ってラベル順に並べる**こと
（CLAUDE.md「既存科目のHTMLを作り直すときの不変条件」と同じ手順）。

⚠️ **連問のステムが参照する図は兄弟全員に付ける**（study.html の 2026-09-06 規約）。
`build_mock_m121s.py` はそうしてある。⚠️ その取り落としは静かに起きる——一度、ステムを切る
区切りに半角スペースを許したせいで症例文の書き出し「66 歳の男性。」が設問番号に見え、
**ステムの図を持つ22問ぶんが丸ごと落ちていた**（`fig` が 100問しか無かった）。
`test_mock_score.js` の「別冊の図の索引が壊れていない（122問・131種）」がこれを見張る。

### 3-4. 連問

- **20群**（2連×10群＝B/E、3連×10群＝C/F）。`series: "B41-42"` の形で群を持たせてある。
- 紙面は「次の文を読み、41、42 の問いに答えよ。」＋共通ステム → `41　…` `42　…` の順。
- ⚠️ **共通ステムが参照する図は兄弟全員に付ける**（study.html の 2026-09-06 規約と同じ）。
  `build_mock_m121s.py` はそうしてある。
- ⚠️ 選択肢考察では、サブ設問が**行頭の裸の番号（`41`）**で区切られる。

### 3-5. 計算問題

- 桁入力（マークシートの数字欄）は **3問だけ**: `A74`（3桁・クレアチニンクリアランス）、
  `A75`（3桁・血漿浸透圧）、`D75`（2桁・尿中の1日塩分排泄量）。
- 「※①②③は全て正答で1点」＝**全か無か**。`mock.js` は桁文字列の完全一致で採点する。
- ⚠️ **正解は数値ではなく桁文字列**。先頭ゼロ・末尾ゼロが意味を持つので数値化しないこと
  （`calc_input.js` と同じ約束）。
- 出題テーマに `【計算】` が付く問題は他にもあるが（`B27` 事後確率・`C61` qSOFA など）、
  そちらは**選択式**なので桁入力ではない。

### 3-6. 著作権

⚠️ **問題文・解説の全文をAIへ渡さないこと**（MEC教材の著作物）。
`mock.html` の Markdown 書き出しは、uid・科目・出題テーマ・成績だけを出し、
本文を1文字も含めない（`stats.html` の弱点カルテと同じ方針）。

---

## 4. 解説の重さをどう受け取るか

解説HTMLは **`MecMock.weights('m121s')` だけを読む**。採点の内部構造には触れない。

```js
{
  "m121s_A_q27": {
    uid, block:'A', no:27,
    weight: 3,              // 0=正解 / 1=誤答 / 2=未解答 / 3=禁忌肢を踏んだ
    state: 'wrong',         // correct | wrong | blank
    picked: 'c', ans: ['e'], taboo: true,
    cat1: '救急・他', cat2: '救急', theme: '電池誤飲への対応',
    pdf: [57, 58]
  }, …
}
```

⚠️ **段の意味を増やすときは `mock.js` の `weights()` と、そこのコメント表の両方を直すこと。**
HTML 側に閾値（`weight >= 2 なら厚く` のような数字）を散らさない——片方だけに新しい段が
入って解釈が食い違う。

⚠️ **`weights()` は「今の周回」の解答から毎回計算される。** 記録として保存されているのは
「何を選んだか」だけで、正誤も得点も保存していない。**解答表を直せば過去の記録も自動で
正しくなる**——これは意図した設計で、正誤を保存する形に戻さないこと。

---

## 5. まだやっていないこと

- ~~D・E・F の入力~~ → **2026-09-08 に全400問の入力が完了**（下記スナップショット）。
- **画像の取り込み**。図は解説書PDFに入っているので抽出できる（3-3）。**解説HTML作成の
  一部としてやること。** ベクター4問と、複数枚の並び（図ラベル順）に注意。
- **模試の推定ボーダー**。配点表に載っていないので `mock.html` の「一般・臨床のボーダー」は
  空のまま。値が出たら入れる（必修80%と禁忌肢4問は制度の値なので固定してある）。
- **解説HTMLそのもの**。入口は 3-2（PDFページ）と 4（重み）。

---

## 6. 解説HTMLの作り方（2026-09-08にユーザーが決めた方針）

| 決めたこと | |
|---|---|
| **出力先** | `questions_m121s.json` を新設し、**study.html へ統合**する（試験モード・SRS復習・弱点カルテを模試の問題にも効かせる） |
| **正解した問題** | 解説書の解説を**そのまま逐語で移植**する（リポジトリがパブリックであることは承知のうえでの判断） |
| **誤答した問題** | **循環器の作り直しと同じ品質**で書き下ろす（`project_circ_rebuild_pipeline.md` の教訓が正本） |
| **着手順** | ① 画像122問の抽出 → ② 採点済みの A・B の誤答から執筆 |

### ✅ 6-0. uid と表示番号 —— 決定済み（2026-09-08 ユーザー裁定）

**採ったのは (a) 科目内通し ＋ 模試ごとに1科目。** 以下は確定事項で、**もう動かさないこと**
（uid を後から変えると localStorage の進捗＝done_v2・SRS・myrate 等が引き継がれない。
精神科の是正で実際にそうなった前科がある）。

| | |
|---|---|
| prefix / 科目名 | `m121s` ／「第121回 夏メック模試」 |
| JSON | `questions_m121s.json` |
| 画像 | `夏メック模試/images/{ブロック}{番号}_{n}.jpeg`（例 `A25_1.jpeg`） |
| 章 | **A〜F を ch01〜ch06**（ch01=A / ch02=B / ch03=C / ch04=D / ch05=E / ch06=F） |
| uid | `m121s_ch{NN}_q{n}` の `{n}` は **科目内で通し**（規約②を守る） |
| 表示 | `Q.{n}`（通し）＋ **模試の番号は新設バッジ「B問題 1」で別に出す** |

章頭の通し番号（`Q_START` に相当）:

| 章 | ブロック | Q. |
|---|---|---|
| ch01 | A問題 | 1〜75 |
| ch02 | B問題 | 76〜125 |
| ch03 | C問題 | 126〜200 |
| ch04 | D問題 | 201〜275 |
| ch05 | E問題 | 276〜325 |
| ch06 | F問題 | 326〜400 |

⚠️ **模試を将来足すときは科目ごと足す**（`m122w` 等）。`mock` 1科目に章を積む形は採らない
＝回ごとに進捗・弱点が独立して見えることを優先した。SUBJECTS とチップが1つ増えるのは承知のうえ。

⚠️ **`mock_data/m121s.js` の uid（`m121s_A_q27`）と questions 側の uid（`m121s_ch01_q27`）は別物。**
`MecMock.weights()` が返すのは前者なので、対応表は**決定論の関数1本**で持つこと
（`{block}{no}` → `ch{NN}` と通し番号）。**採点ツールのデータは触らない**（触ると採点の記録が切れる）。

<details><summary>採らなかった案（記録）</summary>

- **(b) ブロックごとに6科目**（`m121sB_ch01_q1`／`Q.1`）— 表示番号は自然だが
  `gamify.js` の SUBJECTS が6件増え、科目チップも6つ増える。模試を足すたび +6。
- **(c) 1科目6章で番号を振り直し**（`m121s_ch02_q1`／`Q.1`）— **規約②違反**。
  同じ科目内に Q.1 が6つできて `jumpToQnum` が壊れる。採ってはいけない。

</details>

### 参考: 当初の論点（決定の背景）

**統合を選んだ時点で、CLAUDE.md の2つの規約が正面からぶつかる。**

- 規約①: uid は `{prefix}_ch{nn}_q{n}`
- 規約②: **`{n}` と表示される `Q.{n}` は科目内で通し**（章が変わっても続きから振る）。
  破ると「同じ科目内に Q.1 が複数できて `jumpToQnum` が壊れる」——精神科で実際に起きて是正した前科がある。

ところが模試の番号は**ブロックごとに1から振り直される**（A1-75 / B1-50 / …）。
A〜F を1科目の6章にすると、規約②に真っ向から反する。取りうる形は3つ:

| | uid / 表示 | 得るもの / 失うもの |
|---|---|---|
| **(a) 科目内通し** | `m121s_ch02_q76`／`Q.76`（B問題の1番） | 規約②を満たし `jumpToQnum` が壊れない。**表示番号が模試の番号と食い違う**ので、バッジ等で「B問題 1」を別に出す必要がある |
| (b) ブロックごとに別科目 | `m121sB_ch01_q1`／`Q.1` | 番号も表示も自然。**`gamify.js` の SUBJECTS が6件増え**、科目選択が6つ増える |
| (c) 1科目6章で番号は振り直し | `m121s_ch02_q1`／`Q.1` | 見た目は自然だが**規約②違反＝ジャンプが壊れる**。採ってはいけない |

⚠️ **(a) を推す**（規約を曲げず、失うのは表示番号だけで、それはバッジで補える）。
⚠️ **uid を後から変えると localStorage の進捗（done_v2・SRS・myrate 等）が引き継がれない。**
   精神科の是正で実際にそうなった。**最初の1問を書く前に決めること。**

⚠️ **`mock_data/m121s.js` の uid（`m121s_A_q27`）と questions 側の uid は別物になる。**
`MecMock.weights()` が返すのは前者なので、**解説HTML側に決定論的な対応表（関数1本）が要る**。
どちらの体系も残す＝採点ツールのデータは触らない（触ると採点の記録が切れる）。

### 6-1. 統合に要る配管（章を足すときと同じ手順）

1. `questions_m121s.json` を作る（`_work/build_mock_m121s_json.py` を新設して PDF から生成）
2. `gamify.js` の `SUBJECTS` に1件（`total` は実数）
3. `chapters_meta.js` に章メタ
4. `study.html` の `subj-hdr-count`
5. `node _work/test_subject_totals.js` を通す（**この3者の一致だけがテストで守られる**）
6. `sw.js` の `CACHE` を bump（questions_*.json を足したので **SHELL_VERSION だけでは足りない**）
7. `python _work/build_qmeta.py`・`python _work/build_image_dims.py` を回し直す

### ✅ 6-2. 画像 —— 完了（2026-09-08・全138枚を目視で検収済み）

`python _work/mock_pdf.py figs` → `python _work/mock_pdf.py save` の2手で
**`夏メック模試/images/{ブロック}{番号}_{n}.jpeg` に138枚**が出る（12MB）。
テスト: `node _work/test_mock_figs.js`（6件）。中間物 `_work/_mock_tmp/` は `.gitignore` 済み。

⚠️⚠️ **`fig` に取り落としがあったので `build_mock_m121s.py` ごと直した（131種 → 138種）。**
`RE_BETSU` が `([A-Z])?` で枝番を**1文字しか拾っていなかった**が、紙面には
**「別冊No. 15 A、B」と1つの No. にまとめて書く形が7か所**ある（`A、B` 6・`B、C` 1）。
そのため A40・A72・C61-63・D38・D74・E41-42・F49 の**10問ぶんで2枚目が丸ごと消えていた**。
⚠️ 直したのは `fig` だけで、**`ans`・`pts`・`taboo` は1バイトも動いていない**（差分を突き合わせて確認）
＝`mec_mock_v1` の採点記録は無傷。`test_mock_score.js` の期待値も 131 → 138 に更新した。

⚠️ **「①～⑤」は枝番ではない。** `別冊No. 6 ①～⑤` は**1つの図の中の5パネル**（＝図が選択肢の問題）で、
別冊キーは `6` の1つ。A26・D25・D40(16B) が該当。ここを枝番として数えないこと。

#### なぜ xref を保存せず「ページを clip して 300dpi で描画」したか

他科目（整形外科式）は `get_images()` の xref をそのまま保存するが、この模試では採れない:

1. **1つの別冊No.が複数の埋め込み画像でできている。** C5「別冊No. 3」は2列×3段の6スライス、
   A26・D25 は5枚組。xref を1枚ずつ保存するとバラバラになり並べ直す情報が残らない。
2. **4問はベクター描画**（B2 ROC曲線 / C1 生存数曲線 / **C37 は「グラフ」ではなくワクチン接種
   スケジュールの表** / F33 耳の模式図）。clip 描画なら同じ経路で扱える。
3. 埋め込み画像は元々ほぼ 300dpi 相当（173×114pt の枠に 721×477px）なので**描き直しても落ちない**。

#### 実装で潰した罠（`_work/mock_pdf.py` のコメントが正本）

| 罠 | 症状 |
|---|---|
| 見出しを x で絞らないと設問文に当たる | 「**確定診断**に最も有用な検査はどれか。」が見出し扱いになり、A43 で図より手前で打ち切った。見出しは**左マージン x<62** |
| ページ右端・左端の大きな1文字 `A`〜`F` は**ブロックの柱** | 図ラベルと誤認する。矩形の x 範囲内に絞れば構造的に排除される |
| 同じ xref が設問ページと解説ページの両方に出る | A17 の p31/p32。矩形で重複を潰す |
| 設問の別冊図と**解説中の図が同じページに混在** | p50 の2枚はラベル無しの解説図。最初の見出しより上だけを採る |
| **図がページをまたぐ** | A26 の ①②③ が p52・④⑤ が p53。外接を1つ取って先頭ページに当てると **y が 50 まで上がって設問文の段落を丸ごと巻き込む**（実際に起きた）。ページごとに切って save 側で縦に積む |
| ①～⑤ の丸数字は**図の外だが図の一部** | 肢 a〜e に対応するので含める。ただし**縦の中心が図の中にあるものだけ**——緩めると直前の選択肢行「ｅ　⑤」まで引き込む（D25 で出た） |

#### 検収のしかた（次に模試を足すときも同じ手順で）

**枚数が合っていることは何の保証にもならない**（循環器で100問中21問の並び違いを見逃した前科）。
実際に効いたのは次の2つで、**138枚すべてを実際に見た**:

1. **設問文が「どの記号が何の検査か」を書いている**（「胸部エックス線写真（別冊No. 13 A）と
   骨盤部単純CT（別冊No. 13 B、C）」）。**26問の多枚数設問はこれを見出しにして並べ、
   絵と突き合わせた**——超音波かCTか、内視鏡かMRCPか、HE染色か蛍光抗体かは一目で違う。
2. 単図81問も同じく設問文の検査名を見出しにして並べた。

結果は **26問（58枚）の並び・81問の同定ともに誤り0**。連問（C61・F70 等）も含めて正しかった。

⚠️ **循環器の教訓は引き続き効く**——「画像は必ず自分で見る」「設問文と矛盾する
`ans_sub` は画像を開く前に見つかる」「図が選択肢の問題は各図がどう違うかを1枚ずつ書く」。
⚠️ **D40(16B) と D25 の ①〜⑤ は「①②③が左列・④⑤が右列」の列優先の並び**。
行優先だと思って読むと肢の対応が入れ替わる。

### 6-3. 執筆の対象をどう決めるか

`MecMock.weights('m121s')` の `weight` が正本（0=正解 / 1=誤答 / 2=未解答 / 3=禁忌肢）。

- `weight > 0` → 循環器品質で書き下ろす
- `weight === 0` → 解説書の解説を逐語で移植

**スナップショットは `_work/m121s_weights_2026-09-08.json` に取ってある**（400問ぶん・
`MecMock.weights('m121s')` の書き出し）。⚠️ **Git 管理外にしないこと**——これが執筆対象の
唯一の正本で、失うと「どれを厚く書いたか」が二度と再現できない。着手時点の内訳:

| weight | 意味 | 件数 |
|---|---|---|
| 0 | 正解 → **解説書の解説を逐語で移植** | **335** |
| 1 | 誤答 → **循環器品質で書き下ろす** | **63** |
| 3 | 禁忌肢を踏んだ → 同上（最優先） | **2**（A58 前置胎盤／C49 微弱陣痛） |
| 2 | 未解答 | 0（全ブロック入力済み） |

書き下ろし **65問**の内訳: 図あり13問・連問5問。科目は循環器7／産科6／保健5／疫学統計4／救急4／
内代4 が上位で、**公衆衛生13・内科系23** と広く散っている。

⚠️ **`weight` は「今の周回の解答」から毎回計算される**ので、2周目で正解すると 0 に戻る。
**執筆対象は着手時点のスナップショットで固定すること**（結果画面の「JSON」書き出しを
`_work/` に保存しておくのが確実）。そうしないと「前に厚く書いた問題が対象から消える」。

### 6-4. 品質の基準（誤答問題）

`project_circ_rebuild_pipeline.md`（メモリ）と CLAUDE.md「既存科目のHTMLを作り直すときの
不変条件」が正本。特に効くもの:

- `ans_sub` は**まず設問文と照合 → 次に画像を開く**の順で点検する
- **章内の `deep`／`patho` のタイトル重複を着手前に grep する**（循環器で4回踏んだ）
- `ans_label` は肢とバイト一致させない（読みやすい表記が正本）
- `eg` の `cls` は既存6種（`ep`/`ee`/`ept`/`em`/`ec`/`ei`）だけ。独自クラスを作らない

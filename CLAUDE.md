# MEC 医師国試学習ツール

## 作業スタンス

タスクに取り掛かる前に、必要な情報が不足していると判断した場合は実装を始める前にユーザーに確認を取ること。推測で進めず、曖昧な点は必ず質問する。

## プロジェクト概要

医師国家試験対策の学習ツール。GitHub Pages 経由で iPad・PC・スマホからアクセス。

- **リポジトリ:** GitHub `Mio839/MEC`
- **変更反映:** `git push origin main` で GitHub Pages に自動反映

## ファイル構成

| ファイル/フォルダ | 役割 |
|---|---|
| `study.html` | 統合学習ツール（コア12科目5487問＋マイナー講座・実力試験・自作・フィルター）。試験モードUI等のマークアップ＋インラインJS |
| `study_exam.js` | study.htmlの試験モードロジック（state・効果音・演出エフェクト・SRS採点連携）。classic scriptでインライン<script>より前に読込み、共有グローバルスコープで相互参照 |
| `study.css` | study.html専用のCSS（旧インライン<style>を2026-07-05に外出し）。⚠️ study.htmlはこれに依存＝両方一緒にcommit/push必須 |
| `index.html` | ハブダッシュボード（全科目の進捗表示・ナビ・同期設定）。ヒーローのボタンは**席が固定の3つ**＝主（やるべきこと）・副（復習）・「🔁 今日の誤答を再履修」。0件の日も席を空けず `.is-off` で無効表示にする。⚠️ **統計への導線をここに置かないこと**（下記「ヒーローのボタン」） |
| `progress.js` | 共有モジュール：localStorage + GitHub Gist 同期。localStorageキーは`K*`定数が正本 |
| `attempts.js` | 解答イベントログ（`mec_attempts_v1`・`window.MecAttempts`）。1解答=パイプ区切り1行の文字列で上限5000件のリングバッファ（2026-08-06に2000から引き上げ。1日1400解答の日があり2000件では約1.4日分しか持たず「昨日の誤答」がその日のうちに消えた。⚠️`attempts.js`の`CAP`と`progress.js`の`ATT_CAP`は一致必須）。集計値の`myrate_v1`と違い時刻・出題順・所要秒・選んだ肢を残す＝弱点分析の素材。study.html／stats.html／**index.html**が読込み。`todayWrongUids()`は「今日の誤答を再履修」の対象UIDの正本（ハブの件数表示と出題側が同じ関数を使う） |
| `qmeta.json` | 設問メタ（全科目1ファイル・`_work/build_qmeta.py`が生成する**派生物**）。設問形式(診断/検査/治療/対応/知識)・否定形・複数選択・画像・症例・計算・採点除外を自動分類。stats.htmlの弱点カルテが使う。**questions_*.json は一切変更しない**（pdf_audit.pyの監査対象を汚さないため） |
| `stats.html` | 学習統計ページ（30日チャート・SRS統計・AI相談Markdownエクスポート） |
| `knowledge.html` | 検索知識ノート機能 |
| `mindmap.html` / `mindmap.js` / `mindmap.css` | 疾患マインドマップ。**1枚のページで科目マップ（`?sid=hema`）とハブ（引数なし＝全科目）の両方を描く**。2026-08-21に、9科目ぶんの自前エンジンを内蔵した `{科目}/mindmap.html` ＋ `mindmap_integrated.html` から移行した（旧ファイルは `_archive/mindmap_src/`・旧URLにはリダイレクトstubを置いてある）。⚠️ 下記「疾患マインドマップ」の不変条件を読んでから触ること |
| `mindmap_data/` | マインドマップのデータ。`index.js`（科目レジストリ22件＝**マップがあるのは `ready:true` の21件で、公衆衛生 `ph` だけ `ready:false`**・`gamify.js` の SUBJECTS から `_work/build_mindmap_index.js` が生成する**派生物**）／`{sid}.js`（科目1件ぶんの章・疾患・関連）／`_hub.js`（ハブの代表疾患。科目データの射影**ではなく**独立にキュレーションされたもの）。**新科目のマップを足す作業＝ここにファイルを1つ書くこと**（エンジンは触らない） |
| `calc_input.js` | 計算問題の桁入力エンジン（`window.MecCalc`）。原文がマークシートの計算問題50問（科目33＋過去問17）は選択肢を持たないため試験モードで解答不能だった。正解は `.ac`（ans_label）の `計算答：<桁文字列>` が正本。**study.html と 国家試験過去問/*.html の両方が読む共有ファイル**（演出テーマのようなミラー乖離を作らないため）。CSSは自前で注入する |
| `card_renderer.js` | JSON→カードHTML描画（`window._renderSubjectFromJson`、エスケープ処理あり） |
| `fx_engine.js` | エフェクトのCanvas描画エンジン（`window.MecFX`：粒子・花火・グリフバースト等）。ハブのゲージ用に `gears`／`gearRain`／`steam`（真鍮の歯車・蒸気）を、2026-08-14に `shatter`（破片）／`ribbon`（2点間を走る光）／`stamp`（刻印）／`orbit`（極座標で回る粒）／`wave`（走査する波形）を足した。**エミッタの追加は常に純増で行うこと**——study.html／chapter_exam.js の試験演出が同じエンジンを共用しているので、既存関数の引数や既定値を変えると7テーマ全部に波及する。⚠️ **位置を自前で持つ型（ribbon/wave/stamp/bar/bolt/ring）は `STATIC_TYPES` に登録すること**——登録し忘れると step() の物理を通り、重力で画面外へ落ちて1フレームで消える |
| `sounds/` ＋ `sounds_index.js` | 効果音。実体は `sounds/{正解音,起動音,選択音,結果画面}/` の**4フォルダ**（`結果画面/` は 2026-08-21 に追加）、台帳は `sounds/meta.json`、一覧は `_work/build_sounds_index.js` が生成する `sounds_index.js`（**派生物**・`window.MecSounds`）。**ファイル名・キー・音量の唯一の正本**で、study.html／index.html／chapter_exam.js の3つが全部これを読む。⚠️ 音を足すのは「フォルダに置く→meta.json に1行→生成スクリプト」の3手順でコードは触らない。⚠️ 起動音は設定で選ばせず毎回ランダム |
| `image_dims.json` | 問題画像の実寸（パス→[w,h]・約109KB・**派生物**。`_work/build_image_dims.py`が生成）。`card_renderer.js`が`<img width height>`を出す材料。これが無いと遅延読込の画像でレイアウトが後からずれ、章ジャンプが目標に収束しない。**画像を差し替え・追加したら必ず再生成** |
| `sw.js` | Service Worker（オフラインキャッシュ）。`CACHE`版数は**questions_*.json・画像を更新した時にbump**（bumpで全キャッシュ削除＝再DL）。SHELL/CARDSにパス列挙。相対パス必須 |
| `chapters_meta.js` / `rate_index.js` | stats.html等が参照する章メタ・正答率インデックス（`_work/build.py`系で再生成） |
| `questions_*.json` | **問題データの正本**。study.htmlはこれを読み込んで表示。⚠️ 2026-07-24に`questions_*.js`（file://フォールバック用の同内容コピー・計約15MB）を廃止した。運用はGitHub Pages一本で、コピーはリポジトリを二重に太らせ更新のたびに再生成が要るだけだったため。`_work/gen_js_from_json.js`・`_work/check_json_js_sync.js`・pre-commitフックの自動生成ステップも同時に撤去済み |
| `国家試験過去問/` | 過去問ビューアHTML（`chapter_exam.js`で試験モード）。PDFは`.gitignore`済み・追跡はhtmlのみ |
| `chapter_exam.js` | 過去問ビューアの試験モード（`CE_EFFECT_THEMES`＝study_exam.jsの演出を同配色でミラー） |
| `内分泌/` `呼吸器/` `循環器/` `消化器/` `神経/` `肝胆膵/` `腎臓/` `血液/` `免アレ膠/` | 各科目のフォルダ（画像・selfcheck_intro.html等）。章別解答解説HTML(ch01.html等)は全科目 `_archive/{科目}/` へ移動済み（2026-07-07完了） |
| `産婦人科/` | 章別HTML(ch01〜ch13)＋`images/`＋`obg_questions.json`（メタ）。HTMLが`questions_obg.json`のソース＝`_work/build_obg_json.py`で再生成 |
| `_archive/` | 到達不能になった旧・章別HTMLの保管先。編集対象外、読み物としてのみ残す |
| `vars.css` | 共通CSSカスタムプロパティ（全ページ共通色変数） |
| `ui_theme.css` / `ui_theme.js` | **UIテーマ（着せ替えスキン）全8種**（aurora／brass／cyber／liquid／kintsugi／celestial／abyss／frost）。`ui_theme.js` が `localStorage['mec_ui_theme_v1']` を読んで `<html>` に `ui-{id}` を付ける＝**必ず1つ適用される**（既定 `aurora`）。study/index/stats/knowledge の4ページと `sw.js` が読む。テスト: `node _work/test_ui_theme.js`。⚠️ **全8テーマが `.qc` に `animation: {id}CardEnter … both`（`from{opacity:0}`）と `overflow:hidden` を掛けている**。`.qc` を触るときは Phase 4/5/7 の「`.qc` の層は満杯」「`transform` は既存アニメに黙って殺される」がここと直接ぶつかることを思い出すこと。⚠️ `opacity:0` の backwards fill は**非表示タブではアニメが1frameも進まない**間そのまま残る（stats.html の `armReveal`・マインドマップと同型）。ここはアニメが自分で終わる形なのでタブを表に戻せば自力で復帰するが、**JSでクラスを外す形へ作り変えないこと**（落ちた日にカードが白紙になる） |
| `_work/` | ビルド・検証・マージ用スクリプト（`build.py`・`pdf_audit.py`・`build_qmeta.py`・`build_image_dims.py`・`compress_images.py`・`fix_missing_bi_badges.py`・各`test_*.js`等）。⚠️**PDFから新科目の章別HTMLを作るときは先に `_work/新科目HTML生成ガイド.md` を読む**（抽出フロー・産婦人科水準の解説品質基準・統合チェックリスト・着手プロンプト。参照実装は精神科psy=`build_psy_ch01.py`／`build_psy_json.py`） |
| `精神科/` | マイナー講座・精神科（prefix `psy`）。章別HTML(`ch01_seishinka_kihon.html`〜`ch08_sonota.html`)＋`images/`＋`psy_questions.json`（章名メタ）。HTMLが`questions_psy.json`のソース＝章ごとの生成器`_work/build_psy_ch{NN}.py`→`_work/build_psy_json.py`で再生成。産婦人科と同構造。**ch01は`EXTRA`辞書で肢別解説を後付けする方式、ch02以降はQ()に`patho`/`deep`/`point`を直接渡す方式**（新章は最初から4ブロック書くため）。ch03のQ.34(105D-54)は国試の**採点除外問題**＝`bx`バッジ・正解肢0・`rate=None`で作る（`_isExamUngraded`が中立表示で通す）。第1章73問・第2章25問・第3章41問・第4章36問・第5章18問・第6章25問・第7章24問・第8章14問＝**全8章256問（NO.1-256）完成**。**Q番号はPDFの通し番号（NO.）を厳守**＝章ごとにQ.1へ振り直さない（下記「問題番号は科目内の通し番号」） |
| `耳鼻咽喉科/` | マイナー講座・耳鼻咽喉科（prefix `ent`・👂・#0F766E）。章別HTML(`ch01_mimi_kihon.html`〜)＋`images/`＋`ent_questions.json`（全8章の章名メタ）。HTMLが`questions_ent.json`のソース＝章ごとの生成器`_work/build_ent_ch{NN}.py`→`_work/build_ent_json.py`で再生成。眼科と同方式。**全8章214問（2026-08-07完成）・章頭NO.は 1／21／51／99／102／138／152／172**（章名はPDF表記のまま「耳①：耳の基本」等）。**2026-08-06に第1章「耳①：耳の基本」NO.1-20（画像8問14枚）と第2章「耳②：耳の疾患（中耳）」NO.21-50（30問・画像23問41枚）、第3章「耳③：耳の疾患（内耳・後迷路性・その他）」NO.51-98（48問・画像19問39枚＝**ent最大の章**）、第4章「鼻・口・唾液腺①：鼻・口・唾液腺の基本」NO.99-101（3問・画像1問1枚＝**ent最小の章**）、第5章「鼻・口・唾液腺②：鼻の疾患」NO.102-137（36問・画像17問39枚）を追加。2026-08-07に第6章「鼻・口・唾液腺③：口・唾液腺の疾患」NO.138-151（14問・画像11問16枚）と第7章「咽喉頭①：咽喉頭の基本」NO.152-171（20問・画像7問17枚）を追加**。ch02の軸は急性中耳炎9問・真珠腫性中耳炎7問・滲出性中耳炎5問・耳硬化症3問・慢性穿孔性中耳炎3問で、**鼓膜写真1枚から疾患を決めて治療（保存/鼓膜切開/チューブ/鼓室形成術/アブミ骨手術）へ橋渡しする**筋が繰り返し問われる。ch03の軸はBPPV 9問・Ramsay Hunt 8問・聴神経腫瘍6問・騒音性難聴6問・老人性難聴5問・Ménière病4問・前庭神経炎3問で、**めまいは「持続時間」（秒＝BPPV／分〜時間＝Ménière／日＝前庭神経炎）、難聴は「オージオグラムの型」で疾患を決める**という筋。
⚠️ **連問は ch03 の NO.80・81（109C-28/29）と ch05 の NO.132・133（106F-30/31）の2組**。**症例文は両方のカードに載せる**こと——試験モードはカードを1枚ずつ独立に出すので2問目に症例が無いと解けない（設問文と選択肢は問題ごとに別物にする。生成器では `_STEM_80` 定数を両方の qt に連結している）。
⚠️ **ch03 NO.84(107A-56) はPDF本文の「左聴力の低下」と図の患側（○＝右の系列がスケールアウト）が噛み合わない**。解説では左右を断定せず「患側／対側」で書いた。詐聴の根拠は左右ではなく「純音は110dBスケールアウトなのに**あぶみ骨筋反射が正常**」という矛盾にある。
⚠️ **ch03の①〜⑤オージオグラムはレイアウトが紙面ごとに違う**（NO.54＝左列①②③/右列④⑤／NO.72＝上段①②③/下段④⑤／NO.82・86＝上段①②/下段③④⑤）。`images` の抽出順は座標順と一致していたが、必ず200〜500dpiに拡大して目視で確認すること。
⚠️ **NO.19(111I-40) は a〜g の7択**（純音聴力検査の結果①〜⑦から選ぶ）。5択決め打ちのコードを書かないこと——生成器の `FW` は f/g まで持つ。
⚠️ **図中の①〜⑤／①〜⑦が選択肢そのものになる問題がある**（ch01は NO.10・15・19／ch02は NO.25・34・43・**45**／ch05は NO.136＝Bellocqタンポンの留置部位。**上段①②／中段③④／下段⑤で、正解⑤＝後鼻孔にはまり込んだ図**。①はまだ外鼻孔の外、②③は前鼻腔タンポンの位置）。PDFを260〜600dpiで拡大して目視で同定すること。**ch02 NO.45(105G-48) は選択肢a〜eが5枚のティンパノグラムの図で、本文に選択肢の文字が1つも無い**——図を読んで各肢の文章を書き起こした（正解 e＝ピークの無い平坦なB型）。
⚠️ **オージオグラム等の線画は JPEG q85 だと格子線が崩れ、`pdf_audit.py` の知覚ハッシュ照合が閾値を超えて「ゴミ画像」と誤検出される**（104D-1 は q85→hamming 7・q95→0）。`ent_pdf.py` の `cmd_save` は `_is_line_art()` でほぼ無彩色の画像を判定し **q95** で保存する。
⚠️ **採点除外は NO.76（110A-28）の1問**（`bx`バッジ・正解肢0・`rate=None`）。**正答率が無いのは 13・49・50・70・117・150・151・192 の8問**（84B/95A/98/99など旧国試で出典に数字が無いだけ＝採点除外ではない。全8章ぶん対応済み）。
⚠️ **NO.100 の episode は巻末解答一覧表の表記どおり `112E-26／106H-15類`**（表のセルが `112E-26／`／`106H-15類` の2行で「類」で切れており、本文の「類題」と1字違う）。`verify_ent_ch.py` は表と突合するので表側を正本にする。
⚠️ **解答一覧表の国試番号は複数行に折り返すことがあり、1行目がNO列のアンカーより「上」に来る**（NO.48＝`102A-42・`／`99A-11の`／`プール問題` の3行、NO.199も同型）。2026-08-06に `cmd_anstable` を**「各単語を y が最も近いアンカーへ配る」方式**へ直した（旧・範囲切りは1行目を丸ごと落とし、疾患名の折り返しも1つ前の行へ吸われていた）。**episode に `・` 区切りの複数コードが入る問題があるので画像ファイル名は代表コード1つ**にし、`pdf_audit.py` は `・` 区切りのどれかに一致すれば通す（前方一致にすると 112D-3 が 112D-63 を通してしまうので使わない）。
ch04は3問だけの小章（副鼻腔の開口部・口腔の視診・頰部裂創と耳下腺管）。ch05の軸はアレルギー性鼻炎9問・鼻出血8問・副鼻腔炎5問・鼻茸/好酸球性副鼻腔炎4問・腫瘍5問・顔面骨骨折2問・術後性上顎囊胞2問・真菌症2問で、**「片側性なら腫瘍/真菌/歯性、両側性なら好酸球性/アレルギー」という左右の切り分け**と、**鼻出血は圧迫→血管収縮ガーゼ→タンポン→凝固→塞栓という段階**が繰り返し問われる。
ch07の軸は反回神経・声帯麻痺7問（152・153・154・165・167・169・170）・嚥下5問（155・158・159・160・163）・内視鏡と解剖5問（157・161・162・166・171）・発声機能検査2問（156・168）で、**一側麻痺＝嗄声／両側麻痺＝声は良いが呼吸困難**という対比と、**嚥下時に喉頭が「前上方」へ挙上する**ことが繰り返し問われる。⚠️ **ch07 NO.157(112A-14)・NO.171(104F-1) は器具の写真①〜⑤が選択肢そのもの**（本文には「ａ　①」しか無い）。300〜600dpiで拡大して同定した——NO.157＝①耳鏡／②吸引管／③前鼻鏡／④銃剣型ピンセット／⑤アデノイド鋭匙（正解②③）、NO.171＝①前鼻鏡／②間接喉頭鏡／③耳鏡／④Killian鼻鏡／⑤アデノイド鋭匙（正解②）。⚠️ **NO.153(118F-36) と NO.169(108I-5) は同一の喉頭内視鏡写真**（同じ章に同じ写真が2問ぶん入る。ファイル名は国試番号で分かれるので `pdf_audit.py` は通る）。⚠️ **ch07 の最難は NO.158(111B-6・正答率56%)**＝嚥下時の喉頭の移動方向は「前上方」（「前方」「後方」に票が流れる）。次いで **NO.168(109E-24・64%)＝設問は「発声機能検査」なので喉頭内視鏡ではなく最長発声持続時間(MPT)**、**NO.166(111G-26・67%)＝甲状軟骨が見えるスライスはまだ甲状腺の高さではない**。
ch08（最終章・43問＝ch03に次ぐ規模）の軸は**急性喉頭蓋炎12問（174・176・181・194・200・203・206・207・209〜211・213）・扁桃周囲膿瘍5問・頭頸部癌の亜部位当て13問・喉頭全摘後の身体2問・アデノイド増殖症3問・良性の声帯病変5問**で、**「気道が危ないか」→「癌か炎症か」→「どの亜部位か」の3段で切る**。急性喉頭蓋炎の答えはほぼ「まず気道確保」の一択。亜部位は**上咽頭＝EBV・滲出性中耳炎（成人の片側性滲出性中耳炎をみたら上咽頭を観る＝175・185・202の3問で反復）／中咽頭＝HPV（p16）／下咽頭＝飲酒（ALDH2）と食道重複癌／声門＝喫煙・早期から嗄声**で決まる。
⚠️ **ch08 の最難は NO.184(113A-23・正答率23%)＝喉頭全摘後は「気道と食道が完全分離されるので誤嚥しない」**（「喉頭癌＝誤嚥」の連想で e を選ぶと外す。正解は c＝永久気管孔に水が入るので入浴に制限がある）。次いで **NO.188(108D-22・34%)＝頸部腫瘤が7か月先行し原発の症状が乏しい＝中咽頭癌**、**NO.214(104A-14・39%)＝急性扁桃炎との鑑別に使えるのは開口障害と口蓋垂の健側偏位だけ**（膿栓・扁桃腫大・リンパ節腫大は両方にあるので使えない＝「典型的だから」で選ぶと落ちる）、**NO.180(116A-24・47%)＝非喫煙非飲酒の嗄声＋顆粒状隆起＝喉頭乳頭腫**。
⚠️ **ch08 の連問は NO.209・210・211（106E-58/59/60）の3問組**（ent で唯一の3連問）。症例文は3枚すべてに載せる。**この3問の正答率は 91／87／88 で、本文余白の3つの数字は同じ y に横並び＝テキスト抽出の順（88/87/91）は紙面と逆**。**x座標で確定させる**（x=460/488/517）。巻末解答表と突合すれば検出できる。
⚠️ **ch08 で図が選択肢そのものなのは NO.182(115A-28・ポリープ様声帯＝⑤) と NO.208(106D-10・喉頭癌＝①)**。どちらも**左列①②③（上→下）＋右列④⑤**で `get_images()` の抽出順と一致（ch03 NO.54 と同じレイアウト）。
⚠️ **NO.180(116A-24) と NO.198(114F-50) は同一症例**（45歳男性・喉頭乳頭腫）。画像は別物で、198 の JPEG には「矢印＝前交連」の注記が焼き込まれている。
ch06の軸は舌癌4問・唾石症3問・耳下腺腫瘍3問・Warthin腫瘍2問・正中頸囊胞2問で、**唾液腺が腫れたら「反復するか（唾石症）／両側性か（Sjögren・Warthin）／腫瘤か」で割り、耳下腺腫瘍は顔面神経麻痺の有無で良悪を決める**という筋。⚠️ **ch06 の最難は NO.143(106D-49・正答率23%)**——主訴が「右頸部の腫瘤」なので耳下腺と結びつかず側頸囊胞/顎下腺腫瘍に流れる。**T1で健側の耳下腺が脂肪により高信号なのに患側は同じ位置が低信号腫瘤に置き換わっている＝左右の比較**が決め手。⚠️ **NO.144(100I-24) は否定形**（適切「でない」＝RI内用療法は分化型甲状腺癌の治療）。⚠️ **NO.145(114D-61) の決め手は 99mTcO4- 唾液腺シンチの集積（hot）＝Warthin腫瘍だけ**（正答率31%）。
抽出・照合の道具は `_work/ent_pdf.py`（anstable/spec/text/images/save）と `_work/verify_ent_ch.py`（§2品質基準＋§4不変条件＋巻末解答表との突合を一括検査）。巻末解答一覧表はPDF p.137-141で列のx座標は精神科・皮膚科・眼科と同一、国試番号索引はp.142-143。⚠️ **PDF p.4 は「MECマイナー講座の特徴」の見本ページで中身は精神科の問題**。本章の軸は「聴力検査の自覚的／他覚的」「オージオグラムの読み（伝音か感音か・4分法）」「小児の聴力検査の年齢別使い分け」「めまいの末梢性／中枢性トリアージと眼振」「側頭骨の解剖」の5本 |
| `泌尿器科/` | マイナー講座・泌尿器科（prefix `uro`・💦・#0891B2）。章別HTML(`ch01_hinyokika_kihon.html`〜)＋`images/`＋`uro_questions.json`（全6章の章名メタ）。HTMLが`questions_uro.json`のソース＝章ごとの生成器`_work/build_uro_ch{NN}.py`→`_work/build_uro_json.py`で再生成。耳鼻咽喉科と同方式。**全6章242問（2026-08-12完成）・章頭NO.は 1／45／70／134／187／215**（章名はPDF表記のまま「泌尿器の基本」「腎」「尿管・膀胱・尿道」「前立腺」「精巣・外性器」「泌尿器科感染症」）。**2026-08-11に第1章「泌尿器の基本」NO.1-44（44問・画像4問6枚）と第2章「腎」NO.45-69（25問・画像18問22枚）、第3章「尿管・膀胱・尿道」NO.70-133（64問・画像32問51枚＝uro最大の章）を追加**。ch02は**実質2疾患の章**で、**腎細胞癌13問（45・47〜52・54・55・59・60・65・66）と多発性囊胞腎11問（46・56〜58・61〜64・67〜69）＋馬蹄鉄腎1問（53）**。腎細胞癌は**「放射線も殺細胞性抗癌薬もホルモンも効かない」を先に消し、残る「手術／分子標的薬・免疫療法」を転移の有無で割る**（転移なし＝部分切除or腎摘／転移あり＝減量的腎摘＋分子標的薬）。多発性囊胞腎は**「診断がついたら頭部MRAで脳動脈瘤を探す」がNO.46・58・64で3回反復**する。
⚠️ **ch02 の連問は NO.61・62・63（109E-60/61/62）の3問組**（uro 初の3連問）。**症例文は3枚すべてに載せる**こと。共通ステムが参照する腹部単純CTも**3問ぶん別名で保存**してある（`109E-60_1`／`109E-61_1`／`109E-62_1`＝中身は同一。`pdf_audit.py` がファイル名の国試番号と episode の一致を見るため）。
⚠️ **NO.61-63 の正答率は本文余白に同じ y で横並びに3つ入り、テキスト抽出の順（84/81/78）は紙面と逆**。**x 座標で確定させる**（x=460→78=NO.61 / 488→81=NO.62 / 517→84=NO.63。ent ch08 NO.209-211 と同型）。巻末解答表と突合すれば検出できる。
⚠️ **NO.57（115C-41）は選択肢が表**（男児(%)／女児(%)の2列×a〜e）。PDF p.32 の該当領域を300dpiで描画して目視で読み、**列見出しを各肢に埋め込む方式**（`ｅ　男児 50％／女児 50％`）で書き起こした。
⚠️ **ch02 の最難は NO.65(107I-78・43%) と NO.67(102I-12・43%)**。65＝転移性腎細胞癌の薬は分子標的薬とインターフェロンで、**シスプラチンに票が流れる**（無効なうえ腎摘後の単腎に腎毒性）。67＝ADPKDの合併は高血圧と脳動脈瘤で、**「肝硬変」が最大の受け皿**（合併するのは肝囊胞で肝機能は正常。先天性肝線維症はARPKD）。次いで NO.51(48%)＝転移があっても原発巣を摘る**減量的腎摘除術**、NO.52(50%)＝**腫瘍塞栓は静脈系（大動脈へは進展しない）**。
⚠️ **ch02 で正答率が無いのは NO.53・54・55・69 の4問**（97H/94B/86B/99A の旧国試で出典に数字が無いだけ＝採点除外ではない）。**採点除外（解答＝なし）の問題は ch01・ch02 に無い**。
⚠️ **NO.60・66 は「教科書的にはT1a/T1bで腎部分切除だが選択肢に部分切除が無い」型**。与えられた5つの中から相対的に選ぶ＝根治的腎摘除術が正解になる。
ch01の軸は**神経支配（蓄尿＝下腹神経/陰部神経・排尿＝骨盤神経・協調＝橋）7問・尿失禁の5分類6問・尿道カテーテル手技7問・解剖（左右非対称と尿管3狭窄）6問・閉塞の高さの読み分け4問**で、**「膀胱に尿があるか」で閉塞部位を決める**（膀胱が空＝尿管閉塞→ステント/腎瘻／膀胱が張る＝尿道閉塞→カテーテル/膀胱瘻）という筋が NO.6・32・42 で反復する。
⚠️ **連問は NO.33・34（114E-41/42）と NO.35・36（113E-42/43）の2組**。**症例文は両方のカードに載せる**こと（生成器では `_STEM_33`／`_STEM_35` を両方の qt に連結）。
⚠️ **ch01 の最難は NO.11(113F-16・正答率15%)＝射精の中枢は「胸腰髄T11〜L2（交感）」**（「腰仙髄」に票が流れるがそれは勃起の中枢。Point=副交感/仙髄・Shoot=交感/胸腰髄）。次いで **NO.17(106B-5・27%)＝閉鎖神経は排尿に無関係**（泌尿器科での登場はTUR-BTの閉鎖神経反射だけ）、**NO.12(112C-21・31%)＝尿管は総腸骨動静脈の「腹側」**、**NO.18(105I-34・31%)＝切迫性は大脳（橋より上）、馬尾・糖尿病は溢流性**、**NO.41(108I-69・38%)＝修学旅行前という期限があるので三環系抗うつ薬**。
⚠️ **正答率が無いのは NO.24・25・26 の3問**（99A/94F の旧国試で出典に数字が無いだけ＝採点除外ではない）。**採点除外（解答＝なし）の問題は ch01 に無い**。
⚠️ **画像は4問6枚すべて白黒（CT・IVU・膀胱造影）で `_is_line_art()` により q95 保存**。**A/B 2枚組は NO.26(94F-28) と NO.30(116D-48)** で、どちらも図ラベルのx座標（A=左・B=右）が `get_images()` の抽出順と一致した。
ch03（64問＝uro最大）の中身は4つの塊。**尿路結石26問**（70・73〜76・80・87・95・97・98・100・102・104・105・108・110・112・113・116〜118・121・124・129・130・133）・**膀胱癌／尿路上皮癌19問**（72・81・83・84・90〜92・99・101・106・109・111・114・115・122・126〜128・131）・**膀胱尿管逆流(VUR)と小児尿路10問**（78・82・85・86・88・96・103・107・120・125）・**過活動膀胱3問**（93・123・132）。残り6問は間質性膀胱炎(71)・尿膜管癌(77)・腎盂尿管移行部狭窄(79)・血尿の精査(89)・組合せ(94)・下大静脈後尿管(119)。
ch03を貫く筋は3本。①**結石は「熱があるか」で緊急度が割れる**——発熱＋閉塞＝閉塞性腎盂腎炎はその場でドレナージ（尿管ステントか腎瘻）、**砕石は感染が鎮まってから**（NO.75・105・118で3回反復）。②**血尿は「膀胱鏡＋尿細胞診＋上部尿路の画像」の3点セット**。③**膀胱癌は筋層に入っているかで二分**（非浸潤＝TURBT＋BCG／浸潤＝膀胱全摘）。第1章の「膀胱に尿があるかで閉塞の高さを決める」筋は**NO.92**（残尿40mLなのに両側水腎症＝閉塞は尿管の高さ）で、第2章の「腎細胞癌 vs 尿路上皮癌」の対比は**NO.84・111**（腎尿管全摘除術は尿路上皮癌の術式）で回収される。
⚠️ **ch03 に連問は無い**（64問すべて独立）。**正答率が無いのは NO.97・98・99 の3問**（98I/97B/97H の旧国試で数字が無いだけ＝採点除外ではない）。**採点除外の問題も無い**。
⚠️ **ch03 の画像は32問51枚。p54(114A-57) と p67(115D-73) は `get_images()` の抽出順と図ラベル(A/B/C)の並びが食い違う**（114A-57＝抽出順1枚目がB／115D-73＝抽出順は B→C→A）。300dpiのページ画像で目視確認して `ch03_map.txt` で入れ替えた。他の多図問題は抽出順＝A,B,C で一致。
⚠️ **ch03 には同一症例のほぼ同一問題が2組ある**。**NO.100(118C-35) と NO.113(110D-35)**（51歳男性・左尿管結石・Ｘ線陰性＝尿酸結石。113にだけ画像がある）、**NO.74(119B-37) と NO.124(106D-24)**（53歳男性・痛風・尿酸排泄促進薬・Ｘ線陰性。どちらも答えは腹部単純CT）。**MECは同じ症例を年度違いで繰り返し出すので重複を消さない**。
⚠️ **ch03 の最難は NO.71(120A-57・正答率18%)＝間質性膀胱炎の治療は「膀胱水圧拡張術」**（「膀胱拡大術」に票が流れる。**拡張＝水圧で伸ばす／拡大＝腸で作り足す**は別物）。次いで **NO.118(108A-46・24%)＝結石性閉塞性腎盂腎炎は砕石ではなくドレナージ**（血小板8.0万のDIC疑いがあり経皮的腎瘻より逆行性の尿管ステント）、**NO.85(114A-57・23%)＝尿道下裂に伴うのは「陰茎の屈曲」**（尿閉・水腎症・真性尿失禁は来さない。尿道は短いだけで狭くなく括約筋は正常）、**NO.112(111I-32・36%)＝結石予防はクエン酸と水分**（**カルシウム制限は逆効果**＝腸管でシュウ酸を捕まえる網の役がある）、**NO.78(117D-51・38%)＝VURは抗菌薬予防投与＋DMSAシンチ**（腎瘻・尿管カテーテルは閉塞の処置でVURには無意味）。
**2026-08-11に第4章「前立腺」NO.134-186（53問・画像12問19枚）を追加**。ch04は**ほぼ2疾患しかない**——**前立腺肥大症／尿閉31問**（134・135・138・139・141・142・151〜154・156〜159・161・163〜165・167〜172・174・179・181〜185）と**前立腺癌21問**（136・137・140・143〜147・149・150・155・160・162・166・173・175〜178・180・186）、残る1問が夜間多尿（148）。
ch04を貫く筋は4本。①**尿閉をみたら理屈より先に導尿／尿道カテーテル**（Cr 4.4でも透析より先＝NO.153、両側水腎症でも腎瘻より先＝NO.160）。第1章の「膀胱に尿があるかで閉塞の高さを決める」筋がここで回収される。②**尿閉の引き金は市販の総合感冒薬**（抗ヒスタミン薬が膀胱を緩め、α刺激薬が尿道を締める）——**BPH＋感冒薬＋飲酒→尿閉というMECの定番症例が NO.134・157・165・168/169・171 で5回反復**。③**直腸指診の触感で良悪を分ける**（弾性硬・表面平滑＝肥大症／石様硬・表面不整＝癌。肥大症は移行領域・癌は辺縁領域＝**指で触れるのは癌の方**）。④**前立腺癌は転移の有無で治療が真っ二つ**（限局＝全摘/放射線／転移あり＝まずホルモン療法。抗癌化学療法は去勢抵抗性になってから）。
⚠️ **ch04 の連問は6組と多い**（NO.134/135・157/158・163/164・168/169・175〜177・182〜184）。**症例文は組の全カードに載せる**こと。NO.134/135 の共通ステムが参照する腹部超音波像（膀胱／両腎の2枚）は**両カードに別名で保存**してある（`120E-41_1,2` と `120E-42_1,2`＝中身は同一）。
⚠️ **3問並ぶ紙面（p.109＝NO.175-177、p.114＝NO.182-184）は正答率が同じ y に横並びで抽出順が紙面と逆**。x=460/488/517 が NO. の順（ch02 NO.61-63・ent ch08 NO.209-211 と同型）。
⚠️ **ch04 は53問すべてに正答率があり、採点除外の問題も無い**。画像12問19枚は多図問題（NO.153＝A/B/C、159・167・179＝A/B）を含め**抽出順＝図ラベル順で一致**した。
⚠️ **ch04 の最難は NO.137(118D-14・46%) と NO.177(105B-61・46%)**。137＝前立腺癌の治療3つは「精巣摘除術・放射線治療・前立腺全摘除術」で、**TURPは癌の治療ではない**（内腺を削る術式なので外腺の癌には届かない）。177＝**去勢前の転移性前立腺癌に抗癌化学療法は使わない**（まずホルモン療法。しかも本例は腎盂腎炎の活動期で骨髄機能も低下）。次いで **NO.155(51%)＝前立腺全摘後は射精できない**（前立腺と精囊＝精液を作る器官を摘出し精管も切る）、**NO.150(52%)＝根治的放射線は「組織内照射（密封小線源）とIMRT」**（前立腺は実質臓器で腔が無いので**腔内照射は不可**、電子線は深部に届かない）、**NO.140(54%)＝方針はPSA・TNM・Gleason scoreの3つ**、**NO.185(54%)＝IPSS 13点の中等症は薬物療法から**（図のスコア用紙を読ませる一手間で割れる）、**NO.144(56%)＝父親が前立腺癌＝遺伝的要因**（前立腺癌は家族集積性が明確／転移先は骨で肺肝ではない）。
⚠️ **MECは同じ症例テンプレートを年度違いで繰り返す**——ch04 では **NO.154(114E-39)≒NO.170(106A-21)**（71歳・162cm・60kg・154/82mmHg まで同一で、答えはどちらも「抗コリン薬が不適」）、**NO.178(105D-51)≒NO.186(102D-58)**（人間ドックのPSA異常→限局癌→根治治療を2つ）。重複を消さない。
⚠️ **中等症BPH（IPSS 8〜19）の答えは全部「α1遮断薬」**（NO.167・179・181・185 の4問）。**抗コリン薬が不適／誤りになる問題も4問**（NO.139・152・154・170）。この2型で ch04 の1/6を占める。
**2026-08-11に第5章「精巣・外性器」NO.187-214（28問・画像14問24枚）を追加**。ch05の内訳は**精巣腫瘍8問**（189・191・193・196・201・207・211・213）・**精巣捻転症3問**（190・205・214）・**陰囊水腫3問**（203・206・212）・**男性不妊/無精子症3問**（194・200・210）・**停留精巣2問**（187・197）・**精索静脈瘤2問**（192・208）・**陰茎癌2問**（195・209）・**勃起障害2問**（199・202）・**尿道損傷2問**（188・204）・**陰茎海綿体損傷1問**（198）。
ch05を貫く筋は4本。①**陰囊が痛いなら、まず精巣捻転症**——数時間で増強する持続痛＋精巣挙筋反射消失＋カラードプラで患側の血流低下なら**検査を足さずに緊急手術**（190・205・214）。**6〜8時間が精巣救済の境目**で、冷却・抗菌薬・経過観察・穿刺はいずれも時間を捨てる誤答。②**陰囊が痛くないなら、ペンライトで透光性**——**あり＝陰囊水腫（液体）／なし＝精巣腫瘍（充実性）**（203・206・212 ⇔ 189・196・201・207・211・213）。③**精巣腫瘍に針生検はしない**（陰囊を貫くと鼠径リンパ流へ播種経路を作る）＝**診断も治療も高位精巣摘除術が最初**。マーカーは**AFP・hCG・LDの3つ**（191）。④**男性不妊は閉塞性か非閉塞性か**——**FSH正常＋精巣容積正常＝閉塞性→精路再建術／FSH高値＋精巣容積小＝非閉塞性→micro-TESE＋ICSI**（200・210）。
⚠️ **ch05 の最難は NO.203(107I-61・正答率26%)**＝**7歳の陰囊水腫は「学校の休みに合わせて手術」**（乳幼児は自然消退するが学童期まで残ったものは治らない。**NO.206(112A-34・2歳)が「多くは自然になくなります」で正解になるのと表裏一対**で、同じ所見でも年齢で答えが逆になる）。次いで **NO.209(111D-29・30%)**＝不特定多数の性交渉歴に引きずられて性感染症を選ぶが、**1か月持続する硬結＋悪臭を伴う排膿＋真性包茎は陰茎癌**（感染症は自然軽快するか急性発症する＝経過で切る）。**NO.211(110D-27・33%)**＝**精巣は後腹膜から下降した臓器なのでリンパ流は鼠径ではなく傍大動脈へ直接向かう**（転移再発は後腹膜リンパ節と肺。「陰囊にあるから鼠径」と考えると外す。**陰茎癌が鼠径なのは皮膚由来だから**＝NO.195と対比）。**NO.210(110A-11・40%)**＝**閉塞性無精子症と低ゴナドトロピン性性腺機能低下症の対応が入れ替えてある**（正しい治療名同士を誤った組合せで並べるので違和感が出ない）。
⚠️ **NO.192(117D-55) と NO.208(111A-33) は同一症例のほぼ同一問題**（20歳男性・左精索静脈瘤・写真A/超音波B/カラードプラC まで同じで選択肢の並びだけが違う。正答率は 83%→97% に上がっている）。画像は国試番号ごとに**別名で二重保存**（117D-55_1〜3 と 111A-33_1〜3＝中身は同一）。**重複を消さない**。
⚠️ **ch05 に連問は無い**（28問すべて独立）。**28問すべてに正答率があり、採点除外の問題も無い**。3問横並びの紙面も無い。
⚠️ **図ラベルと `get_images()` の抽出順が食い違うのは2問**——NO.192(117D-55)＝抽出順は **B→C→A**、NO.213(108I-80)＝抽出順は **B→A**。110dpiのページ画像で目視確認して `ch05_map.txt` で入れ替えた。他は抽出順＝A,B,C で一致。
**2026-08-12に第6章「泌尿器科感染症」NO.215-242（28問・画像4問9枚）を追加し全6章242問が完成した。これでマイナー講座5科目（psy/derm/oph/ent/uro）がすべて完成**。ch06は大きく2つに割れる——**腎盂腎炎（診察・原因菌・検査・治療・敗血症）16問**（216・218〜222・231〜239・242）と**性感染症（淋菌・クラミジア・HPV）8問**（215・217・225・226・228・230・240・241）、残りが**前立腺炎2問**（223・229）・**ムンプス精巣炎1問**（224）・**尿路感染の局在1問**（227）。
ch06を貫く筋は4本。①**熱があるかどうかで「下部（粘膜）」と「上部・実質」を割る**（膀胱炎・尿道炎は発熱しない／腎盂腎炎・前立腺炎・精巣上体炎・腎膿瘍は発熱する＝227が代表）。②**腎盂腎炎の診察は「肋骨脊柱角(CVA)の叩打痛」ただ1つ**——**218・234・237・238の4問が全部この所見を答えさせる**（本章で最も繰り返される正解）。③**抗菌薬で治らない尿路感染は、感染症ではなく「閉塞・膿瘍」の問題**（成人男性の腎盂腎炎は尿管嵌頓結石を疑う＝219／72時間解熱しなければ血液培養と造影CT＝222）。第1章・第3章の筋がここで回収される。④**尿道炎は淋菌かクラミジアかの二択**（Gram染色で陰性双球菌が見えれば淋菌・潜伏期2〜7日・膿性／見えなければクラミジア・潜伏期1〜3週・漿液性。**確定診断はどちらも初尿の核酸増幅検査**、**淋菌の第一選択はセフトリアキソン点滴静注でニューキノロンは耐性**）＝215・217・225・228・230・241の6問が1枚の表で解ける。
⚠️ **ch06 の連問は4組**（NO.220〜222／NO.231〜233／NO.235・236／NO.238・239）。**症例文は組の全カードに載せる**こと。NO.231〜233 の共通ステムが参照する腹部単純CT(A)は**3問ぶん別名で保存**（`116C-63_1`／`116C-64_1`／`116C-65_1`＝中身は同一）。
⚠️ **NO.221(118F-68) は選択肢が a〜i の9択**（表2のセルA〜Iを選ばせる）。5択決め打ちのコードを書かないこと。表1・表2・Cockcroft-Gaultの式は PDF p.142 を220dpiで描画して目視で読み qt に `<table class="tb">` で書き起こした。**CrCl＝(140－80)×72/(72×1.0)＝60mL/min＞50 → アンピシリンはR、メロペネムは温存 → セファゾリンのD**。⚠️**最大の罠は入院時のCr 1.3で計算すると約46mL/minでEに落ちること**——設問が「現時点での血清クレアチニンは1.0mg/dL」とわざわざ断っている。
⚠️ **NO.216(120B-18) は Gram染色写真①〜⑤が選択肢そのもの**。紙面は**上段①②③／下段④⑤**で `get_images()` の抽出順と一致（300dpiで目視確認）。正解 d＝④は**赤く染まる短桿菌＝Gram陰性桿菌＝大腸菌**。
⚠️ **NO.215(120A-24) と NO.230(117D-68) は同一症例のほぼ同一問題**（文章が完全に同じで、選択肢ｂが「パートナーの女性に感染する」→「女性では骨盤炎症性疾患の原因となる」に差し替わっただけ。正答率は68%→97%）。**重複を消さない**。
⚠️ **ch06 は28問すべてに正答率があり、採点除外の問題も無い**。3問並ぶ紙面（p.142＝NO.220-222、p.148＝NO.231-233）の正答率は**x=460/488/517 が NO. の順**で、巻末解答表と全28問一致した。
⚠️ **ch06 の最難は NO.222(118F-69・正答率53%)**＝適切な抗菌薬でも4日目に発熱が残るなら**血液培養と造影CT**（膿瘍・閉塞を探す）。前立腺生検・薬剤リンパ球刺激試験に票が流れる。次いで **NO.241(109A-12・61%)＝クラミジアとの混合感染は20〜30％で「90％」は嘘**（正しい記述に数字だけ誤りを混ぜてある）、**NO.224(111A-23・63%)＝耳下腺炎の4〜8日後の片側精巣腫大はムンプス精巣炎**（急性精巣上体炎に流れる）、**NO.231(116C-63・66%)＝敗血症性ショックはまず細胞外液30mL/kgの急速輸液**（アドレナリンはアナフィラキシーの薬。CTに水腎症が無く尿管ステントも急がない）、**NO.221(118F-68・67%)＝de-escalationでカルバペネムを温存しセファゾリンへ**。
抽出・照合の道具は `_work/uro_pdf.py`（anstable/spec/text/images/save）と `_work/verify_uro_ch.py`。巻末解答一覧表はPDF p.155-159で列のx座標は精神科・皮膚科・眼科・耳鼻咽喉科と同一、国試番号索引はp.160-161。⚠️ **PDF p.4 は「MECマイナー講座の特徴」の見本ページで中身は精神科の問題** |
| `整形外科/` | マイナー講座・整形外科（prefix `ortho`・🦴・#A16207）。章別HTML(`ch01_seikeigeka_kihon.html`〜)＋`images/`＋`ortho_questions.json`（全6章の章名メタ）。HTMLが`questions_ortho.json`のソース＝章ごとの生成器`_work/build_ortho_ch{NN}.py`→`_work/build_ortho_json.py`で再生成。泌尿器科と同方式。**全6章174問（2026-08-13完成）・章頭NO.は 1／31／74／107／140／167**（章名はPDF表記のまま「整形外科の基本」「神経の障害」「関節疾患」「骨　折」「その他の疾患」「ロコモティブシンドロームとサルコペニア・フレイル」）。**2026-08-12に第1章「整形外科の基本」NO.1-30（30問・画像10問19枚）と第2章「神経の障害」NO.31-73（43問・画像10問17枚＝ortho最大の章）、第3章「関節疾患」NO.74-106（33問・画像19問29枚）、第4章「骨　折」NO.107-139（33問・画像19問**39枚＝ortho最多の画像章**）、第5章「その他の疾患」NO.140-166（27問・画像18問31枚）、2026-08-13に第6章「ロコモティブシンドロームとサルコペニア・フレイル」NO.167-174（8問・**画像0枚＝ortho唯一**）を追加**。
ch06（8問＝**本科目の最小章**）の内訳は**フレイル4問**（167身体的フレイルの判定／170フレイル高齢者への対応／172予防策／173介護予防）・**ロコモティブシンドローム3問**（169指導／171対応／174概念）・**低栄養1問**（168入院時の栄養評価）。
ch06を貫く筋は4本。①**3つの概念は「見ている範囲」が違うだけで同じ坂道**——**ロコモ＝運動器／サルコペニア＝骨格筋／フレイル＝心身と社会を含む全体**で、いずれも**要介護の手前にある可逆的な状態**＝介護予防の標的（173がその答え）。②**身体的フレイルは J-CHS の5項目で「数える」**——**体重減少（6か月2〜3kg）・疲労感・歩行速度1.0m/秒未満・握力（男28／女18kg未満）・身体活動低下**の5つで3項目以上。**日中の眠気・認知機能・抑うつは入らない**（167）。③**高齢者に「動かさない・食べさせない」指導をしたら、その肢は誤り**——安静・外出制限・減量・蛋白制限が本章の誤答肢の全部（169・170・171・172）で、**第3章の変形性関節症「動かさない方向の肢は例外なく誤り」がここで回収される**。④**体重が落ちた高齢者は、器質的疾患を除外したら社会と生活を診る**（独居・死別・孤食・閉じこもり・服薬アドヒアランス・認知機能・口腔＝170）。
⚠️ **ch06 の最難は NO.170(117D-25・正答率53%)＝栄養指導・認知機能評価・介護認定の申請**。**票が流れるのは c 降圧薬減量**で、これは「間違った治療」ではなく**順番が飛んでいる対応**——**家庭血圧130台/70台は75歳以上の目標（家庭135/85未満）内でコントロール良好**、かつ **eGFR 34.7＝CKD G3b で降圧は継続すべき**。**「残薬が多い」は減薬の根拠ではなく、飲めない理由（認知機能・意欲・独居）を評価すべきサイン**。次いで **NO.169(118B-32・62%)＝ロコトレ（片脚立ちとスクワット）**——**BMI 24.4は65歳以上の目標21.5〜24.9の範囲内で減量指導は誤り**、**ビール350mL週3回は節度ある適度な飲酒の範囲**。残る6問はすべて正答率96%以上の必修レベル。
⚠️ **NO.167(120C-44) と NO.171(117E-33) は本文中の下線部①〜⑤が選択肢そのもの**（ch05 NO.142 と同じ型）。**ステムの該当箇所を `<u>` で囲まないと解けない**。**NO.171 は症例文ではなく「研修医と指導医の会話」の中に下線がある**。
⚠️ **本章に画像は1枚も無く（ortho で唯一）、連問も無い。8問すべてに正答率があり採点除外も無い**。
⚠️ **NO.171 の「横断歩道を青信号のうちに渡りきれない」は歩行速度1.0m/秒未満の生活語訳**（横断歩道の信号は1m/秒で設計されている）。
ch05（27問）の内訳は**骨腫瘍・軟部腫瘍7問**（146・150・152・154・155・161・166）・**転移性骨腫瘍／転移性脊椎腫瘍6問**（148・149・156・162・164・165）・**骨と関節の感染10問**（141〜143化膿性脊椎炎／144・145・159・160化膿性関節炎／151腸腰筋膿瘍／157骨髄炎）・**漏斗胸3問**（140・147・158）＋153緊急手術・163モルヒネの副作用。
ch05を貫く筋は4本。①**骨腫瘍は「年齢・部位・エックス線像」の3点で決まる**（骨肉腫＝10代・膝周囲の骨幹端・Codman三角とsunburst／骨巨細胞腫＝20〜40歳・**骨端**・soap bubble／骨軟骨腫＝10代・骨幹端・関節から遠ざかる向きの隆起で**皮質と髄腔が母骨と連続**／類骨骨腫＝夜間痛とNSAID著効）。②**腫瘤を見たら「診断がつく前に手を出さない」**——**146・161は同型の設問で、切開排膿・ギプス・鎮痛薬・抗菌薬という「決めつけて処置する」肢を並べて「生検」を選ばせる**。③**膿があるところは、薬より先にドレナージ**（閉鎖腔＝腱鞘・関節・硬膜外・区画は内圧上昇で血流が絶たれ抗菌薬が届かない。143硬膜外膿瘍・145/160化膿性関節炎・151腸腰筋膿瘍・153壊死性筋膜炎が全部これ）。④**安静で軽快しない背部痛は red flag＝腫瘍・感染・血管**（148・149・156・164・165。**まず単純エックス線ではなくMRI**——**単純エックス線は骨量が30〜50％失われるまで写らない**）。
⚠️ **ch05 の連問は NO.141・142・143（120C-73/74/75）の3問組**。**症例文は3枚すべてに載せる**こと（生成器では `_STEM_141` を3問の qt に連結）。共通ステムが参照する A(腰椎MRI)・B(Gram染色) も**3問ぶん別名で保存**（`120C-73_1,2`／`120C-74_1,2`／`120C-75_1,2`＝中身は同一）。
⚠️ **NO.142(120C-74) は症例文中の下線部①〜⑤が選択肢そのもの**（ａ　①〜ｅ　⑤）。ステムの該当箇所を `<u>` で囲まないと解けない。**正解は③血液透析＝週3回の血管穿刺＋尿毒症性免疫低下で黄色ブドウ球菌菌血症の最大のリスク**（①胃全摘・②脾摘・④飲酒・⑤カメ＝Salmonella はいずれも別の感染と結びつく）。**この型は「先に起炎菌を決めて入口を逆引きする」**。
⚠️ **図ラベルと `get_images()` の抽出順が食い違うのは3問**——**NO.145(119A-43)＝B→A／NO.149(113A-67)＝B→C→A／NO.151(111A-50)＝B→C→A**。150dpiのページ画像で目視確認して `ch05_map.txt` で入れ替えた。NO.140・150・152・161・166 は抽出順＝A,B で一致。
⚠️ **ch05 の最難は NO.153(105D-19・正答率22%＝ortho全体でもNO.45(9%)・NO.82(16%)に次ぐ)＝緊急手術を要するのは壊死性筋膜炎・手の化膿性腱鞘炎・乳児化膿性股関節炎の3つ**。**蜂巣炎は「派手に腫れるが切って出す膿がない」、Brodie骨膿瘍は「名前は膿瘍だが実体は慢性限局性骨髄炎」**で落とす。**選び落とされやすいのは手の化膿性腱鞘炎**（腱鞘は閉鎖腔で半日で腱が壊死する）。次いで **NO.149(113A-67・39%)＝麻痺を伴う転移性脊椎腫瘍は除圧固定術と放射線照射**——**票はc抗癌化学療法（時間軸が合わない）とd残存機能訓練（MMT 0〜1を見て諦める）へ流れるが、麻痺の完成は"今朝"で48時間以内なら戻る＝"どれだけ悪いか"ではなく"いつからか"で決める**。**NO.155(102I-54・32%)＝骨軟骨腫**（「無痛性の隆起」の5文字で**外へ出るのか内を食うのか**という一軸を使えば4つ消える）、**NO.152(106A-26・40%)＝骨巨細胞腫は病巣掻爬・骨移植**（**放射線は悪性転化のリスクで原則禁、化学療法は無効**）、**NO.154(105D-29・40%)＝類骨骨腫**（10歳・大腿骨まで骨肉腫と一致するので割れる。**分岐は①局所に腫脹も圧痛も無い②CTでnidusと同心円状の骨硬化**）。
⚠️ **NO.162(115D-17・90%) は「適切でない」を選ぶ設問で、正解ｂ＝塩酸モルヒネ皮下投与**。**モルヒネ自体が誤りなのではなく投与経路**——**症例文の「食欲は良好」がWHO方式の第一原則 by mouth を問うための一文**（かつ非オピオイドすら未使用）。
⚠️ **ch05 で正答率が無いのは NO.157（95B-58）の1問のみ**（旧国試で出典に数字が無いだけ＝採点除外ではない。**JSON上は `rate:-1`** で、ch01 の NO.15・16・17 と同じ扱い）。**採点除外（解答＝なし）の問題は ch05 に無い**。
ch04（33問）の内訳は**コンパートメント症候群7問**（107・112・120・131・135・136＋116のVolkmann拘縮）・**大腿骨近位部骨折5問**（114・121・130・134・137）・**脊椎圧迫骨折3問**（108・109・110＋122のステロイド性）・**小児の肘周辺外傷4問**（112・119・135・139）・**開放骨折3問**（115・125・138）・**骨折治癒2問**（113・123）・**骨粗鬆症の易骨折部位2問**（126・127）＋単発（111手指X線・117人工股関節脱臼・118 CRPS・124副子固定範囲・128ギプス後指導・129足関節不安定性・132足関節脱臼骨折・133歩行補助具）。
ch04を貫く筋は4本。①**骨そのものより「数時間で手足を失う合併症」を先に見る**——コンパートメント症候群と開放骨折で章の3分の1を占める。**5Pは順番が命で、疼痛と他動伸展時痛が最も早く、脈拍消失は最も遅い**（太い動脈は区画内圧では潰れないので「触れるから大丈夫」は誤り）。②**進行度で「測る」と「切る」が入れ替わる**——**NO.135(拍動が「弱い")＝区画内圧測定／NO.112(拍動を「触知しない」・受傷8時間)＝ただちに筋膜切開**。**NO.131 は判断指標そのものを問い（区画内圧と橈骨動脈）、創の有無・転位の程度・皮下出血は区画内圧を反映しないので誤り**。③**高齢者の骨折は「骨」ではなく「臥床」との戦い**——大腿骨近位部骨折は**可及的早期（原則48時間以内）の手術と早期離床**で、**牽引・ギプス・車椅子生活への移行はすべて誤答**（NO.121・130）。方針は**年齢ではなく受傷前のADL**で決める。④**固定は上下1関節ずつ、荷重は許さない**（NO.124・128）。
⚠️ **ch04 の最難は NO.124(103H-21・正答率38%)＝脛骨骨折の副子は「足部から大腿部まで」**。**本文に選択肢が1つも無く、模式図 a〜e が選択肢そのもの**（側臥位の下肢・足元が左）。600dpiで拡大して同定した——**ａ足関節〜膝／ｂ足部〜膝／ｃ足関節〜大腿／ｄ足部〜大腿（正解）／ｅ足部〜股関節**。**見分けるのは「足元に立ち上がった矢印があるか（＝足部を巻き込むか）」と「右端が膝の山を越えるか」の2点だけ**。なお**解答表のこの行の国試番号は `103H-21※不正解者のみ採点除外`＝正解 d は存在するので bx バッジも rate=None も使わない**（`verify_ortho_ch.py` の kid 突合を `※.*$` を落とす形に直した）。
⚠️ 次いで **NO.119(108D-54・44%)＝小児肘の2診断は「肘関節脱臼」と「上腕骨外側顆骨折」**（顆上骨折が最多なので d に流れるが、**骨折線が顆上部でなく骨端核を含む外側顆にあり、かつ関節の対応関係が失われている**）。**NO.118(109D-44・46%)＝橈骨遠位端骨折後3か月のアロディニア＋発汗亢進＋全関節の拘縮＋斑状骨萎縮＝複合性局所疼痛症候群**（**合言葉は「不釣り合い」と「越境」**＝損傷に不釣り合いな痛みが神経支配や骨折部位を越えて広がる。**両手を撮ってあるのは健側と比較させるため**）。**NO.132(113B-36・51%)＝足関節脱臼骨折はまず整復**——**症例文が開放創なし・拍動触知可・循環安定と危険所見を全部否定してあるので「急がなくてよい」と誤読させる罠**で、正しくは「まだ血流も神経も保たれているうちに整復する」。
⚠️ **写真①〜⑤が選択肢そのものなのは NO.111(119E-12・手指X線／正解 e＝⑤の DIP関節部の骨折) と NO.133(113F-33・歩行補助具／正解 c＝③松葉杖)**。どちらも `get_images()` の抽出順が紙面と一致した（**NO.111 上段①②③＋下段④⑤／NO.133 左列①②③＋右列④⑤**）。**NO.133 は「完全免荷」と「屋外歩行」の2条件で一意に決まる**（①歩行車・②固定式歩行器＝屋内／④多脚杖・⑤ロフストランド杖＝部分荷重）。
⚠️ **NO.123(103E-68) は a〜h の8択**（骨折治癒過程で最も遅いもの＝b皮質骨形成）。5択決め打ちのコードを書かないこと。
⚠️ **図ラベルの抽出順が紙面と食い違うのは NO.115(115A-59)＝B→A と NO.131(114A-71)＝B→A の2問**。116・119・124・128・129・135・139 は抽出順＝A,B,C で一致。**NO.113(117C-49) は「来院時」「術後8週」の2枚が1枚の合成画像**で、A/Bラベルの検出には掛からない。
⚠️ **NO.114(117E-34) と NO.134(111H-22) は同一症例のほぼ同一問題**（77歳女性・玄関で倒れている・バイタルも選択肢も同一で、正答率93%/91%）。**重複を消さない**。
⚠️ **ch04 は33問すべてに正答率があり、採点除外（解答＝なし）の問題も無い**。
ch01は**MMT（徒手筋力テスト）8問**（2・7・9・10・18・20・26・28）が最大の塊で、**関節軟骨・骨組織4問**（11・13・14・15）・**人工関節置換術3問**（27・29・30）・**装具/歩行補助具3問**（4・5・22）・**筋の作用3問**（10・16・17）・**ROM3問**（3・19・20）・**骨形成不全症2問**（8・24）が続く。
ch01を貫く筋は4本。①**MMTは「重力」と「抵抗」の2軸だけ**——**3＝重力に抗して全可動域／2＝重力を除けば全可動域／1＝収縮のみ**。この3つの境目で6段階すべてを再構成できる（NO.2・18・26・28が全部これ）。②**MMTの図問題は「関節と運動方向」→「主動作筋」の順に読む**（端座位で大腿挙上＝腸腰筋／側臥位で上側の下肢挙上＝中殿筋／背臥位で頭部挙上＝胸鎖乳突筋）。③**装具は「またぐ関節」で適応が決まる**——短下肢装具は足関節しかまたがないので適応も下垂足・内反尖足に限られる（NO.4・5・6が同じ原則で解ける。頸椎転移に腰椎コルセットは無意味）。④**関節軟骨は無血管・無神経・無リンパ**——ここから「自然修復しない」「痛まない」「加齢で菲薄化する」がすべて導ける（NO.11・13・14）。
⚠️ **ch01 の最難は NO.27(104G-30・正答率35%＝章最低)**＝人工膝関節置換術の禁忌は**活動性感染と神経病性関節症（Charcot関節）**（「3つ選べ」で禁忌2つを正確に外す必要がある）。次いで **NO.11(106B-4・43%)＝自然修復しないのは関節軟骨**、**NO.10(107H-10・53%)＝端座位で大腿を挙げる＝腸腰筋**（腹直筋は骨盤で止まり大腿骨に付かない）、**NO.14(100G-48・54%)＝加齢変化は「硝子様軟骨の菲薄化」**（弾性線維は弾性軟骨のもので関節軟骨に無い）、**NO.29(103G-17・61%)＝骨セメント挿入症候群では血圧が「低下」する**（人工関節はMRI禁忌ではない）。
⚠️ **NO.10(107H-10) と NO.17(93D-8) は「股関節を屈曲する筋」を問う同型の設問だが正解が違う**——10は腸腰筋が肢にあるので腸腰筋、17は腸腰筋が無いので大腿四頭筋（大腿直筋＝二関節筋）。**「並んでいる肢の中で最も適切なもの」を選ぶ**。⚠️ **NO.20(112B-4) は「入れ替え型」**＝大腿周径（基準点から一定距離）と下腿周径（最大周径）の主語だけを交換してある。
⚠️ **写真①〜⑤が選択肢そのものなのは NO.5(111B-40・歩行補助具) と NO.22(109E-55・歩行補助具)**。**NO.5 は `get_images()` の抽出順が紙面と食い違う**（紙面は上段①②③＝x168/297/427・下段④⑤／抽出順は ②③①④⑤）ので `ch01_map.txt` で入れ替えた。**NO.22 と NO.28(A/B) は抽出順＝紙面順で一致**。
⚠️ **ch01 に連問は無く、採点除外（解答＝なし）の問題も無い**。**正答率が無いのは NO.15・16・17 の3問**（98G/97G/93D の旧国試で出典に数字が無いだけ）。
ch02（43問＝ortho最大）の内訳は**頸髄損傷・脊髄損傷10問**（36・42・44・47・50・52・55〜57・61・66）・**手根管症候群6問**（45・54・58・63・64・69）・**腰椎椎間板ヘルニア5問**（32・37・40・51・68）・**腓骨神経麻痺4問**（39・65・70・73）・**橈骨神経麻痺3問**（46・59・62）・**脊柱側弯症3問**（31・35・72）・**後縦靱帯骨化症3問**（43・49・53）・**肘部管症候群2問**（67・71）。
ch02を貫く筋は4本。①**末梢神経麻痺は「どの神経が・どこで締まって・何を支配するか」の3点セット**——正中/手根管＝母指〜環指橈側の掌側と母指球筋、尺骨/肘部管＝環指尺側と小指と骨間筋、橈骨/上腕骨骨幹部＝下垂手と手背橈側、総腓骨/腓骨頭＝下垂足と足背。第1章NO.18の「腓骨頭の圧迫＝下垂足＝短下肢装具」がここで回収される。②**頸髄損傷は「C6かC7か」で到達ADLが割れる**——**C6＝手関節背屈が残り万能カフとtenodesisで食事は自立するがプッシュアップはできない／C7＝上腕三頭筋が働き移乗・除圧・自己導尿・更衣が自立**。**NO.42・47（C6）とNO.61・66（C7）の4問がこの一点だけで解ける**。③**脊椎疾患は「上位運動ニューロンか下位運動ニューロンか」で切る**——**腱反射が低下している高さが病巣、亢進している高さはその下**（NO.33）。**L1/2より下に脊髄は無いので腰椎病変でBabinski・痙性歩行は出ない**（NO.40・49）。④**側弯症はAdams前屈テストで肋骨隆起**——NO.31・35・72の3問が全部これに収束し、**痛み（叩打痛）があれば特発性ではない**。
⚠️ **ch02 の最難は NO.45(111D-7・正答率9%＝本科目で最低)＝手根管症候群で筋力低下が出るのは短母指外転筋**。**母指球筋のうち母指内転筋だけが尺骨神経支配**という一点で割れる（「母指の筋＝正中神経」と括ると外す）。加えて**方形回内筋は正中神経支配だが手根管より近位で分岐するので無事**＝「絞扼部位より近位の枝は残る」。次いで **NO.46(111F-7・29%)＝上腕骨骨幹部骨折は橈骨神経麻痺だが、上腕三頭筋枝は橈骨神経溝より近位で分岐するので肘関節伸展は残る**（「肘関節伸展」が最大の受け皿）、**NO.53(104A-42・31%)＝頸椎OPLLの脊髄症が軽微な外傷で急性増悪＝まず安静**（手術は選択肢に無い。硬膜外ブロックは無効かつ有害）、**NO.48(110I-3・36%)＝線維束性収縮だけが前角細胞の変性＝C6病変ではS1の前角は壊せないのでALSの所見**、**NO.49(110I-32・45%)＝胸椎OPLL＝上肢正常の痙性対麻痺／腰部脊柱管狭窄症の馬尾型＝会陰部の異常感覚**。
⚠️ **ch02 の連問は NO.55・56・57（118F-64/65/66）の3問組**（脊髄損傷・orthoで初の連問）。**症例文は3枚すべてに載せる**こと（生成器では `_STEM_55` を3問の qt に連結）。
⚠️ **NO.32(120A-49) と NO.39(118A-50) は英語問題**。qt・選択肢は原文どおり英語で書き、解説は日本語（`questions_circ.json` の 116E-38 等と同じ扱い）。
⚠️ **NO.42(114F-15) は写真①〜⑤が選択肢そのもの**。紙面は**上段①(x291)②(x424)／下段③(x150)④(x283)⑤(x422)** という2+3レイアウトだが `get_images()` の抽出順と一致した（200dpiで目視確認済み）。①インセンティブスパイロメトリー／**②万能カフ付きフォーク＝正解**／③電動車椅子／④上肢懸垂装置／⑤床走行式リフト。
⚠️ **NO.71(113D-56) の2枚組はA/Bラベルではなく「右」「左」**（1=右＝健側、2=左＝患側）。`cmd_images` の図ラベル検出には引っかからないので目視で確認した。NO.40・NO.53 の A/B は A=左・B=右で抽出順と一致。
⚠️ **NO.61(116A-69) と NO.66(115D-25) は同一のMMT（上腕二頭筋5・橈側手根伸筋5・上腕三頭筋4・深指屈筋0・小指外転筋0）で問い方だけが違う**（獲得ADL／屋内の移動手段）。**MECは同じ症例テンプレートを年度違いで繰り返すので重複を消さない**。
⚠️ **ch02 は43問すべてに正答率があり、採点除外の問題も無い**。
ch03（33問）の内訳は**変形性関節症9問**（膝＝81・93・94・97／股＝96・99〜101・103）・**膝の靱帯と半月板6問**（79・89・92・102・105）・**肘内障3問**（74・82・95）・**腱板断裂3問**（75・80・104）＋**肩関節脱臼1問**（76）・**小児の股関節5問**（86すべり症・88先天股脱・90 Perthes・106 DDH＋99の成人像）・**骨壊死とoveruse3問**（78・91・98）・その他（77 de Quervain・83外反母趾・84術後発熱・85アキレス腱・87離断性骨軟骨炎）。
ch03を貫く筋は4本。①**変形性関節症の保存療法は「減量・運動療法・杖／装具」の3本柱**——**「安静・固定・杖を避ける・ステロイド内服」という“動かさない方向”の選択肢は例外なく誤り**（NO.81・94・96・97の4問がこの一点で解ける）。②**膝の外傷は症状の言い回しで壊れた組織が決まる**——**ロッキング（曲がったまま伸ばせない）＝半月板／giving way（ガクッと崩れる）＋血性関節液＝前十字靱帯**。検査は**Lachman＝ACL・外反ストレス＝MCL・McMurray＝半月板**と「加える力の向き」で対応する。③**小児の股関節疾患は年齢と発熱の2軸で振り分ける**——**乳児＝DDH／4〜8歳＝Perthes病（血行障害）／10〜14歳の肥満＝大腿骨頭すべり症／発熱＋強い炎症＝化膿性股関節炎（緊急）**。しかも**小児は「膝が痛い」と言って股関節疾患で来る**（NO.86・90・99で3回反復）。④**過負荷〈overuse〉と血行障害は別系統**——腰椎分離症・離断性骨軟骨炎・Osgood-Schlatter病は使いすぎ、**大腿骨頭壊死症はステロイド／アルコール／外傷／減圧症／放射線**（NO.78・91・98の3問が同じ表を3方向から問う）。
⚠️ **ch03 の最難は NO.82(117D-11・正答率16%＝本科目でNO.45(9%)に次ぐ)＝肘内障の肢位は「上肢下垂位」＋「前腕回内位」**。**正しくは肘“軽度屈曲”位なので「肘関節伸展位」は誤り**（だらりと下げている＝伸びている、と早合点させる罠）。次いで **NO.85(114D-31・30%)＝アキレス腱断裂の保存療法は背屈制限装具**（尖足位で断端を近づける。**Thompsonテストは「底屈しない」が陽性**で、肢dは正常所見を書いている）、**NO.78(119D-6・46%)＝SLE→ステロイド→大腿骨頭壊死**、**NO.76(119A-9・46%)＝再脱臼は若年者ほど多い**（Bankart損傷が残るため。合併は腋窩神経麻痺、脱臼は前方95%）、**NO.77(119A-39・47%)＝母指を握り込んで尺屈で痛い＝de Quervain病**（神経の絞扼ではないのでしびれも筋力低下も出ない）、**NO.84(115D-22・54%)＝人工膝関節全置換術“翌朝”の37.4℃は吸収熱で広域抗菌薬は不要**（創部正常・白血球正常）。
⚠️ **ch03 の連問は NO.99・100・101（112F-81/82/83）の3問組**（73歳・変形性股関節症）。**症例文は3枚すべてに載せる**こと（生成器では `_STEM_99` を3問の qt に連結。**NO.100 の「その後の経過」はその設問だけのものなので `_STEM_99` には入れない**）。
⚠️ **NO.89(105D-59) は模式図のラベルａ〜ｆが選択肢そのもので、本文に選択肢が1つも無い**。600dpiで拡大して同定した——**左膝を前方から見た図なので画像の左＝内側・右＝外側**（右下に腓骨が描かれているのが決め手）。**ａ＝前十字靱帯（正解）／ｂ＝外側半月／ｃ＝外側側副靱帯／ｄ＝内側半月／ｅ＝内側側副靱帯／ｆ＝後十字靱帯**。**6択なので5択決め打ちのコードを書かないこと**。
⚠️ **NO.92(116A-45) は関節鏡写真①〜⑤が選択肢そのもの**。紙面は**左列①②③（上→下）＋右列④⑤**で、`get_images()` の抽出順（①→③→⑤→②→④）と食い違うので `ch03_map.txt` で入れ替えた。**正解は d＝④（半月板の遊離縁が破綻し断片が舌状に翻転）**で、**③は正常な前十字靱帯**（中央の縦溝は前内側束と後外側束の境界であって断裂ではない＝最大の罠）。⚠️**spec の ans を読み違えないこと**——一度 c と読み誤り、画像の読みと矛盾して初めて気づいた。**画像の所見と正解が噛み合わないときは自分の読みではなく解答表を疑い、`anstable.json` の当該行を直接見る**。
⚠️ **ch03 の多図問題で `get_images()` の抽出順が紙面と食い違うのは2問**——NO.79(119D-41)＝**B→A**、NO.86(112D-32)＝**B→A→C**。NO.80・87・102 は抽出順＝A,B で一致した。
⚠️ **ch03 は33問すべてに正答率があり、採点除外の問題も無い**。
⚠️ **同じ「徒手整復」が“適切でない”の正解になる問題が2問あり、×になる理由が違う**——NO.83（外反母趾）とNO.96（変形性股関節症）は**整復すべき「ずれ」が存在しない（変形であって脱臼ではない）**、NO.88（乳児のDDH）は**脱臼はしているが徒手整復＋ギプスが大腿骨頭壊死を招く**（正しい初期治療はリーメンビューゲル装具）。
⚠️ **PDF全体で連問は4組**（NO.55-57／99-101／108-109／141-143）・**複数選択27問**（2つ選べ17／3つ選べ10）・**正答率なし4問**（15・16・17・157）・**採点除外0問**。
⚠️ **本PDFは各ページの左右端に章名の縦書きが1文字ずつ入る**（「神」「経」「の」「障」「害」…）。本文テキストに混じるので `ortho_pdf.py` の `cmd_text` は **x<40 / x>540 の単語を落として** dump する。
抽出・照合の道具は `_work/ortho_pdf.py`（anstable/spec/text/images/save）と `_work/verify_ortho_ch.py`。巻末解答一覧表はPDF **p.109-112**で列のx座標は精神科・皮膚科・眼科・耳鼻咽喉科・泌尿器科と同一、国試番号索引はp.113-114。⚠️ **PDF p.4 は「MECマイナー講座の特徴」の見本ページで中身は精神科の問題** |
| `麻酔科/` | マイナー講座・麻酔科（prefix `anes`・💉・#7C3AED）。章別HTML(`ch01_shujutsuki.html`〜)＋`images/`＋`anes_questions.json`（全2章の章名メタ）。HTMLが`questions_anes.json`のソース＝章ごとの生成器`_work/build_anes_ch{NN}.py`→`_work/build_anes_json.py`で再生成。整形外科と同方式。**全2章52問（2026-08-13完成・マイナー講座で最小）・章頭NO.は 1／36**（章名はPDF表記のまま「周術期の麻酔」「緩和医療」。PDFは各章が★問題／無印問題の2ブロックに分かれるが章としては割らない）。**2026-08-13に第1章「周術期の麻酔」NO.1-35（35問・画像8問17枚）と第2章「緩和医療」NO.36-52（17問・画像1問1枚）を追加して全2章52問が完成。これでマイナー講座7科目（psy/derm/oph/ent/uro/ortho/anes）がすべて揃った**。
ch02（17問）の内訳は**WHO方式の原則・ラダー5問**（51ラダー／52 5原則／46投与量調節／50併用／48第1段階から）・**オピオイドの導入と強化5問**（37まずオピオイド／39経口で追加／44経口へ／41適切でない＝皮下注／43適切でない＝弱オピオイド追加）・**副作用3問**（42便秘と呼吸数低下／47含まれない＝下痢／49ローテーション）・**評価と目標2問**（36冒頭は開かれた質問／38まず夜間の睡眠）・**ローテーション1問**（45＝採点除外）・**がんリハ1問**（40すぐに開始）。
ch02を貫く筋は4本。①**WHO方式の5原則が答えそのもの**——とくに **by mouth（飲めるなら口から）** と **by the clock（時刻を決めて）** の2つで章の半分が解ける。**「内服できているのに注射・坐薬・貼付へ行く肢」（41・45）と「疼痛時にのみ投与する肢」（46・51）はそれだけで誤り**。②**痛みが強くなったら「同じ薬を増やす」か「強い薬へ上げる」**——**弱オピオイド（コデイン）を強オピオイドに足すのは誤り**（43）。**ラダーで積み上げるのは非オピオイドと鎮痛補助薬だけで、オピオイドどうしは置き換える**。③**副作用は「耐性がつくか」で扱いが分かれる**——**便秘だけは最後まで耐性がつかない＝開始と同時に緩下薬を予防的に併用**（50）、悪心・眠気は数日〜2週で慣れるので**中止せず制吐薬でしのぐ**（42・47・49）。**呼吸抑制の見張りは SpO2 ではなく呼吸数**（42）。④**効かない／副作用が強いときは「増量」ではなく「別のオピオイドへ替える」**＝ローテーション（49）。**腎障害ではモルヒネの活性代謝物 M6G が蓄積する**のでオキシコドン・フェンタニルを選ぶ（39）。
⚠️ **ch02 の最難は NO.43（112F-50・正答率19%＝本科目で最低）**＝強オピオイド（オキシコドン）で治療中の患者に**弱オピオイドのリン酸コデインを追加する**のが誤り。**票が流れるのは b 椎弓切除術**——「進行肺腺癌に手術は過剰」と読んでしまうが、**新たに出た左上肢のしびれ＝神経根・脊髄の圧迫は薬では取れない**ので除圧の適応になる（**麻痺は「どれだけ悪いか」ではなく「いつからか」で判断する**＝整形外科ch05 NO.149と同じ論理）。次いで **NO.44（117E-29・63%）**＝NSAIDが効かない膵癌の痛みは**増量ではなくオピオイドの経口投与へ上げる**（**神経ブロック・硬膜外はラダーを上りきってからの手段**）。他の15問は正答率82%以上。
⚠️ **NO.45（117F-42）は国試の採点除外問題＝本科目で唯一**（`bx`バッジ・正解肢0・`rate=None`）。争点は用語の定義で、**「オピオイドローテーション＝薬剤そのものの変更」なのに、対応として最も妥当な肢 c は同じオキシコドンのまま経路を変える＝<u>経路変更</u>**。**妥当な肢と定義に合う肢が一致しないため採点除外になったと考えられる**。
⚠️ **NO.41（115D-17）は整形外科 ch05 の NO.162 と同一問題**（MECは同じ問題を別講座にも載せる）。**誤りはモルヒネという薬ではなく「皮下投与」という経路**で、症例文の「**食欲は良好**」が by mouth を問うための一文。整形外科側と食い違う解説を書かないこと。
⚠️ **画像は NO.43 の胸部CT 1枚だけ**。p.28 の問題枠の下にある「骨転移の放射線治療 → 根治が目的ではない／疼痛コントロールが目的である」は**講義用の書き込みで設問の図ではない**（imgs に入れない）。**選択肢が表・図の問題は ch02 に無い**。
⚠️ **ch02 に連問は無く、`／`区切りの複数コードが episode に入るのが4問**（39・41・42・43）。
ch01の内訳は**気道確保・気管挿管10問**（7・8導入／17 Mallampati／18体位／19喉頭鏡／20・21食道挿管／22・31片肺挿管／23 ETCO2／24気道内圧）・**周術期管理〈ERAS〉6問**（2術前リスク／3絶飲食／4内服／5・6消化管手術／32飲水）・**区域麻酔6問**（25穿刺層／26脊髄／27デルマトーム／28合併症／29硬膜外投与薬／35肋間神経麻痺）・**麻酔薬と拮抗薬5問**（9静脈麻酔薬／10亜酸化窒素／14拮抗薬／15・33バイタル変動）・**外傷救急の連問3問**（11輸液組成／12麻酔法／13脂肪塞栓症）・**モニタリング2問**（1パルスオキシメトリ／16悪性高熱症）・**局所麻酔2問**（30手技／34浸潤麻酔）。
ch01を貫く筋は4本。①**麻酔は「4要素（鎮静・鎮痛・筋弛緩・有害反射の抑制）を薬で分担する」仕事で、足りない／効きすぎのサインはバイタルに出る**——**侵襲を契機に血圧と心拍が上がったら鎮痛不足＝オピオイドを足す（NO.33）／侵襲が無いのに下がったら吸入麻酔薬が効きすぎ＝濃度を下げる（NO.15）**。**同じ「バイタルが動いた」でも向きで答えが正反対**になる（この2問は国試番号まで似ていて 107E-54 と 109E-54）。②**気道は「入れる前（評価と体位）→入れる（確認）→入ってから（圧とCO2）」の3段**——Mallampati(17)→sniffing position(18)→喉頭鏡(19)→**食道挿管の否定は ETCO2**(20・21・23)→片肺挿管(22・31)→気道内圧(24)。③**周術期管理は ERAS＝絶食は短く離床は早く**。**選択肢に「◯日間の安静」「◯週間の絶食」と長い期間が書いてあったら、それが誤りの肢**（NO.5・6・32 が同じ一手で解ける）。④**区域麻酔は「針をどの層で止めるか」**（棘上→棘間→黄色靱帯→硬膜。**前縦・後縦靱帯は椎体側で通らない**＝25／**脊髄はL1-L2で終わるのでL3/4・L4/5で穿刺**＝26／高さ＝デルマトーム(27)／**交感神経遮断による血圧低下が最頻**(28)／**硬膜外に入れてよいのは局所麻酔薬とオピオイドだけ**(29)）。
⚠️ **ch01 の最難は NO.35(104E-54・正答率14%＝本科目で最低)＝脊髄くも膜下麻酔後の「息ができない」は肋間神経麻痺**。**横隔神経はC3〜C5なので腰から入れた脊麻では届かず、届けば話すこともできない**（「訴えた」の一語がdを消す）。**肋間筋が麻痺しても横隔膜が動いていればSpO2は下がらない＝呼吸困難「感」と低酸素血症は別物**。次いで **NO.7・NO.8（ともに49%）**＝急速導入で最初に投与するのは薬ではなく**純酸素**（前酸素化でFRCの窒素を酸素に置換し無呼吸許容時間を1〜2分→5〜8分へ）／**輪状軟骨圧迫の目的は誤嚥の防止**（**輪状軟骨は気管で唯一の完全な輪だから押しても気道は潰れず食道だけが閉じる**。「食道への誤挿管の回避」が最大の受け皿だが、押すのは逆流を止めるためでチューブの行き先を変えるためではない）。**NO.33(52%)＝血圧・脈拍が上がったら鎮痛不足**（降圧薬・β遮断薬で数字だけ下げるのは原因を隠す対症療法）。
⚠️ **NO.17(106E-22) は模式図 a〜e が選択肢そのもので、本文に選択肢の文字が1つも無い**。**1枚の画像に5つの図が入っている（上段 a b c ／下段 d e）**ので図を読んで各肢を書き起こした。**濃い灰色＝軟口蓋／淡い灰色＝口蓋弓／中央の白い突起＝口蓋垂**で、**正解 c＝灰色が一切なく硬口蓋しか見えない＝Class Ⅳ**（d が Class Ⅰ、a・b が Class Ⅱ 相当）。PDF p.4 の解説パートに同じ Class Ⅰ〜Ⅳ の参照図がある。
⚠️ **NO.18(108B-28) は写真①〜⑤、NO.19(117E-6) は写真A〜E が選択肢そのもの**。**NO.18 の紙面は左列①②③（上→下）＋右列④⑤で `get_images()` の抽出順と一致**（正解 d＝④＝枕＋頭部後屈の sniffing position）。**NO.19 は抽出順が A,B,C,E,D でラベルと食い違う**ので `ch01_map.txt` で D と E を入れ替えた（A鼻鏡／Bクスコ式腟鏡／C筒状の開創器／D耳鏡／**E喉頭鏡＝正解**）。
⚠️ **NO.11(108B-56) の選択肢は表**（Na⁺/K⁺/Cl⁻/Lactate⁻/糖質 の5列×a〜e）。ガイド§1に従い200dpiのページ画像を目視で読み、**列見出しを各肢に埋め込む方式**（`ａ　Na 130／K 4／Cl 109／Lactate 28／糖質 0`）で書き起こした。正解a＝乳酸リンゲル液。
⚠️ **連問は NO.11・12・13（108B-56/57/58）の3問組**。**症例文は3枚すべてに載せる**こと（生成器では `_STEM_11` を3問の qt に連結）。**共通ステムに図は無く、NO.13 だけが自前の胸部エックス線写真を持つ**（p11の画像は自動帰属だとNO.12になるので目視でNO.13へ付け替えた）。
⚠️ **NO.1(110H-15) の傍らにある「参考画像」（バイタル画面・パルスオキシメーター／© Dr. Watari）は設問の図ではない**。**しかも正解 e を示してしまう**ので imgs に入れない（bi バッジも付かない）。本PDFは講義用の参考図が問題の傍らに置かれることがあるので map から手で外す。
⚠️ **★問題のセクション（NO.1-30）にありながら★バッジが付かない問題が2つある**（NO.11・NO.13＝連問に混ざった「救急の問題」で解答表の種別欄が空）。バッジは解答表に従うので **★バッジは28問**。**35問すべてに正答率があり、採点除外は第2章 NO.45（117F-42）の1問だけ**。
⚠️ **本PDF固有の罠＝各ページの左右端に章名の縦書き（「周」「術」「期」…）が1文字ずつ入る**（`anes_pdf.py` の `cmd_text` は x<40 / x>540 を落とす）。抽出・照合の道具は `_work/anes_pdf.py`（anstable/spec/text/images/save）と `_work/verify_anes_ch.py`。**巻末解答一覧表はPDF p.33-34 で列のx座標は精神科・皮膚科・眼科・耳鼻咽喉科・泌尿器科・整形外科と同一**、国試番号索引はp.35。⚠️ **PDF p.4 は「MECマイナー講座 麻酔科の特徴」の見本ページで NO.1-3 が再掲される**（問題ページ範囲 p.5- で限定して避ける）。⚠️ **episode に `／` 区切りの複数コードが入る問題がある**（`120E-30／114B-40類` 等7問）ので画像ファイル名は代表コード1つにし、**`pdf_audit.py` の照合を `・` だけでなく `／` でも分割するよう一般化した**（ent の `112E-26／106H-15類` も同型） |
| `放射線科/` | マイナー講座・放射線科（prefix `rad`・☢️・#475569）。章別HTML(`ch01_josho.html`〜)＋`images/`＋`rad_questions.json`（全4章の章名メタ）。HTMLが`questions_rad.json`のソース＝章ごとの生成器`_work/build_rad_ch{NN}.py`→`_work/build_rad_json.py`で再生成。麻酔科と同方式。**全4章60問（2026-08-18完成）・章頭NO.は 1／2／35／45**（章名はPDF表記のまま「序　章」「放射線診断学」「放射線治療学」「医療安全・放射線防護」）。**内訳は序章1問（本科目・全科目通じて最小の章）／放射線診断学33問（画像15問30枚）／放射線治療学10問（画像0）／医療安全・放射線防護16問（画像1問2枚）**。⚠️ **この科目だけPDFが2冊ある**——`MECマイナー講座・放射線科_問題（表紙2026）.pdf`（41p・問題の正本）と`…_レジュメ（表紙2026）.pdf`（43p・**解説パートの正本**）。他科目では問題PDFしか無いが、放射線科はレジュメがあるので**解説はレジュメ編（放-2〜放-37）に沿って書いた**（モダリティ一覧・造影剤の禁忌・アーチファクト・IVR一覧・感受性の3段階・単位と線量限度は全部レジュメの表が下敷き）。
本科目を貫く4本の筋。①**検査を選ぶ前に必ず禁忌を通す**——**ヨードもガドリニウムも「まず腎機能」**（ヨード＝造影剤腎症で可逆／Gd＝腎性全身性線維症で不可逆）、次いで喘息と副作用歴。MRIはこれに**体内金属とペースメーカの機種**が加わる（NO.4・6・23・26 の4問がこれだけで解ける）。②**モダリティは「何を見たいか」で決まる**——**出血・石灰化・骨・救急＝CT／早期脳梗塞・後頭蓋窩・骨盤内・骨軟部＝MRI／機能と全身検索＝核医学／ベッドサイドと血流＝超音波**。③**放射線感受性はBergonié-Tribondeauの法則1本**（分裂が盛ん・未分化・将来の分裂回数が多いほど高い）で、正常組織の3段階も腫瘍の序列も導ける。**外的因子では「酸素が多いほど効く」が最頻出で、「低酸素の癌は感受性が高い」は必ず誤り**。④**防護は時間・距離・遮蔽の3原則だけ**（第1章の「距離の2乗に反比例」がそのまま根拠）。
⚠️ **NO.11・14・16 はMECのオリジナル問題**（国試番号が無く、解答表の正答率欄も空）。`rate=None` で作るが**採点除外ではないので `bx` バッジは付けない**（正解肢は存在する）。**画像ファイル名は国試番号を名乗れないので `orig{NO.}_{n}.jpeg`** とした——`pdf_audit.py` の `FNAME_CODE` は数字始まりなのでこの名前は照合対象から外れる。**本科目に採点除外の問題は無く、正答率が無いのはこの3問だけ**。
⚠️ **連問は3組**（NO.18・19／NO.20〜22／NO.27〜29）。**症例文は組の全カードに載せる**こと。共通ステムが参照する図は**各問ぶん別名で保存**してある（`120E-41_1,2`／`120E-42_1,2`、`114C-69_1,2`／`114C-70_1,2`／`114C-71_1,2`＝中身は同一）。生成器では `_STEM_27` を3問の qt に連結している。
⚠️ **NO.9（115F-40）は `get_images()` の抽出順が紙面と逆**（xref139=胸部CT が先、xref140=胸部Xp が後）。`ch02_map.txt` で A=胸部エックス線写真／B=胸部単純CT に入れ替えてある。他の多図問題（NO.10・13・15・16・32・57）は抽出順＝図ラベル順で一致した。
⚠️ **NO.2（120C-59）は本文中の下線部①〜⑤が選択肢そのもの**。**ステムの該当箇所を `<u>` で囲まないと解けない**（整形外科 ch05 NO.142・ch06 NO.167 と同じ型）。
⚠️ **本科目（および本章）の最難は NO.27（114C-69・正答率20%）**＝**ポータブル仰臥位AP像と立位PA像では撮影体位も方向も違うので心拡大を比較できない**（AP像は心臓が検出器から遠く幾何学的に拡大する）。画像を読むのではなく「その画像で何が言えて何が言えないか」を問う、放射線科らしい一問。次いで **NO.54（109G-17・11%＝本科目で最低）＝確率的影響で正しいのは「ヒトでは遺伝的影響は確認されていない」**（a〜dはすべて確定的影響の性質。消去法で残ったものを信じられるかが分かれ目）、**NO.32（111A-42・23%）＝ステントを留置したのは左浅大腿動脈**（ASOの最好発部位）、**NO.15（117D-48・27%）＝起床時に症状に気づいた＝最終健常時刻が不明でt-PA適応外**（wake-up stroke。「起床時に気づいた」を「起床時に発症した」と読み替えると d を選ぶ）、**NO.44（109G-27・31%）＝通常分割照射は週5日・1回1.8〜2Gy・全期間6〜7週**。
⚠️ **各ページの左右端に章名の縦書き（「放」「射」「線」「診」「断」「学」…）が1文字ずつ入る**（`rad_pdf.py` の `cmd_text` は x<40 / x>540 を落とす）。抽出・照合の道具は `_work/rad_pdf.py`（anstable/spec/text/images/save）と `_work/verify_rad_ch.py`。**巻末解答一覧表はPDF p.39-40 で列のx座標は精神科・皮膚科・眼科・耳鼻咽喉科・泌尿器科・整形外科・麻酔科と同一**、国試番号索引はp.41。⚠️ **PDF p.4 は「MECマイナー講座 放射線科の特徴」の見本ページで NO.2-6 が再掲される**（問題ページ範囲 p.5- で限定して避ける）。
⚠️ **第2章の生成器 `build_rad_ch02.py`（33問・336KB）は `_work/_rad_ch02_p1.py`〜`p6.py` を `_work/_rad_join_ch02.py` で連結して作った派生物**。1回のWriteに収まらないための分割で、**章を直すときはパート側を編集してから join を流し直す**こと（build_rad_ch02.py を直接編集すると次の join で消える） |
| `中毒・職業病/` | **横断テーマ・中毒・職業病（prefix `tox`・☠️・#65A30D）**。章別HTML(`ch01_kinzoku_chudoku.html`〜`ch07_butsuriteki_shikkan.html`)＋`tox_questions.json`（全7章の章名メタ）。HTMLが`questions_tox.json`のソース＝章ごとの生成器`_work/build_tox_ch{NN}.py`→`_work/build_tox_json.py`で再生成。放射線科と同方式。**全7章48問（2026-08-19完成）・章頭NO.は 1／5／10／16／22／25／31**（章名はPDF表記のまま「金属中毒」「有機溶剤中毒」「農薬中毒」「その他の中毒」「自然毒」「ガス体中毒」「物理的原因による疾患」）。内訳は金属中毒4／有機溶剤中毒5／農薬中毒6／その他の中毒6／**自然毒3（全科目通じて最小級の章）**／ガス体中毒6／**物理的原因による疾患18（本科目の最大章＝48問の3分の1以上）**。
⚠️ **これはマイナー講座ではなく「横断テーマ」の独立PDF**（`MEC問題文pdf/中毒・職業病（表紙2026）.pdf`・69ページ）。**版面が他科目と根本的に違い、「レジュメ＋問題」が交互に並ぶ**——解説ページと問題ページが同じ章の中に混ざるので、章のページ範囲は解説ページも含む。問題は `□□□` に続く `N. （国試番号）↗M` で始まる。
⚠️ **解答一覧表には★（種別）列・CBT列が無い**（NO./解答/国試番号/IRT型/出題テーマ/疾患名/必修/一般/臨床/正答率 の10列）。したがって**付きうるバッジは `bh`（必修）だけ**で、`bs`／`bc` が付いていたらそれ自体が誤り（`verify_tox_ch.py` が検出する）。**必修/一般/臨床/正答率の4列はヘッダが1語 `必修一般臨床正答率`（x=476.9〜543.9）に潰れている**ので、○ の x で列を決める（必修≈480／一般≈497／臨床≈514）。
⚠️ **各ページの左右端に章名の縦書き（「金」「属」「中」「毒」…）が7章ぶん1文字ずつ入る**（`tox_pdf.py` の `cmd_text` は x<40 / x>540 を落とす）。
⚠️ **画像は0枚**。PDFに埋め込み画像は1枚しか無く（p.40 xref=178「図4 酸素欠乏症の症状」）、**それはレジュメの解説図で設問の図ではない**ので imgs に入れていない（`_tox_tmp/ch06_map.txt` の該当行は削除済み）。したがって `bi` バッジも `image_dims.json` の更新も不要。
⚠️ **必修バッジは NO.21（119E-36・灯油誤飲）と NO.32（120E-33・熱中症の体温測定部位）の2問だけ**。**48問すべてに正答率があり、採点除外（解答＝なし）の問題は無い。連問は NO.17-19／NO.11-13（ch03）等がある一方、画像由来の帰属問題は起こりえない**。
⚠️ **PDF本文は亜硫酸ガス〈SO2〉の臭いを「腐敗した卵のような臭気」と書いているが、これは硫化水素〈H2S〉の腐卵臭と取り違えたもの**。**SO2 は「マッチを擦ったときのようなツンとした刺激臭」**である。**同じ ch06 の NO.28 が「卵が腐ったような臭い＝硫化水素」で解く問題なので、PDFをそのまま写すと章内で矛盾する**——解説は正しい方（刺激臭）で書いた。
⚠️ **第7章の生成器 `build_tox_ch07.py`（18問・194KB）は `_work/_tox_ch07_p1.py`〜`p4.py` を `_work/_tox_join_ch07.py` で連結して作った派生物**。1回のWriteに収まらないための分割で、**章を直すときはパート側を編集してから join を流し直す**こと（放射線科 ch02 と同方式）。
■ 本科目を貫く4本の筋：①**曝露源から物質を決める**——設問はまず職業と作業内容を書いてくるので、**「どこに勤めているか」ではなく「何をしているか」**で曝露を判定する（第7章 NO.36 の「大型塗料店だが在庫管理＝曝露なし」が典型）。生物学的モニタリング〈BM〉はトルエン→尿中馬尿酸、スチレン→尿中マンデル酸、鉛→血中鉛・尿中δ-ALA。②**ガス体中毒は「酸素のどこを止めるか」の3系統**——**追い出す（酸素欠乏・CO2＝チアノーゼあり・SpO2低下）／運ばせない（CO＝皮膚紅潮・SpO2は見かけ正常）／使わせない（シアン・硫化水素＝チアノーゼなし・SpO2正常）**。**チアノーゼとSpO2の2つだけで系統が決まる**。③**物理的因子は「エネルギーがどこまで届くか」で障害部位が決まる**——**紫外線は表層で止まる（電気性眼炎・雪目・日光皮膚炎／温熱作用は無い）／赤外線・マイクロ波は深部まで届く（白内障・熱中症）／レーザーは一点集中（網膜火傷）**。④**現場では「まず自分が死なない」**——退避＞救助。**硫化水素の浴室（ch06 NO.28）で「助けに行く」肢は全部誤り**。
⚠️ **本科目の最難は NO.46（115F-21・正答率60%）**＝**奇形（構造異常）が出るのは受精2〜8週の器官形成期**で、**8〜15週は精神発達遅滞**（別物）。**受精後週数と妊娠週数は2週ずれる**（受精2〜8週＝妊娠4〜10週）。次いで **NO.43（110I-24・64%）＝職業被ばくは100mSv/5年 かつ 50mSv/年**（500mSv/年は「皮膚の等価線量」で、実効線量と等価線量の取り違えが受け皿）、**NO.44（109G-25・65%）＝公衆被ばく1mSv/年＜職業被ばく**（**患者＝医療被ばくにだけ線量限度が無い**）、**NO.48（118C-20・66%）＝職業性白内障と関係があるのは病院**（放射線。**「病院＝有害業務のある事業場」と意識しにくいのが割れる原因**）、**NO.27（109I-18・48%＝ch06最難）＝粘膜刺激は亜硫酸ガス**（**サリン・CO・ブタンは無色無臭・無刺激、シアン化水素は臭うが刺激しない**＝「毒性が強い＝刺激する」ではない）。
抽出・照合の道具は `_work/tox_pdf.py`（anstable/spec/text/images/save）と `_work/verify_tox_ch.py`。**巻末解答一覧表はPDF p.66-67**、国試番号索引はp.68 |
| `公衆衛生/` | **公衆衛生講座（prefix `ph`・🏛・#0891B2）**。章別HTML(`ch01_ishihou_to_iryouhou.html`〜)＋`ph_questions.json`（全19章の章名メタ）。HTMLが`questions_ph.json`のソース＝章ごとの生成器`_work/build_ph_ch{NN}.py`→`_work/build_ph_json.py`で再生成。**全19章619問（PDF 283ページ＝産婦人科685問に次ぐ規模）**・章頭NO.は 1／72／81／113／132／156／191／227／273／282／305／317／338／353／398／435／470／481／498。章名はPDF表記のまま「医師法と医療法」「保健所」「死」「医療職」「医療保険」「介護保険」「人　口」「疫学研究」「検査学」「健康増進と生活習慣」「医薬品・食品・嗜好品」「母子保健」「小児保健・学校保健」「障害とノーマライゼーション」「感染症」「産業衛生」「環境問題」「海外協力」「その他基本事項」。**2026-08-25に第1章「医師法と医療法」NO.1-71（71問・画像0枚・連問なし）と第2章「保健所」NO.72-80（9問・画像0枚・連問なし＝**第9章「検査学」と並ぶ本科目最小の章**）を追加**。
**2026-08-25に第3章「死」NO.81-112（32問・画像6問10枚・連問1組＝本科目初の画像章／初の連問）を追加**。
**2026-08-25に第4章「医療職」NO.113-131（19問・画像0枚・連問なし＝本科目で唯一、図表を1枚も持たない章）を追加**。
**2026-08-25に第5章「医療保険」NO.132-155（24問・画像0枚・連問2組）を追加**。
ch05を貫く筋は4本。①**「保険」で払うのか「税（公費）」で払うのかを先に割る**——保険は<u>病気・けがに対する療養の給付</u>だけを現物給付で賄い、**健康診断・人間ドック・予防接種・正常妊娠分娩・介護に関わる費用は保険の外**にある（NO.134・139・141・144・149・155）。**保険の外に置かれたものの一部を、国家賠償的・社会福祉的な理由で税が拾うのが公費負担医療**。②**根拠法は「誰を守る制度か」で決まる**——結核・1類2類感染症＝感染症法／措置入院＝精神保健福祉法／未熟児の養育医療＝母子保健法／小児慢性特定疾病＝児童福祉法／自立支援医療＝障害者総合支援法／医療扶助＝生活保護法／指定難病＝難病法。**NO.132 がこの表を5方向から一度に問う**。③**自己負担は「年齢」で階段を作り、「所得」でその階段を上書きする**——原則3割／未就学児2割／70〜74歳2割／75歳以上1割、**ただし現役並み所得なら年齢に関係なく3割**（NO.150・152・154）。**高額療養費制度の限度額を決めるのも年齢と所得の2つだけ**で、**疾患名・治療内容・医療機関の規模は一切関係しない**（NO.133・148）。④**国民医療費は「含まないもの」を覚える**——「<u>予防・正常・介護・自費</u>は外」（健診／正常妊娠分娩／介護保険給付／差額ベッド・自由診療・OTC）。**規模は46兆円（令和4年）・65歳以上が6割・診療種類別は医科診療が最多・傷病分類別は循環器系が最多**（NO.136・140・153・155）。
⚠️ **ch05 の最難は NO.139（115C-6・正答率30%）＝全額公費は「指定感染症による入院」**。**結核の一般医療（適正医療）は95%止まりで自己負担5%が残り、措置入院も所得に応じた費用徴収がある**ので、**「公費負担医療の一覧」を覚えているだけでは絞れない**——**「患者は1円も払わないのはどれか」という第2の軸**が要る。次いで **NO.137（118F-27・42%）＝公費5割・現役世代の支援金4割・保険料1割**（**受け皿は肢e＝5と4が入れ替わっている**）。
⚠️ **ch05 の連問は2組**（**NO.142/143＝118B-47/48**、**NO.145/146＝117B-43/44**）。**症例文は両方のカードに載せる**こと（生成器では `_STEM_142`／`_STEM_145` を両方の qt へ連結）。**NO.146 の「1か月後に自宅退院し職場復帰した」以下はその設問だけの情報なので共通ステムに入れない**（ch03 の NO.103/104 と同じ扱い）。
⚠️ **NO.142（118B-47）は本文中の下線部①〜⑤が選択肢そのもの**（研修医の発話の中にある）。**ステムの該当箇所を `<u>` で囲まないと解けない**（整形外科 NO.142・167・171／放射線科 NO.2 と同じ型）。
⚠️ **ほぼ同一問題の組が2組ある**——**NO.133≒NO.148**（高額療養費の限度額を規定する条件。正解肢の書き方が「加入者の所得水準」→「患者世帯の標準報酬月額」に変わっただけ）／**NO.147≒NO.151**（社会保障制度の一般問題で、**5肢のうち4肢が完全に同じ**。違うのは選択肢ｃの年数だけ＝「2000年から20年で3倍」／「2005年から10年で3倍」）。**重複を消さない**。
⚠️ **24問すべてに正答率があり、採点除外・画像・計算問題は無い**。★問題10問・CBT 4問・必修8問。**レジュメは p.22-24 の3ページ**（p.25 は白紙）で、①医療保険と国民医療費の表 ②公費負担医療の根拠法一覧 ③特定健診・特定保健指導の判定基準、の3枚が骨格。
ch04を貫く筋は4本。①**医行為は「誰が」ではなく「誰の指示で」で開く**——看護師・臨床検査技師・救急救命士はいずれも<u>医師の指示の下</u>でのみ診療の補助を行える。**薬剤師は調剤であって処方はできず（NO.116・123・126）、介護福祉士は原則として医行為を行えない**（NO.113。例外は**喀痰吸引と経管栄養の注入の2つだけ**で、**「入っている胃管から注入する」は可・「胃管を挿入する」は不可**＝この境目が NO.115・127 で3回反復する）。②**リハビリ3職は「体のどこを診るか」で割る**——**PT＝基本的動作（座る・立つ・歩く）／OT＝応用的動作・社会適応（物を持つ・書く・作業）／ST＝口と言葉（発声・嚥下・摂食）**。この一本で NO.114・119・120・125・129・131 が解ける。③**「制度・お金・仕事・施設間の調整」が出てきたら医療ソーシャルワーカー〈MSW〉、「介護保険サービスの計画」が出てきたらケアマネジャー**（NO.117・121・128・130）。**MSW は本章に出る職種で唯一の非国家資格**（NO.124）。④**ケアマネジャーは「計画を立てる人」であって「認定する人」ではない**——要介護認定を行うのは**市町村**（介護認定審査会の二次判定に基づく）。NO.129・NO.131 が同じ形でこれを問う。
⚠️ **ch04 の最難は NO.116(118B-1・正答率59%)＝医師の指示があっても採血できないのは薬剤師**。**保健師・助産師は保健師助産師看護師法31条2項により「看護師の業（療養上の世話・診療の補助）」を行える**ので採血できる——**「保健師＝保健指導の人」という日常のイメージが最大の受け皿**（2007年以降、保健師・助産師の免許には看護師国家試験の合格が必要）。次いで **NO.131(111B-2・71%)＝作業療法士と理学療法士の定義の入れ替え**（**「<u>基本</u>的動作能力＝PT／<u>応用</u>的動作能力・社会的適応能力＝OT」の2語で決まる**）、**NO.118(115F-54・76%)＝退院支援は入院時から始める／家族の在宅処置は「業」でないので行える**、**NO.122(115C-46・77%)＝「7月の屋外作業」で熱中症へ誘導されるがアドレナリン筋注**（**救急救命士が打てるのは症例文に「自己注射薬の処方を受けている」と書いてあるから**）、**NO.117(116B-29・79%)＝「入院中に新たな機能障害は認めなかった」の一文がヘルパー・福祉用具・在宅リハの3肢を消す**。
⚠️ **組合せ問題2問（NO.123・NO.129）は動詞を見る**——誤りの正体は職種と業務の対応ではなく**動詞が一段上へすり替わっていること**（看護師―抗菌薬の「処方」／ケアマネジャー―介護度の「認定」）。**「処方」「認定」「判定」「診断」は権限を伴う語で法律が担い手を決めている**。
⚠️ **ほぼ同型の設問が3組**（**NO.123≒NO.129**＝職種と職務内容の誤った組合せ／**NO.128≒NO.130**＝支援制度の相談と施設間調整はどちらもMSW／**NO.121≒NO.118c≒NO.127d**＝ケアプランを作るのはケアマネジャー）。**重複を消さない**。
⚠️ **19問すべてに正答率があり、採点除外・連問・画像・複数選択の混在は無い**（複数選択は NO.118 の「2つ選べ」1問だけ）。★問題6問・CBT 4問・必修7問。**レジュメは p.20 の職種表1ページだけ**（p.21 は白紙）なので、解説はその表を骨格に国試の出題論点で肉付けした。
ch03を貫く筋は4本。①**異状死かどうかは「内因死か外因死か」＋「診療行為に関連した予期しない死か」で決まる**（医師法21条。異状と認めたら **24時間以内に所轄警察署**へ届け出る。**保健所ではない**——この「保健所」が誤り肢の最大の受け皿で NO.85・95・110 の3問に置かれている）。②**死亡診断書と死体検案書の分かれ目は「診療継続中の患者が、診療していた疾患で死んだか」の1文だけ**（NO.91・100・101・110）。**「診療継続中」に時間の定めは無く**（NO.91 c は最終診察から75時間後でも死亡診断書）、**「異状死＝死体検案書」でもない**。③**死体現象は時計として読む**（死斑・死後硬直はともに2〜3時間で出現→12時間で固定／最強、角膜混濁は48時間で完全、直腸温は37℃から1時間に1℃）。④**臓器提供は「脳死後」と「心臓死後」で提供できる臓器が違う**——**心臓死後は腎臓・膵臓・眼球（角膜）の3つだけ**。
⚠️ **ch03 の採点除外は NO.111（111G-34）の1問**（`bx`バッジ・正解肢0・`rate=None`）。争点は膵の扱いで、**厚生労働省・日本臓器移植ネットワークの「心停止後に提供できるのは腎臓・膵臓・眼球」に沿えば b（肝）と e（小腸）**だが、「膵は脳死下のみ」とする資料もあり整合しない。**32問中これだけが正答率「－」**。
⚠️ **連問は NO.103・104（113B-44/45）の1組**（本科目初）。**症例文は両方のカードに載せる**こと（生成器では `_STEM_103` を両方の qt に連結）。**NO.104 の既往歴・生活歴・検査所見はその設問だけのもの**なので `_STEM_103` には入れない。
⚠️ **NO.93（118B-18）は死亡診断書Ⅰ欄の写真A〜Eが選択肢そのもの**。紙面は上から A→B→C→D→E で `get_images()` の抽出順（xref 161/163/165/167/169）と一致した。**Ⅰ欄は下へ遡るので死因の種類を決めるのは常に一番下の欄**——正解 C は「低酸素脳症←縊頸」。
⚠️ **NO.90（112F-16）は表が問題文に入る**（死亡の場所別割合）。`<table class="tb">` で書き起こした。**NO.89・98 は死亡診断書の様式そのものが図**で、NO.98 の Ⓐ は「（イ）（ア）の原因」の欄。
⚠️ **ほぼ同一問題の組が3組**——**NO.97≒NO.108**（どちらも「医療関連死に含まれないのはどれか」で「負荷心電図検査中の心室細動」の肢が共通。外し方が①医療が原因でない②予期されていた、の2通りある）／**NO.85≒NO.105**（死亡診断書の一般問題で剖検所見・主治医以外・署名の3肢が重なる）／**NO.82≒NO.95**（どちらも診療行為に関連した予期しない死亡で正解は異状死の届出）。**重複を消さない**。
⚠️ **ch03 の最難は NO.90(112F-16・正答率72%)**＝死亡の場所別割合（病院74.6／自宅12.7／老人ホーム6.3／診療所2.3／介護老人保健施設2.0）。次いで **NO.85(118E-22・77%)＝死亡診断書は歯科医師も作れるが死体検案書は医師のみ**、**NO.102(114F-5・77%)＝本人の意思表示があってもまず家族と相談**（e「臓器移植ネットワークに連絡」は1つ後の段階）、**NO.107(111C-20・81%)＝索条痕とロープの性状の一致を確認**（遺書があっても自殺と決めない）。
ch02は9問すべてが「誰の仕事か」を問い、軸は3機関の対比ただ1本——**保健所＝広域的・専門的（許認可・立入検査・感染症・食品衛生・人口動態統計の審査）／市町村保健センター＝住民に身近な対人サービス（健康相談・保健指導・乳幼児健診・健康教室）／地方衛生研究所＝検査と調査研究（対物・技術支援）**。誤り肢はここへ**労働基準監督署（産業医の届出・労災認定）・検疫所（船舶の隔離）・福祉事務所（生活保護・身体障害者手帳）・市町村（3歳児健診・予防接種）**を混ぜて作られる。
⚠️ **レジュメが冒頭で並べる「保健 health＝健康を守り保つ／保険 insurance＝皆でお金を出し合う」の対比がそのまま NO.79 の正解**（保健所は医療保険に関わる業務を行わない）。**「お金の話が出てきたら保健所ではない」**で生活保護・予防接種の健康被害救済・労災認定もまとめて切れる。
⚠️ **ch02 の最難は NO.74(114C-33・正答率52%)**＝「3つ選べ」で**国勢調査の調査票の審査**を外せるか。**人口動態統計の審査は保健所だが、国勢調査は総務省の系統で保健所は関与しない**——この2つを並べて入れ替えてある。次いで **NO.77(115C-27・60%)** で**産業医の届出＝労働基準監督署／船舶の隔離＝検疫所／労災認定＝労働基準監督署**の3つを同時に外す必要がある。
⚠️ **1994年の地域保健法改正で母子保健などの対人サービスが保健所→市町村へ移った**という歴史が出題の軸そのもの（3歳児健診が保健所でない理由）。**難病・精神保健・結核・エイズが保健所に残る**のは専門性が高く市町村単位では経験が蓄積しないため。
⚠️ **NO.72 と NO.80 はほぼ同一問題**（正解が「食品衛生に関する事項」→「食品に関する営業者の監視」と言い換わっただけで、誤り肢4つは完全に同じ）。**重複を消さない**。
⚠️ **この科目だけPDFが2冊ある**——`MEC公衆衛生講座_問題（表紙2026）.pdf`（283p・設問の正本）と`MEC公衆衛生講座_レジュメ（表紙2026）.pdf`（96p・**解説パートの正本**）。放射線科と同じ構成で、**レジュメの19章が問題PDFの19章と1対1で対応する**（レジュメPDFページ＝印刷+6、問題PDFページ＝印刷+5）。解説はレジュメを下敷きに書くこと。
⚠️ **解答一覧表はPDF p.267-277**（NO./解答/国試番号/種別★/CBT/出題テーマ/必修/一般/臨床/正答率の**10列で、疾患名の列は無い**）、国試番号索引は p.278-283。**CBT列の○は出題テーマの1語目とくっついて1単語になる**（x≈173.7）ので先頭の○を切り離してから theme に回す（`ph_pdf.py` が処理済み）。右端に章名の縦書きが x≈563-569 に入るので `cmd_text` は x>545 を落とす。
⚠️ **「※不正解者のみ採点除外」の注記は解答一覧表で2行に折り返し、2行目が次の行の国試番号セルへ流れ込む**（NO.15→NO.16、NO.33→NO.34）。`ph_pdf.py` は国試番号を正規表現で取り直して `kid_raw` に注記を残す。
⚠️ **真の採点除外（解答＝なし・正答率－）は3問だけ**——NO.28（117C-15）・NO.111（111G-34）・NO.332（113F-42）。この3問は`bx`バッジ・正解肢0・`rate=None`で作る。**「※不正解者のみ採点除外」は4問（NO.15・33・272・616）で正解が存在する＝通常扱い**（整形外科 NO.124 と同型）。
⚠️ **マークシートの計算問題が6問**（NO.207総再生産率／218年齢調整死亡率／238オッズ比／245人口寄与危険度割合／259・263リスク比）。すべて ch07・ch08 にあり、`calc_input.js` の桁入力型（`計算答：<桁文字列>`）で作る＝**既存50問→56問になるので `_work/test_calc_input.js` の定数も更新が要る**。
⚠️ **画像は実質30枚前後**（35ページに49枚あるが p.2 の目次サンプルと p.6「MEC公衆衛生講座の特徴」の見本ページは設問外）。一方で**2×2表・人口ピラミッド・統計グラフが問題文に入る問題が多い**ので、そこは `<table class="tb">` で書き起こす（PDFを300dpiで描画して目視）。
⚠️ **既存科目と重複する章が3つある**——ch15感染症↔`kansen`、ch16産業衛生・ch17環境問題↔`tox`。MECは同じ問題を別講座に載せるので**重複は消さず、解説が既存科目と食い違わないようにする**。
⚠️ **レジュメは臨床研修を「努力義務」と書いているが、医師法第16条の2 は「２年以上…臨床研修を受けなければならない」と定めており平成16年以降は必修（義務）**。本科目の設問はどれも義務／努力義務の別を問うていないので、解説は**「医師法に臨床研修の規定がある」までを書き、義務の強さには踏み込まない**（レジュメと矛盾させず、かつ誤りも書かないため）。
ch01（71問）を貫く筋は4本。①**医師法＝人（医師）／医療法＝施設（医療機関）**——この一本で「医師法に規定されているか」を問う6問（NO.2・30・32・38・61・67）が全部解ける。境界は**臨床研修（研修そのもの＝医師法／管理者要件＝医療法）と診療録（記載と5年保存＝医師法／その他の記録2年＝医療法）**の2つ。②**権限の相手は3層**——**医師免許・特定機能病院・臨床研究中核病院＝厚生労働大臣／医療機関の開設・地域医療支援病院・医療計画・災害拠点病院・へき地医療拠点病院＝都道府県知事／保健センター・予防接種・要介護認定＝市町村**。③**「単位」を取り違えさせる肢が4回出る**（NO.17・19・26・63）——**地域医療構想の構想区域＝概ね二次医療圏／地域包括ケアシステム＝日常生活圏域（概ね30分以内）**。④**在宅医療は「〜でなければならない」と限定する肢がほぼ誤り**（家族の同居・要介護認定・特定の疾患はいずれも要件ではない）。訪問看護は**介護保険が医療保険に優先**（例外＝末期がん・厚労大臣が定める疾病等・特別訪問看護指示書）。
⚠️ **ch01 の最難は NO.15(116E-16・正答率24%)＝患者満足度は「受療行動調査」**（患者調査は**施設**が回答するので満足度を聞かない）。次いで **NO.17(114C-1・36%)＝地域医療構想の構想区域は都道府県単位ではない**（**策定主体が都道府県であることと区域の単位は別**）、**NO.33(116B-6・42%)＝主治医以外への相談は医療安全支援センター**（「センター」が多く紛れる）、**NO.70(111G-24・51%)＝地域医療支援病院の紹介率要件は30%ではない**（80%以上／65%以上かつ逆紹介率40%以上／50%以上かつ逆紹介率70%以上のいずれか）。
⚠️ **ch01 には同一論点・ほぼ同一問題の組が3組ある**（**NO.23≒NO.55**＝どちらも正解が「医療法—無診察治療の禁止」／**NO.27≒NO.65**＝選択肢 e だけが DHEAT/DMAT で違い正解は同じ d／**NO.11≒NO.69**＝在宅療養中の誤嚥性肺炎で正解はどちらも「地域医療支援病院へ紹介」）。**重複を消さない**。
⚠️ **次の章に着手するときは、まず `_work/公衆衛生_引き継ぎ.md` を読む**（全19章の章頭NO.・問題PDFとレジュメのページ対応表・手順・ハマりどころ・未了事項をまとめてある）。
抽出・照合の道具は `_work/ph_pdf.py`（anstable/spec/text/images/save。`text --resume --p0 N --p1 M` でレジュメPDFも dump できる）と `_work/verify_ph_ch.py`。生成器は分割方式で、`_work/_ph_ch01_p1.py`〜`p14.py` ＋ `_ph_ch01_tail.py` を `_work/_ph_join_ch01.py` で連結して `build_ph_ch01.py` を作る**派生物**（放射線科 ch02・中毒 ch07 と同方式。**直接編集すると次の join で消える**）。⚠️ **PDF p.6 は「MEC公衆衛生講座の特徴」の見本ページ**（問題ページ範囲 p7- で限定して避ける） |
| `眼科/` | マイナー講座・眼科（prefix `oph`）。章別HTML(`ch01_ganka_kihon.html`〜)＋`images/`＋`oph_questions.json`（全8章の章名メタ）。HTMLが`questions_oph.json`のソース＝章ごとの生成器`_work/build_oph_ch{NN}.py`→`_work/build_oph_json.py`で再生成。皮膚科と同方式。**全8章213問・章頭NO.は 1／51／77／97／116／162／179／192**（生成器の`Q_START`がこれを持つ）。**2026-07-30に第1章「眼科の基本」NO.1-50（画像11問13枚）と第2章「結膜・角膜疾患」NO.51-76（画像15問16枚）、2026-07-31に第3章「水晶体疾患」NO.77-96（画像12問12枚）・第4章「緑内障」NO.97-115（画像9問16枚）・第5章「網膜疾患」NO.116-161（46問・画像23問40枚＝**oph最大の章**）・第6章「黄斑部疾患」NO.162-178（17問・画像12問23枚）・第7章「ぶどう膜疾患」NO.179-191（13問・画像10問19枚）・第8章「その他の眼科疾患」NO.192-213（22問・画像8問9枚）を追加して**全8章213問が完成**。ch08は章名に反して**実体は眼外傷の章で、眼窩吹き抜け骨折だけで11問（半数）**——残りはうっ血乳頭3問・眼化学外傷/電気性眼炎4問・穿孔性眼外傷/眼内異物3問・視神経炎1問。
⚠️ **巻末解答一覧表には「疾患名」の列がある**（NO・解答・国試番号・種別・CBT・IRT・テーマ・**疾患名**・必修/一般/臨床・正答率）。これが**眼底写真の読みを裏取りする正本**になる——ch06 NO.165(113D-59) は画像から「滲出性黄斑症」と読んだが、この列が「**網膜前膜**」と明示していて正解 e(OCT) と噛み合った。新章では `spec` を取ったら**必ずこの列も見る**こと。
⚠️ **視野図①〜⑤が選択肢そのものになる問題が3問ある**（ch01 NO.39／ch04 NO.103(108I-3)／ch05 NO.134(98A-8)・143(110E-50)・152(104G-54)）。PDF内での入り方が問題ごとに違うので `images` サブコマンドの出力を必ず目視する——**134は2枚（左列①②③／右列④⑤）・143は5枚バラバラ・152は1枚にまとめて**入っている。図の下の「斜線部は暗点または盲点を示す。」は文字画像なので保存せず qt に文章で入れた。
⚠️ **右眼の眼底写真は視神経乳頭が画面の右**に写る（PDF p.78 の「右／左」ラベル付き写真で確認済み）。視野欠損の部位と眼底所見を突き合わせるとき（ch05 NO.133・134・142）に効く。
⚠️ **ch05 NO.153(104I-14)「眼科検査の写真」は Goldmann動的視野計**（大きな半球のボウル）。ERGと読み違えないこと。
⚠️ **「右眼＝乳頭が画面の右」は中心固視の標準的な後極写真にだけ当てはまる**。ch07 NO.183(102I-66) は紙面に「左眼」とあるのに乳頭が画面の右寄りに写る（鼻側に寄せて撮った写真）＝**左右の判定は問題文の記載を優先する**。
⚠️ **図ラベルの抽出順は紙面の並びと一致しない**（ch06 NO.174・178／ch07 NO.180・182・183・187・189）。map の連番は**必ず x/y 座標**で確定すること。
⚠️ **ch08 NO.207(108I-62) は oph 全体の最難（正答率37%）**＝眼窩底骨折の上転障害の原因筋を「上直筋」と誤答させる問題。正解は **下直筋の嵌頓**（機械的拘束なので**障害された筋と動かせない方向が逆**になる）。牽引試験陽性で麻痺性と分ける。
`_work/oph_pdf.py`（anstable/spec/text/images/save）と `_work/verify_oph_ch.py` が抽出・照合の道具。巻末解答一覧表はPDF p.125-129で列のx座標は精神科・皮膚科と同一、国試番号索引はp.130-131。⚠️ **PDF p.4 は「MECマイナー講座の特徴」の見本ページで中身は精神科の問題**——眼科の問題として拾わないこと。⚠️ 解答表を x 座標で切ると**章区切り行がNO列・解答列を汚す**（NO.50が `d問題`）ので、章区切りのyでその行を閉じる。本章の軸は「解剖と生理」「屈折と調節」「小児眼科（弱視・調節性内斜視）」「検査法」の4本で、**調節性内斜視だけで6問**（NO.7・37・40・42・43・44）ある |
| `皮膚科/` | マイナー講座・皮膚科（prefix `derm`）。章別HTML(`ch01_hifuka_kihon.html`〜)＋`images/`＋`derm_questions.json`（全9章の章名メタ）。HTMLが`questions_derm.json`のソース＝章ごとの生成器`_work/build_derm_ch{NN}.py`→`_work/build_derm_json.py`で再生成。精神科ch02以降と同方式（Q()に`patho`/`deep`/`point`を直接渡す）。**全9章249問・章頭NO.は 1／28／55／82／99／122／151／191／223**（生成器の`Q_START`がこれを持つ）。**2026-07-29に全9章249問が完成**（ch03「紅斑」NO.55-81・27問・画像16問23枚／ch04「角化症」NO.82-98・17問・画像8問13枚／ch05「水疱・膿疱」NO.99-121・23問・画像20問33枚／ch06「母斑と良性腫瘍」NO.122-150・29問・画像18問26枚／ch07「悪性腫瘍」NO.151-190・40問・画像25問46枚＝**derm最大の章**／ch08「感染症」NO.191-222・32問・画像21問32枚／ch09「その他の皮膚疾患」NO.223-249・27問・画像6問6枚＝**実質「褥瘡14問＋創傷治癒5問」の章**）。精神科との最大の差は**画像問題が主体**（PDF全体で埋め込みJPEG204枚・第1章は8問／第3章は16問）。⚠️ 問題文の`↗N`は解答表の**IRT型**の値で画像枚数とは無関係——画像の有無は「〜の写真を示す」の記載と`page.get_images()`で判定する。巻末解答一覧表はPDF p.155-159で、列のx座標は精神科と同一 |

## 問題数

実測値（`node -e`で `questions_*.json` の `chapters[].qs` を集計、2026-07-05時点）。

| 分野 | prefix | 章数 | 問題数 |
|---|---|---|---|
| 内分泌 | endo | 10 | 542 |
| 呼吸器 | resp | 9 | 506 |
| 循環器 | circ | 10 | 572 |
| 消化器 | dige | 11 | 501 |
| 神経 | neur | 11 | 594 |
| 肝胆膵 | hbp | 8 | 418 |
| 腎臓 | jinzo_d | 6 | 315 |
| 血液 | hema | 8 | 378 |
| 免アレ膠 | imma | 5 | 247 |
| 感染症 | kansen | 22 | 356 |
| 小児科 | peds | 13 | 373 |
| 産婦人科 | obg | 13 | 685 |
| **コア12科目 小計** | | **126章** | **5487問** |
| 精神科（マイナー講座） | psy | 8 | 256 |
| 皮膚科（マイナー講座） | derm | 9 | 249 |
| 眼科（マイナー講座） | oph | 8 | 213 |
| 耳鼻咽喉科（マイナー講座） | ent | 8 | 214 |
| 泌尿器科（マイナー講座） | uro | 6 | 242 |
| 整形外科（マイナー講座） | ortho | 6 | 174 |
| 麻酔科（マイナー講座） | anes | 2 | 52 |
| 放射線科（マイナー講座） | rad | 4 | 60 |
| 中毒・職業病（横断テーマ） | tox | 7 | 48 |
| 公衆衛生（公衆衛生講座） | ph | 5（全19章中） | 155（全619問中） |
| 実力試験Ⅰ | jitsu1 | 2 | 160 |
| 自作問題 | custom | 1 | 可変（現在28） |
| 暗記メモ | memo | 4 | 可変（現在121） |
| **総合計** | | **196章** | **7459問** |

※ `study.html` タイトルの「5487問」はコア12科目の合計（マイナー講座・実力試験・自作・暗記メモは含まない）。
※ 精神科は2026-07-28に全8章完成。`study.html` の科目ヘッダは実数（256問）を出す。
※ 皮膚科は2026-07-29に全9章249問が完成。`study.html` の科目ヘッダは実数（249問）を出す。
※ 眼科は2026-07-30に第1・2章76問、2026-07-31に第3〜7章115問、2026-08-04に第8章22問を追加して**全8章213問が完成**。`study.html` の科目ヘッダは実数（213問）を出す。
※ 泌尿器科は2026-08-11に第1章「泌尿器の基本」44問・第2章「腎」25問・第3章「尿管・膀胱・尿道」64問・第4章「前立腺」53問・第5章「精巣・外性器」28問、2026-08-12に第6章「泌尿器科感染症」28問を追加して**全6章242問が完成**（基本44／腎25／尿管膀胱尿道64／前立腺53／精巣外性器28／感染症28）。`study.html` の科目ヘッダは実数（242問）を出す。**これでマイナー講座5科目（psy/derm/oph/ent/uro）がすべて完成した。**
※ 整形外科は2026-08-12に第1章「整形外科の基本」30問・第2章「神経の障害」43問・第3章「関節疾患」33問・第4章「骨　折」33問・第5章「その他の疾患」27問、2026-08-13に第6章「ロコモティブシンドロームとサルコペニア・フレイル」8問を追加して**全6章174問が完成**（基本30／神経の障害43／関節疾患33／骨折33／その他27／ロコモ8）。`study.html` の科目ヘッダは実数（174問）を出す。**これでマイナー講座6科目（psy/derm/oph/ent/uro/ortho）がすべて完成した。**
※ 耳鼻咽喉科は2026-08-06に第1章20問・第2章30問・第3章48問・第4章3問・第5章36問、2026-08-07に第6章14問・第7章20問・第8章43問を追加して**全8章214問が完成**（耳20/30/48・鼻口唾3/36/14・咽喉頭20/43）。`study.html` の科目ヘッダは実数（214問）を出す。
※ 麻酔科は2026-08-13に第1章「周術期の麻酔」35問・第2章「緩和医療」17問を追加して**全2章52問が完成**。`study.html` の科目ヘッダは実数（52問）を出す。**マイナー講座7科目め**で、PDFは35ページ・全52問と講座中で最小。**これでマイナー講座7科目（psy/derm/oph/ent/uro/ortho/anes）がすべて完成した。**
※ 放射線科は2026-08-18に第1章「序　章」1問・第2章「放射線診断学」33問・第3章「放射線治療学」10問・第4章「医療安全・放射線防護」16問を追加して**全4章60問が完成**。`study.html` の科目ヘッダは実数（60問）を出す。**マイナー講座8科目め**で、**この科目だけPDFが2冊ある**（問題41ページ＋レジュメ43ページ）——解説はレジュメ編（放-2〜放-37）を下敷きに書いた。**これでマイナー講座8科目（psy/derm/oph/ent/uro/ortho/anes/rad）がすべて完成した。**
※ 中毒・職業病は2026-08-19に全7章48問が完成（金属中毒4／有機溶剤中毒5／農薬中毒6／その他の中毒6／自然毒3／ガス体中毒6／物理的原因による疾患18）。`study.html` の科目ヘッダは実数（48問）を出す。**マイナー講座ではなく「横断テーマ」の独立PDF**で、版面が違う（レジュメと問題が交互に並ぶ・解答表に★列とCBT列が無い・画像0枚）。**これで科目prefixは全24となり、総合計191章7304問**。
※ 公衆衛生は2026-08-25に第1章「医師法と医療法」71問・第2章「保健所」9問・第3章「死」32問・第4章「医療職」19問・第5章「医療保険」24問を追加（**全19章619問のうち第1〜5章155問が完成／残14章464問**）。`study.html` の科目ヘッダは実数（155問）を出す。**これで科目prefixは全25となり、総合計196章7459問**。**産婦人科685問に次ぐ規模の科目**で、**この科目だけPDFが2冊**（問題283ページ＋レジュメ96ページ）——**レジュメが解説の正本**。**マイナー講座ではなく独立した公衆衛生講座**。次は第6章「介護保険」NO.156-190（35問）。章を足すたびに総数が変わるので、`node _work/test_subject_totals.js` を必ず通すこと。
　 章を足すたびに `gamify.js` の `total`・`chapters_meta.js`・`study.html` の `subj-hdr-count` を実数へ更新すること。

## localStorage キー（全ページ共通）

**正本は `progress.js` 冒頭の `K*` 定数**（`KD`/`KF`/`KA`/`KR`/`KT`/`KE`/`KDT`/`K_SRS`/`KRT`/`KER`/`K_TOKEN`/`K_GIST` 等）。同期対象キーの追加・変更時はここと `_mergeRemote`（progress.js）・`pushToGist`のpayload・`index.html`の復元パスを揃えること。主要キー:

- `done_v2` — UID → 周回数（整数、0=未済）／ `done_tombstones_v1` — undo削除の墓標（同期で復活防止）
- `flag_v2` — 苦手UID → 設定時刻ms（旧データは1）／ `flag_tombstones_v1` — 旗解除の墓標（uid→解除時刻ms、マージは旗vs墓標の新しい方が勝つ）
- `mec_choice_v1` — UID → 選択肢別の誤答回数＋`_last`（最後に選んだ肢）。同期対象（回数はmax、`_last`はローカル優先）
- `activity_v1` — YYYY-MM-DD → 操作回数（連続日数🔥と30日間の学習記録の算出元）。**書き込み口は `logActivity()` の1本だけ**。通常モードは`mecIncrLap`、試験モード／SRS復習は`_markExamDone`（study_exam.js）から`window.mecLogActivity()`を呼ぶ。done_v2を直接書く新経路を足すときはここも通すこと（通し忘れるとその日が学習日として残らない）
- `myrate_v1` — UID → `{correct,total}`（試験モードの自己正答率。マージは各フィールドmax）
- `studytime_v1` — YYYY-MM-DD → 学習分数
- `mec_srs_v1` — SRS復習スケジュール ／ `mec_exam_resumes_v1` — 試験中断の再開データ ／ `mec_ch_exam_v1` — 章別試験履歴
- `mec_attempts_v1` — 解答イベントログ（attempts.js）。`"uid|t|c|o|s|m|sess|n"` の文字列配列・上限5000件。追記専用なので同期は`sess+n`をキーにしたunion＋時刻昇順ソート＋上限切り詰め
- `error_reports_v1` — 問題エラー報告 ／ `mec_err_cleared_at` — 一括消去のタイムスタンプ
- `mec_gist_token` — GitHub PAT（gistスコープ）／ `mec_gist_id` — Gist ID ／ `mec_last_sync_v1` — 最終同期時刻
- UIローカル設定（非同期）: `mec_subjects_v1`（選択科目）/`mec_filter_v1`/`mec_state_v1`/`mec_correct_sound_v1`/`mec_select_sound_v1`/`mec_boot_sound_v1` 等

## UID フォーマット

- 各科目解説: `{prefix}_ch{nn}_q{n}` 例: `endo_ch01_q1`, `resp_ch02_q3`, `jinzo_d_ch03_q136`
- 科目prefix（全25）: `endo` / `resp` / `circ` / `dige` / `neur` / `hbp` / `jinzo_d` / `hema` / `imma` / `kansen` / `peds` / `obg` / `psy` / `derm` / `oph` / `ent` / `uro` / `ortho` / `anes` / `rad` / `tox` / `ph` / `jitsu1` / `custom` / `memo`

### ⚠️ 問題番号は科目内の通し番号（章ごとにQ.1へ振り直さない）

`{prefix}_ch{nn}_q{n}` の `{n}` と、カードに表示される `Q.{n}` は **科目内で通し**。
章が変わっても続きから振る（産婦人科: ch01=Q.1〜26／ch02=<b>Q.27</b>〜85／ch03=Q.86〜145…）。
これは講座PDFの `NO.` と一致し、`_work/build_{sid}_json.py` が `id="qN"` からuidを作るので
**表示番号とuidの番号は常に同じ**になる。

- 根拠: `jumpToQnum`（study.html）は `.qn` のテキストで探し、コメントにも
  「Q番号は科目ごとの連番」とある。章ごとに振り直すと**同じ科目内にQ.1が複数できてジャンプが壊れる**。
- 2026-07-28に精神科(psy)がこれに違反していた（ch01〜ch04が全部Q.1始まり）ので、
  PDF巻末の解答一覧表と全問を突き合わせて是正した。psyの章頭NO.は
  1／74／99／140／176／194／219／243（全8章・最終NO.256）。
- 生成器は各 `_work/build_psy_ch{NN}.py` の **`Q_START` 定数**が章頭のNO.を持ち、
  カード番号は `Q_START + idx`、セクション見出しの「Q.a〜Q.b」も自動計算する。
  新章を足すときは `Q_START` を前章の最終NO.+1 にする。
- ⚠️ 振り直しを是正するとuidが変わる（psy ch02〜ch04がそうだった）。
  **旧uidに紐づくlocalStorageの進捗（done_v2・SRS・myrate等）は引き継がれない**ので、
  是正するなら早い段階で行う。

## UI 構造（study.html・各章共通）

### フィルター（2行）
- 行1（難易度）: 全問 / 難問(<60%) / 標準(60-80%) / 易問(≥80%) / 正答率なし / ★問題 / 🖼️画像
- 行2（状態）: すべて / 🚩赤旗 / 未済 / 済み

### カード内ボタン
- `🚩` 赤旗ボタン（`mecToggleFlag`）
- 自己採点 `×` `△` `○`（`mecIncrLap`・キーボード `1` `2` `3`／`Enter`=○）:
  3つとも `data-action="lap"` で、違いは `data-grade`（`ng`/`mid`/`ok`）だけ。
  どれを押しても周回数 +1・学習日の記録・次カードへのスクロールは共通で、
  変わるのは SRS へ渡す自己申告のみ（[SRSの自己採点]参照）。
  周回数と緑の塗りは常に `○`（`.mec-lap-btn`）が持つ。
  ⚠️ `.mec-lap-btn` というクラス名は変えないこと。`study_exam.js`・`progress.js`・
  キーボード操作・旧`selfcheck_intro.html`がこの名前で掴んでいる。

### SRSの自己採点（2026-07-24〜）

以前は「済」1つで、押すたびに**無条件で正解扱い**として SRS の間隔を伸ばしていた。
想起テストを経ずに間隔だけ伸びるため、通常モードで一周すると SRS が「定着済み」で埋まり、
試験モードでしか書かれない `myrate_v1` と食い違っていた。`_updateSRS(uid, grade)`（study.html）:

| grade | ef | 間隔 |
|---|---|---|
| `ok`（○ 余裕） | +0.1（上限2.5） | 1 → 6 → 前回×ef |
| `mid`（△ あやふや） | 据え置き | 1 → 3 → 前回×max(1.15, ef-0.6) |
| `ng`（× わからない） | -0.2（下限1.3） | 1日へ戻し reps=0 |

- 試験モードは従来どおり真偽値で呼ぶ（`true`=○ / `false`=×）。互換は維持されている。
- 直近の申告は `mec_srs_v1` の `g` に入る（同期対象）。カード上の選択表示に使う。
- 間隔上限90日・ef上限2.5は意図的（本番まで1年スパンで間隔を暴走させないため）。
- テスト: `node _work/test_srs_grade.js`

## セレモニーとトーストは1本の列（2026-08-18／2026-08-20に併合）

`gamify.js` の全画面セレモニー（LEVEL UP / MISSION COMPLETE / 章制覇 / 科目制覇）と
トースト（実績解除・個別ミッション達成）は、**`_annQ` という1本の列に積んで1つずつ再生する**。
テスト: `node _work/test_gamify_ceremony.js`（28件・仮想時計で実ソースを回す）。

⚠️⚠️ **2列（`_cerQ` / `_toastQ`）へ戻さないこと**（2026-08-20に併合した）。旧実装は2本が
**並行に**流れており、そのせいで ① 全画面セレモニーの真上にトーストが重なって両方読めない
② 「あと何件来るか」を数えられない、が同時に起きていた。**位置表示（3 / 4）と授与トレイが
意味を持つ前提条件がこの併合そのもの**なので、見た目や速さのために分けない。

### 授与トレイ ＝ 結果画面の「これから何件来るか」（2026-08-20・Phase 6）

結果画面（`#examOverlay`）の `#gmTrayMount` に **`🎖 今回の獲得 N件` の一覧を先に描く**。
行は未再生＝暗い／再生中＝🔆／済＝✅ で、**残り件数は「まだ暗い行」として常に読める**。
セレモニーはその本編にあたり、隅に `3 / 4` の位置表示を持つ（総数が1件のときは出さない）。

- **飛ばせる**——セレモニー／トーストを**タップすると次へ**、ヘッダの
  **「まとめて受け取る」で残り全部を打ち切る**（`_annSkipAll`）。
  ⚠️ **これを許せるのは「飛ばしても行がトレイに残る＝情報が失われない」から**。
  スキップ時に**音も fx も鳴らさない**（一気に鳴ると事故に聞こえる）。
  ⚠️ タップは**出てから `ANN_TAP_GUARD_MS`(350ms) の間は受け付けない**——入場アニメ中の指は
  結果画面へ向いた指なので、ガードが無いと出た瞬間に消える。
- ⚠️ **総数は `onExamFinish` が返った時点で確定している**（`_bumpMission` も `_afterEvent` も
  同期呼び出し）。だから**あの1行が唯一の描画アンカー**で、あとから行が増えて表がガタつかない。
  獲得0件のセッションだけは `_annPush` が走らず前回の一覧が残るので、同じ場所で `_annDone` を畳む。
- ⚠️ **マウント要素は study.html の結果画面の中にしか置かない**。無いページでは何も描かず
  従来どおり流れる＝**「結果画面だけ」がコード分岐ではなく構造で決まる**。
  他のページへ足すと、そのページでも獲得一覧が出るようになる。
- ⚠️ **位置表示は `.gm-cer` の中ではなく overlay の直下（画面下部）に置く**。中に入れると
  本編の直下＝画面中央付近に来て**トレイの見出しと重なる**（実機で確認して移した）。
  `#gmCerOv` は `inset:0` ではなく **`height:100dvh`** で切る（iOS Safari のツールバーが
  出ていると `inset:0` では下端が画面外へはみ出し、位置表示がツールバーの裏に隠れる）。
- ⚠️ **トーストの `add('show')` には rAF の落とし所を必ず置く**（`setTimeout(...,60)`）。
  非表示タブでは rAF が1フレームも来ないので、保険が無いと**一度も出ないまま寿命だけ尽きる**
  （ハブの `_tweenNum`・統計の `countUp` と同じ穴）。
- **裏タブで全部消化されることはある**（タイマーが間引かれ、戻った瞬間に一気に流れる）。
  これは許容する——**トレイに全件が残るので情報は1つも失われない**。
- reduced-motion では**セレモニーを1件も再生せず、トレイを最初から全点灯**にする。
  ⚠️ ただし**トレイが無い画面では従来どおり再生する**（出さずに捨てると情報が丸ごと失われる）。
- **獲得が多い日の圧縮はしない**（2026-08-20にユーザーが判断）。個別ミッション達成も1件＝1トースト
  なので、調子のいい日は6〜12件・20〜38秒になりうる。件数が見えてスキップできることで受ける設計。

- ⚠️ **`ceremony()` に上書き実装を戻さないこと**。旧実装は overlay の `innerHTML` を
  書き換えるだけでキューが無く、近接して2つ発火すると先の1つが誰にも見られないまま消えた。
  しかもそれが起きる条件が **「40問目の解答」そのもの**で、`onAnswer` の中の
  `_bumpMission → MISSION COMPLETE` の直後、**同じ同期呼び出しの中で**
  `_afterEvent → _checkLevelUp → LEVEL UP` が走り、MISSION COMPLETE は常に消えていた。
- **`opts.fx` / `opts.snd` は積んだ瞬間ではなく、そのセレモニーが実際に出る瞬間に鳴らす**
  （旧実装は積んだ瞬間に鳴っていたので、消えた方の音だけが残っていた）。
- **`toast()` の第4引数 `snd`** も同じ理由で足した。`SND.ach()` / `SND.mission()` を
  `toast()` の外で鳴らすと、保留されたトーストの音だけが試験中に鳴る。
- **`toast()` の第5引数 `label`** はトレイの行に出す文字。`'ミッション達成！'` のような
  汎用タイトルではなく `def.label`（実際に達成したミッション名）を渡す。
  `ceremony()` は `opts.icon` / `opts.label` で同じものを受ける（省略時は `gm-cer-big` から拾う）。

### 試験中は溜めて、結果画面で再生する

`_fxHeld()` が真の間はセレモニーもトーストも**出さずに溜める**。理由は2つ:

1. 全画面セレモニー(2.4秒)は tier 演出の真上に被る。`_microLapFx` / `_lapMilestoneFx` は
   先頭で `examMode` を見て黙るのに、**一番大きいこれだけが素通し**だった。
2. `#gmToast` は `top:14px` 固定＝**iPad では約180pxある試験ヘッダ(`.st-hdr`)の裏**に出る。
   出しても読めないので、出さずに取っておく方が情報が残る。

- ⚠️ **再生の開始は `examMode` 解除の直後ではない**。`showExamSummary` は
  ランクスタンプ(950ms)と祝賀花火(980ms)を自前で走らせるので、そこへ被せると両方読めなくなる。
  **`onExamFinish` が `_quiet(CER_SETTLE_MS=2000)` を置き、その後から再生する**
  （`onExamFinish` は結果画面の末尾で必ず1回呼ばれる＝セッションの終わりを知る唯一の確実な合図。
  この呼び出し自身が生む達成 exam/acc80/perfect も同じ静粛時間に乗る）。
  **この配管のおかげで `study_exam.js` 側の変更はゼロ**。
- ⚠️ **`_afterEvent` に `if (examMode) return;` を足さないこと**。記帳
  （`L.lastLevel` / `L.chDone` / `L.subjDone`）まで止まると
  **試験中に上がったレベルが二度と祝われない**＝取りこぼす。抑止は演出側だけで行う
  （ceremony/toast の保留・`_starGainFx`・章仕切りの光）。
- 保留タイマー `_armHold()` は**溜まっている間だけ1本**張り、解除を検知したら自分で止まる。
- レベルアップが「上がったその場」ではなく結果画面で出るのは**承諾済みの仕様変更**
  （集中を切らないことを優先）。

## デイリー／ウィークリーミッション（2026-07-30 拡張）

定義は `gamify.js` の `MISSIONS_DAILY`（8個）・`MISSIONS_WEEKLY`（8個）。進捗は同期キー
`mec_missions_v1` の**端末別カウンタ**（表示・判定は端末横断で sum）。テスト: `node _work/test_missions.js`。

### tier（core / bonus）— 新しいミッションを足すときの基準

`MISSION COMPLETE` セレモニーは **`tier:'core'` だけ**で判定する。core は「手を動かせば必ず届く」
ものに限ること（現在: 解答40問・試験セッション1本・試験で20問正解／週次は250問・120正解・7セッション）。

- ⚠️ 旧仕様はセレモニーが**全ミッション達成**を条件にしていて、そこに「試験で全問正解」という
  運任せの条件が混ざっていたため、日次のセレモニーが事実上一度も発火しなかった。
  在庫や運に左右されるもの（`perfect`・`acc80`・`srs`）は必ず `bonus` に置く。
- ハブ（index.html）の見出し件数が緑になる条件も core のみ。行の `data-tier` 属性で判定している。

### カウンタと加算口

| counter | 意味 | 加算する場所 |
|---|---|---|
| `ans` / `cor` | 解答数／試験モードの正解数 | `onAnswer`（通常モードの「済」も `onLap` から `ans` に算入） |
| `srs` | SRS復習セッションでの解答（正誤問わず消化数） | `onAnswer(..., {srs:true})`。`_recordMyRate` が `_srsReviewMode` を渡す |
| `redo` | 過去に落とした問題を正解し直した | `onAnswer(..., {wasWrong})`。**`myrate_v1` 加算前**の値で判定すること |
| `hard` | 難問（正答率60%未満）に触った数 | `onAnswer`／`onLap` から `_isHardQ(uid)`。正誤は問わない（正解だけだと難問を避けるほど有利になる） |
| `subj` | その日に触った科目数 | `_dailyFirstBumps`（`onAnswer`／`onLap` が同じ `_bumpMission` に合流させる）。同じ科目はその日1回だけ。**日次専用**（下記） |
| `day` | 学習した日 | `_dailyFirstBumps`（`onAnswer`／`onLap` が同じ `_bumpMission` に合流させる）。その日1回だけ＝週次バケットが「今週の学習日数」になる |
| `exam`/`acc80`/`perfect` | セッション完了／80%以上／全問正解 | `onExamFinish`（10問以上のセッションのみ） |
| `chexam80` | 章別試験で80%以上 | `onExamFinish(..., {chPrefix})`。同じ章は週1回だけ |

- `hard` の判定はカードの `data-rate` が正本（`study.html` のフィルタ「難問(<60%)」と同じ閾値
  `HARD_RATE = 60`）。**`data-rate` が無い問題（正答率なし）は難問に数えない**——出典に数字が
  載っていないだけで、難しいという意味ではないため。
- ⚠️ **`subj` を週次ミッションに使ってはいけない**。`_bumpMission` は日次と週次の両方へ足すので、
  週次側の `subj` は「日ごとの異なる科目数」の週合計＝同じ科目を5日やれば5になる（科目数ではない）。
  科目の広さを週で問うなら週キーの帳簿を別に持つこと（`chexam80` と同じ方式）。
- ⚠️ **`day` を core に置いてはいけない**。日数は**最終日に巻き返せない唯一のカウンタ**で、2日空けた
  時点でその週は到達不能になる。週次のペース表示は「カウンタ型は理屈の上では最終日でも巻き返せる」
  前提で遅れをグレーアウトしない設計なので、core にすると週の前半でセレモニーが死ぬ週が出る。
- 🚩の克服数（旧 `unflag`）は**2026-07-30に廃止**。① 旗が5個溜まっていない日は達成不能な在庫依存、
  ② 報酬が「旗を外すこと」に付くので弱点リストを畳む動機になる（旗は「後で戻る印」で、消すことは
  上達の証明ではない）、③ 解除はワンタップで想起テストを経ていない＝`cor`／`redo` と違い証拠が無い。
  `onFlag` は演出だけを担い、ミッションカウンタを触らない。代わりに日次へ `subj`（科目を2つまたぐ）を置いた。

- ⚠️ **章の「制覇」を週次ミッションの材料にしてはいけない**。旧 `chclear` は端末ローカルの
  `L.chDone` を見ていたため、全章を済にした時点で**永久未達**になり週次コンプリートが不可能だった。
  周回しても成立する指標（章別試験のスコア等）を使うこと。
- 期間キーは `'d:'+日付` / `'w:'+週キー` と名前空間を分ける。**月曜は日次キーと週次キー（その週の月曜）が
  同じ日付文字列になる**ので、素の日付で引くと `__all__` が衝突して片方のセレモニーが消える。

### 達成ボーナスXP

達成すると `mec_missions_v1.xp` に記帳され、`stats()` の XP に加算される（レベルに効く）。

```
xp: { banked: number, ledger: { 'd:2026-07-29': { ans:40, exam:40, __all__:150 } } }
```

- 「いくつ取ったか」ではなく**「何を取ったか」**を持つ。同じ `(期間キー, missionId)` にはどの端末も
  同じ値を書くので、同期の union マージで合流しても**二重加算にならない**。
- 台帳から落ちた古い期間は `banked` へ繰り入れる（総額が保存され、レベルが下がらない）。
  ⚠️ 保持日数 `MISSION_XP_KEEP_DAYS = 150` は **`gamify.js` と `progress.js` の両方**にある。
  両側が同じ日付基準で同じ繰り入れをすることで、同期が繰り入れ済みのキーを復活させて
  二重加算するのを防いでいる。**片方だけ変えないこと**。

### 週次のペース表示

週次の行だけバーに「今そこまで進んでいるべき位置」の目盛り（`.gm-pace`）を引き、遅れている行の
数字をアンバーにする（`.behind`）。見出しに残り日数を出す。カウンタ型のミッションは理屈の上では
最終日でも巻き返せるので、**達成不能としてグレーアウトはしない**（遅れの提示までに留める）。

## ヒーローのボタン（2026-08-04〜・席を固定）

`renderHero()`（index.html）。3つの席の役割は固定で、**中身が入れ替わることはあっても
席そのものが消えない**。0件の日は `.is-off`（薄く・`pointer-events:none`・href を外す）。

| 席 | due>0 | due=0 |
|---|---|---|
| 主 `#heroPrimary` | 🔔 復習をはじめる | 📚 全科目から選ぶ |
| 副 `#heroSecondary` | 📚 全科目から選ぶ → | 🔔 今日の復習はなし（無効） |
| 3つ目 `#heroTertiary` | 🔁 今日の誤答 N問を再履修 ／ 誤答0なら「🔁 今日の誤答なし」（無効） ||

- ⚠️ **統計への導線をこの3席に置かないこと**。旧実装は due が0の日だけ副が
  「📊 統計を見る」に化けており、**その日は復習ボタンがハブから消えていた**。
  席が入れ替わると「昨日あったボタンが無い」になる。統計の入口は
  ヘッダーの📊アイコン（`.hub-icon`）と☰メニューの2か所。
- ⚠️ 0件のとき href を外すのは飾りではない。`?mode=srs_review` / `?mode=today_wrong` は
  対象0で起動するとトーストを出して通常閲覧へ戻るだけなので、押させても無駄足になる。

### ボタンと計器ベイの演出（2026-08-14〜・E1〜E10）

テスト: `node _work/test_hero_cta.js`（43件）。

- **粒子の色は「席」ではなく「中身」で決める**。`renderHero` が各ボタンへ `data-fx`
  （`srs`＝青 / `browse`＝緑 / `redo`＝赤）を入れ、`_ctaColors()` がそれを引く。
  主ボタンは due の有無で復習と科目選びが入れ替わるので、**席で色を固定すると
  「同じ緑が復習を指す日」ができて意味が濁る**。
- **誤答ボタンの `data-load`（1/2/3＝1・10・30件〜）は縁の脈の速さ**、`#statDue` の
  `data-load`（15/50/120問〜）は**復習待ちの滞留の色**。どちらも0で属性ごと外すこと
  （脈が残ると「片付いた」の✓と矛盾する）。
- ⚠️ **`.cta-sub` に付けた `overflow:hidden` は `min-width:min-content` とセット**。
  フレックス項目の `min-width:auto` は `overflow` が `visible` の時しか min-content に
  解決されない＝`hidden` を付けた瞬間に最小幅が0になり、**4つのボタンが折り返さず
  横一列に潰れて文言が全部切れた**（実際に起きた）。
- ⚠️ **入場と常時の脈を同じ要素の `animation` に書かないこと**（後勝ちで片方が死ぬ）。
  入場＝要素本体（`.hero-cta a` / `.strip-c` の `--i` スタッガー）、
  常時＝`::before`（`.cta-redo[data-load]::before`）で分担している。
- ⚠️ **E10（遷移前のひと呼吸）は `<a>` のクリックを横取りする**。素通しすべきものを
  殺さないこと＝修飾キー・中クリック・`target`・`href` なし・`is-off`・
  他で `preventDefault` 済み。**演出が例外を投げても必ず遷移させる**（`try` の外で
  `setTimeout(go, …)` を2本＝余韻用と保険用）。reduced-motion では横取り自体をしない。
- ⚠️ **`_tweenNum` には rAF が止まった時の落とし所が要る**（`setTimeout(finish, dur+400)`）。
  非表示タブでは rAF が動かないので、保険が無いと**裏で開いたハブの数字が開始値
  （多くは0）のまま凍る**。大きな読み値・XP・計器行の3つが全部これに乗っている。
- 「今日解いた問題」の数字は `data-goal`（1＝100%以上 / 2＝150%以上）で金になる。
  段の出どころは `MecGamify.dailyGoal()` の pct ＝**ゲージと同じ正本**で、
  ゲージ（機械の動き）と読み値の色で同じ事実を2通りに出している。

#### 常時の演出（F1〜F5・2026-08-14）

E1〜E10 は「押した時」しか動かず、開いた瞬間の見た目はほぼ静止画だったので足した。

- ⚠️ **1要素で使える層は3つしかない**（本体の `animation` / `::before` / `::after`）。
  分担は固定で、**同じ層に2つ置くと後勝ちで片方が黙って死ぬ**:

  | 層 | 用途 |
  |---|---|
  | 本体 | E3 の入場（`--i` スタッガー・`.hero.settled` で止まる） |
  | `::before` | F2 呼吸するリング（誤答は `data-load` で速くなる） |
  | `::after` | F1 光沢。**ただし `is-off` の席では E5 の ✓ が使う** |

- ⚠️ **光沢は `.cta-sub:not(.is-off)::after` に限定すること**。無条件に当てると
  片付いた席の ✓ が `position:absolute` を食らって字が飛ぶ。
- ⚠️ **ボタン用の CSS 変数は `--cta-` 接頭辞を必須にする**。最初 `--cta-cb` を `--cb` と
  書いたところ、`vars.css` の共通トークン（カード背景色 `rgba(...)`）を**継承で拾い**、
  `animation:ctaBreath var(--cb,3.4s) …` が色を duration に食って**主・副ボタンの呼吸が
  黙って死んだ**（誤答ボタンだけは自前で `--cb` を上書きしていたので動き、
  「1つだけ動く」という気づきにくい壊れ方になった）。**`var()` を含むショートハンドは、
  置換結果が不正だとプロパティごと `unset` になる**。`test_hero_cta.js` が
  `vars.css` のトークン名と突き合わせて再発を止める。
- **文言を入れる口は `_setCtaLabel()` の1つだけ**（先頭の絵文字を `.cta-ic` に包んで跳ねさせる）。
  `textContent` へ直接入れると**その席だけ絵文字が動かなくなる**。テストが全席を検査する。
- **F4 の常時粒子（`_startCtaAmbient`）は段が変わらない限りタイマーを張り替えない**。
  `renderHero` は同期完了で何度も走るので、毎回 `setInterval` すると多重に撒かれる
  （ゲージの `_startGaugeAmbient` と同じ約束）。
- **片付いた席（`is-off`）は光沢も呼吸も絵文字も止める**。動くと「まだ何かある」に読める。

### 見出し行の日付（2026-08-13〜）

「今日解いた問題」と同じ行の右端に `#heroDate`（例 `2026年8月13日(木)`）。文字列は
`_todayLabelJa()`（index.html）が作る。

- ⚠️ **端末のローカル日付ではなく JST で切ること**（`Date.now() + 9h` を作って `getUTC*` で読む）。
  `activity_v1`・ミッション・`mec_attempts_v1` の日境界が全部 UTC+9 なので、ここだけ端末ローカルに
  すると深夜0〜9時に**日付と数字が別の日を指す**。

## 今日の誤答を再履修（2026-08-04〜）

ハブの3つ目のボタン（`#heroTertiary`）→ `study.html?mode=today_wrong`。
その日落とした問題だけを科目をまたいで出し直す試験セッション。

- **対象UIDの正本は `MecAttempts.todayWrongUids()`（attempts.js）**。ハブの件数表示と
  出題側（`startTodayWrongReview`）が同じ関数を呼ぶ＝数え方を2か所に書かない。
  そのために `index.html` も `attempts.js` を読み込む。
- **「今日間違えた問題すべて」が対象**＝あとで正解し直してもリストから消えない。
  同じ問題を何度落としても1件。並びは最初に落とした順（時系列）。
- **1セッション50問（`TODAY_WRONG_LIMIT`）で区切り、続きは結果画面の「🔁 続けて次の50問」**
  （2026-08-05〜）。⚠️ **どこまで出したかは `_todayWrongDone`（study.html）が持つ**。
  SRS復習は解けば due から外れるので毎回「先頭50問」を取り直せば次へ進むが、今日の誤答は
  **解き直しても集合から消えない**ので、位置を覚えないと同じ50問が出続ける（51問目以降へ
  永久に到達できなかった旧不具合）。残り件数も集合の大きさではなく `_todayWrongRemaining()`
  ＝未出題の位置で数える（`_srsDueRemaining` を流用すると常に0になる）。
  位置はページ内だけの記憶で、ハブから入り直すと常に1問目から。
- ⚠️ **`_srsUnloadedForReview` は代入で上書きしないこと**（`||` で足す）。「続けて次の50問」は
  科目カードを解放済みの状態から入るので `_loadedSids.size === 0` になり、上書きすると旗が
  落ちて通常閲覧へ戻ったときにカードが1枚も無い画面になる。
- ⚠️ **母数は `mec_attempts_v1` に残るものだけ＝試験モード・SRS復習・章別試験**。
  通常モードの「済」（×△○）は正誤を残さないので、通常モードだけで解いた日は0問になる。
  これは不具合ではない（[採点データの不変条件]と同じく `_recordMyRate` の仕様）。
- ⚠️ ハブの件数 ≧ 実際の出題数。出題側は更に科目の実在チェックと `_isScoreExcluded` で絞る。

### 🚧 TEMP: 「昨日の誤答を再履修」（2026-08-06〜・一時的）

ユーザーの依頼で入れた**仮設の4つ目のボタン**（`#heroYesterday` → `study.html?mode=yesterday_wrong`）。
**あとで消す前提**なので、席は固定3つという原則の外にある例外として扱うこと。恒久機能として
育てない。出題の配管は今日ぶんと完全に同じで、対象の日（`_wrongDayOffset` 0/1）と文言だけが違う。

消すときは以下をまとめて（`attempts.js` の `yesterdayWrongUids` のコメントが正本）:
① `attempts.js` の `yesterdayWrongUids`（`_wrongUidsForDay` は `todayWrongUids` が使うので残す）
② `index.html` の `#heroYesterday`（マークアップ＋`renderHero` 内の同名ブロック）
③ `study.html` の `mode=yesterday_wrong` 分岐・`_wrongDayOffset`・`_wrongDayJa`
④ `study_exam.js` の `_wrongDayJa()` 参照3か所（見出し・結果タイトル・完了バナー）
⑤ `_work/test_attempts.js` の `yesterdayWrongUids` 2件

### `_srsReviewMode` と `_todayWrongMode` を混ぜないこと

配管（専用ホストへ必要な問題だけ起こす・中断データを持たない・フィルターを隠す）は
SRS復習と共通で、判定は **`_isHostSession()`**（study_exam.js）に集約してある。
新しい配管の分岐を足すときは個別フラグではなくこれを使う。

一方、次の4つは **SRS復習だけの意味**なので `_srsReviewMode` のまま残す:

| 箇所 | 混ぜると起きること |
|---|---|
| `onAnswer(..., {srs})` | 週次ミッションの `srs`（SRS復習の消化数）が水増しされる |
| attempts の `m` | `s`（SRS復習）と `e`（試験）の区別が消え、弱点カルテの母数が濁る |
| 完走演出の「続けて次の50問」 | 両モードとも続きのボタンを出すが**残りの数え方が違う**（SRS=`_srsDueRemaining()`／再履修=`_todayWrongRemaining()`）。混ぜると再履修側が常に「残り0」で続きへ進めない |
| 結果画面のタイトル | どちらのセッションを終えたのか読めなくなる |

`retryWrongExam`（誤答再試験）は `_lastSessionWasSrs` / `_lastSessionWasTodayWrong` を
見てどちらのモードへ戻すかを決める。**両方立てないこと**。

## ハブの円弧ゲージ＝「今日の目標」（2026-07-29〜）

`index.html` のヒーロー右の円弧ゲージは **「一日にやるべき問題数のうち何％まで来たか」**。
以前は生涯の全体進捗（済 / 全7472問）だったが、毎日ほとんど動かない数字だったので主役を降ろし、
**旧・全体進捗はゲージ下の小さな一行（`.gauge-all`）に残した**。

- **分子も分母も `MecGamify.dailyGoal()` が正本**（gamify.js）。日次ミッション `ans`（40問 解答する）の
  target と端末横断カウンタをそのまま借りる＝**ゲージのすぐ下に並ぶミッション行と必ず同じ数字**になる。
  index.html 側に目標値を持たせないこと（`DAILY_GOAL_FALLBACK` は gamify.js が読めない時の代用の1箇所だけ）。
  目標を変えたいときは `MISSIONS_DAILY` の `ans` の `target` を直す。
- ⚠️ **達成率を 100% で頭打ちにしないこと**。超えた日は `130%` と出す。弧だけが2周目
  （`.gauge-ovf`＝1周目に重ねて描く弧）に回り、200%超は2周目が満タンで止まる（**数字は素の値のまま**）。
  3桁は円からはみ出すので `.gauge-mid.wide` で一段字を落とす。
- 演出の段は `_goalTier(pct)`（0 / 1〜4=途中 / 5=達成 / 6=1.5倍超）。`data-tier` を `.gauge` に載せ、
  CSSが「発光 → 歯車列が速く回る → 盤面が脈打つ → 達成色」と層を積む。粒子は `_gaugeCelebrate(tier)`
  が同じ段構造で撒く（**2026-08-13に画面全体を使う層の閾値を段5→段3へ下げ、全体の量も上げた**）。
- **盤面は viewBox 168×168・中心(84,84)**（2026-08-13に 140×140 から拡大）。弧の半径は 54 のまま
  ＝`GAUGE_C = 2π×54` は据え置きで、**外側に空いた余白へ歯車列を大きく載せている**。
  CSS幅は `clamp(148px,40vw,208px)`。⚠️ viewBox を変えるなら `GEARS` の座標・`<circle>` の cx/cy・
  光点の座標・`transform-origin` を全部揃えること（テストが viewBox をマークアップから読んで検査する）。
- **祝砲（`_gaugeCelebrate`）は段が上がったときだけ鳴らす**（`_gaugeTierShown`）。Gist同期の完了で
  `renderHero()` は何度も走るので、条件を外すと同期のたびに祝砲が上がる。
- **常時の演出は `_startGaugeAmbient(tier)` が別に持つ**（2026-08-13〜）。1.7秒ごとに歯車の噛み合いから
  火花・軸から蒸気（段3以上は歯車の粒も）、**22秒ごとにその段の祝砲をもう一度**撒く＝眺めている間ずっと
  機械が動いて見える。⚠️ **段が変わらない限りタイマーを張り替えないこと**（`_gaugeFxTier` の早期return）。
  再描画のたびに `setInterval` が増えると多重に撒かれる。撒く条件は `_fxOk()`（reduced-motion・
  非表示タブ）＋**ゲージが画面内にいること**。段0（まだ1問も解いていない日）は火花だけで祝砲は鳴らさない。
- テスト: `node _work/test_daily_goal.js`（index.html から `_goalTier`/`_driveGauge`/`_startGaugeAmbient`/
  `_gearPath`/`GEARS` を切り出して実行するので、関数名や `let _gaugeTierShown` / `let _gaugeFxTimer` の
  宣言を変えるとテスト側も直す必要がある）。

### スチームパンクの歯車（2026-07-29）

ゲージの意匠は**真鍮・銅の歯車列**。色はテーマに振らず固定（`.gauge` の `--brass`/`--brass-hi`/`--copper`）。
進捗の弧（`--or`／達成の `--gr`）とは別系統の色にして、「読み値」と「機械」を描き分けている。

- 歯車の `d` は `_buildGears()` が起動時に**歯数から生成**する（`_gearPath` + `_holePath`、`fill-rule:evenodd` で軸穴）。
  諸元は `GEARS` 定数：大24枚／小 a=12・b=16・c=10・**d=14** 枚（四隅に1枚ずつ）。
  `gear-c` は段3・`gear-d` は段4で濃くなる。**歯車を足したら CSS に `.gear-d{animation:gearSpin
  calc(var(--gear-t) * 歯数比) ... reverse}` を対で書く**（テストが歯数比と逆回りを検査する）。
- ⚠️ **大歯車の軸穴（`hole`）は弧の外側に置くこと**（現在 hole 66 ＞ 弧 r54＋線幅の半分5）。
  内側へ食い込ませると読み値（％）に歯車の面が重なる。テストがこの関係を守る。
- ⚠️ **速さは `--gear-t` 1本だけを動かすこと**。小歯車の duration は `calc(var(--gear-t) * 歯数比)` で、
  逆回り（`reverse`）。個別に `animation-duration` を上書きすると歯数比が崩れて噛み合いが嘘になる。
- ⚠️ 小歯車の位置は「歯先が大歯車の歯の領域（root〜tip）へ食い込む」距離で決めてある。
  座標を動かすと離れて浮くか、深く刺さって潰れる。`test_daily_goal.js` がこの寸法を守る。
- 粒子も同じ意匠。花火・紙吹雪・絵文字は使わず、**真鍮の火花（`shard`）・回る歯車（`gears`/`gearRain`）・
  蒸気（`steam`）**で作る。⚠️ 蒸気は `blend:false`（加算合成にすると湯気ではなく発光体になる）。
- canvas と SVG で歯車の実装が**2本ある**（`fx_engine.js` の `gearPath` と index.html の `_gearPath`）。
  テストは両方の輪郭を検査する（canvas側は rAF を掴んで1フレーム描かせて確認している）。

## 試験モードの出題範囲は examQueue が権威（2026-08-24）

「整形外科だけで試験を始めたのに、一度誤答すると他科目を巻き込んだ複数科目の試験が進む」
不具合の修正で入れた不変条件。テスト: `node _work/test_exam_queue_scope.js`（19件）。

出題キューの正本は `examQueue`。**所属判定は `_examSet`／並び順は `_examOrder`**（`study_exam.js`
冒頭）で、`examQueue` を差し替えたら**必ず `_examSyncQueue()` を呼ぶ**（startExam・resumeExam・
`_removeCardFromExam` の3か所）。`_examHas(card)` が唯一の所属判定で、`window._examHas` として
study.html からも引ける。

⚠️ **「次の問題」も「クリック可否」も「採点可否」も、DOM の全走査で決めないこと。**
旧実装は `_getExamTargetCard()`・`_scrollToNextCard()`・選択肢の click ハンドラ・`revealAnswer()`
の4つが `document.querySelectorAll('.qc[data-uid]')` を走査しており、キュー外のカードを
締め出していたのは **startExam で1度だけ**実行される「キュー外は `display:none`」の1行だけだった。
`revealAnswer()` は `examBySubj[sid]` を無条件に作るので、キュー外のカードを1問通すだけで
別科目がセッションに生える。

⚠️ **開始後にDOMへ足されたカードにも関所を貼り直すこと**（`study.html` の `_applyJson`）。
直接の引き金は**試験中リロードの自動復元**——`mec_exam_active_key` に残った古いセッション
（例 `anes:52`）を見つけると `selectedSubjects` へその科目を足して `_fetchSubjectCards` する。
`?sid=ortho` で開いても麻酔科52問がDOMに載るので、開始時の関所より後に着いたぶんが素通しになる。
**実機で再現済み**（2026-08-24・127.0.0.1 で `selectedSubjects = ['ortho','anes']`）。

⚠️ **選択肢の click リスナーは `_bindExamChoices` / `_examChoiceClick` の1組だけ**（2026-08-25 に `startExam` と `resumeExam` の二重定義を併合した）。リスナーは `ch.dataset.examInit` で一度きり付き、以後どのセッションでも生き続けるので、**ガードは `examMode` だけでなく `_examHas` も見ること**。

⚠️⚠️ **`exitExam` で `dataset.examInit` を消さないこと。** `removeEventListener` はしていないので、フラグだけ消すと次の `startExam` が**2本目**を張る。単一選択は `revealAnswer` の `exam-revealed` ガードが二重採点を弾くが、**複数選択は `classList.toggle('exam-selected')` が2回走って選択が入らない＝その問題が解けなくなる**。シャッフルされた問題は `_restoreChoices` が肢を**シャッフル前のクローン**で差し替えるのでノードごと入れ替わりフラグもリスナーも落ちるが、**シャッフル対象外（`.qimg-row` の画像問題・`.qt u` の下線部参照型・①②③/a〜eだけの参照型）は残る**——2026-08-25 まで、同じページで2回目の試験を始めると該当317問（画像あり×正解2つ以上）が押せなかった。

⚠️ **併合前は `resumeExam` 側のリスナーにだけ `_examHas` が無く、2026-08-24 の修正が再開経路だけ素通しになっていた**（`revealAnswer` の入口ガードが採点は止めるので、肢が選択状態のまま固まる形で出る）。ついでに選択音・スパーク・採点除外の中立表示も欠けていた＝「再開した試験だけ手触りが違う」状態だった。**分岐を2本に戻さないこと。**

⚠️ **リスナーは名前付き関数（`_examChoiceClick`）で渡すこと。** `addEventListener` は**同じ関数オブジェクトの重複登録を仕様上無視する**ので、名前付きで渡している限り万一もう一度 bind しても2本にはならない（旧コードは毎回インラインの `function(e){…}` リテラルを渡していたので別オブジェクトになり、本当に2本付いた）。インライン化に戻すとこの保険が消える。**2026-08-25 に実機で確認済み**——同じ参照の再 bind は `listeners:1` のまま、別オブジェクトを足すと `listeners:2` になり複数選択が `selected:false`（＝押しても入らない）になる。

⚠️ **連続正解中に露見しないのは演出のせいではない。** 正解時だけ `_scrollToNextCard` が必ず
キュー内の次カードへ強制スクロールして視界をキュー上に固定するのに対し、**誤答時は自動スクロールが
無い**（`_maybeShowFinishBtn()` を呼ぶだけ）ので、ユーザーが自力でスクロールしてキュー外へ
入り込む。誤答は原因ではなく**露見の契機**。エフェクトを減らしても直らない。

## 採点データの不変条件（試験モードが壊れる原因になる）

試験モードの必要選択数は **`.ch2.ok` の個数**（`_getRequiredCount()`・study_exam.js）で決まる。
`ans_label` や「Nつ選べ」バッジは採点に一切使われない。したがって:

- **`ok` が1つも無い問題は、何を選んでも不正解になる**（例外を出さず黙って全問不正解）。
  2026-07-09に呼吸器ch02〜ch09の429問がこの状態で、以前から「正解でも不正解」と報告されていた原因だった。
- 選択肢は `a`,`b`,`c`… の順で1つずつ独立した要素であること。`"a　①　/　b　②"` のように結合していると選べない。
- `ok` の個数は問題文末尾の「Nつ選べ」と一致すること（`複数正解`・`採点除外` バッジのある問題を除く）。
- 📷バッジ（`bi`）と `imgs` の有無は必ず一致させること（🖼️フィルタの根拠）。
  2026-07-24に jitsu1 53問・custom 8問で欠落が見つかり `_work/fix_missing_bi_badges.py` で修正した。
  同スクリプトを `--dry-run` で流せばいつでも検出できる（現在0件）。
- `N択` バッジは PDF の `↗N`（画像枚数と無関係の内部マーカー）を誤読していたことがある。**正解数の根拠は問題文の「Nつ選べ」のみ**。
- 連問の図は右段にまとめて置かれ、図の直下に `A` `B` `C` のラベルが描かれる。**帰属はラベル文字の座標で決めること**（読み順=A,B,C とは限らず、実際に `B A` の順で並ぶ紙面がある）。ステムが参照する図は連問1問目、後続の設問が参照する図はその設問に付ける。

### 選択肢を持たない問題（2026-07-26〜）

選択肢が0個の問題は**計算問題50問だけ**になった（2026-07-28に欠落分の復元が完了）。
`node _work/test_calc_input.js` の「選択肢を持たない問題は計算問題50件だけ」がこれを守る。
（当初48問。下記「正解肢（ok）が無いカードの修復」で過去問2問が入力型へ戻り50問になった。
⚠️ **公衆衛生 ch07・ch08 のマークシート計算6問を入れると56問になる**＝そのときテストの
定数も一緒に上げる。`_work/公衆衛生_引き継ぎ.md` §3-5 に同じことが書いてある）

- **計算問題50問（科目33・過去問17）** — 原文がマークシートの桁入力。`ans_label` は
  **`計算答：<桁文字列>` の正規形**でなければならない（例 `計算答：2.0` `計算答：0.40` `計算答：315`）。
  桁数＝文字数、小数点位置＝文字列そのもの。**正解は数値ではなく桁文字列**で、`0.40` の
  先頭ゼロ・末尾ゼロは意味を持つ（数値化して `0.4` にすると採点が壊れる）。
  採点は完全一致・部分点なし。`calc_input.js` が入力欄を作り、`revealAnswer`（study_exam.js）／
  `ceSubmitCalc`（chapter_exam.js）が採点する。
  - 旧形式 `計算答：2,0`（カンマ区切り）は小数点位置が読めないので**戻してはいけない**。
    形式の統一は `node _work/normalize_calc_answers.js`（冪等・`--dry-run` あり）。
  - 同じ問題が questions_*.json と 国家試験過去問/*.html の両方にあるものが11問あり、
    正規化スクリプトはこれを使って桁数と小数点位置を相互検証・相互補完する。
  - ⚠️ 同じ正規表現が `calc_input.js`（CANON）・`build_qmeta.py`・`pdf_audit.py` の3箇所にある。
    `node _work/test_calc_input.js` が乖離を検出する。
- **選択肢データの欠落 — 26問すべて復元済み（2026-07-28完了）**。図のa〜eや組合せの選択肢が
  データから落ちていたもの。計算問題ではないので入力型では解決しない。
  12問はPDFのブロック解析で復元（下記）、残り14問は下記「表・図の選択肢の復元」で復元した。
  未解決の欠落が再発した場合の受け皿は残してある：試験モードは中立で開封して先へ通し
  （`_isExamUngraded`）、過去問側は出題から外して理由を表示する（`ceIsAnswerable`）。
  ⚠️ `117F74` だけ **a〜f の6択**（表が6行）。5択決め打ちのコードを書かないこと。

### 欠落選択肢のPDF復元（2026-07-26）

`python _work/extract_missing_choices.py` で抽出・検証 → `python _work/apply_missing_choices.py` で適用。
⚠️ この2本は `.gitignore` の `extract_*.py` / `apply_*.py` に該当し**Git管理外**（マシンローカル）。
別マシンでは再作成が必要。以下の知見はそのための記録でもある。
**テキストの並び順ではなくPDFのブロックを見る**のが要点。MEC解説集は1問が
「設問文＋選択肢」「着目point」「選択肢考察」「正解」「正答率（選択率）」の独立ブロックに
分かれており、素のテキスト順で切ると最後の選択肢（ｅ）が解説を飲み込む。

信頼度は PDF が持つ独立な3情報と `ans_label` の一致で判定する:
「正解」ブロック／選択率が最大の肢／全体正答率が `data-rate` と一致。2つ以上一致で `ok`。

- ⚠️ **`○`/`×` マーカーは検証に使えない**。MEC は「その記述が正しい」の意味で使う紙面と
  「これが正解」の意味で使う紙面が混在する（`116C-41` は `×ｃ` が正解、`116B-15` は `○ｄ` が正解で
  どちらも否定形の設問）。参考情報としてだけ出す。
- ⚠️ 検証材料はページ全体から拾ってはいけない。1ページに2問載る紙面があり前の問題の正解を
  拾う（`116C-41`）。次ページを無条件に足すと次問のマーカーを拾う（`116B-15`）。
  **選択肢ブロックの直後から「正答率」ブロックまで**で閉じる（`verify_region`）。
- 選択肢ラベルの直後は **U+0001**（PDF・過去問HTMLの qt 共通）。空白決め打ちでは切れない。

### 表・図の選択肢の復元（2026-07-28・残り14問を完了）

`python _work/restore_table_choices.py`（冪等・`--dry-run` あり・**Git管理下**）。
上の PDF ブロック解析では最後まで取れなかった14問。2種類あった。

- **表・図が選択肢の11問** — PDFからテキスト抽出すると列の対応が崩れるため、
  当時は**ユーザーがスクリーンショットを撮って送り、それを目視で読んで書き起こした**。
  ⚠️ **2026-08-19 にこの依頼は不要になった**——`fitz` で該当ページを **300dpi（読めなければ
  600dpi）で描画し、その画像を自分で目視して読む**（ユーザー承認済み。公衆衛生155問・
  エラー報告19問の修正はこの方式）。手順は `_work/新科目HTML生成ガイド.md` §1。
  依頼するのは**描画しても判読できなかったときだけ**。
  書き起こしは**列見出しを各肢に埋め込む**（`ｃ　Na 75／K 0／Cl 75／…`）。
  表を別途置く方式は肢のシャッフルで対応が崩れるので採らない。単位は qt 末尾に注記。
  ⚠️ **表を直したら `ans_label` も作り直すこと**（列の対応を直すと正解肢の文字列が変わる。
  2026-08-19 に3問がこれだった）。⚠️ **5択決め打ちのコードを書かない**（6択・7択・9択が実在）。
- **カードごと別問題の紙面から作られていた3問**（`118C15` `119E7` `119E14`）— qt に隣の問題の
  check point の表が丸ごと入り、設問文も選択肢も失われていた。`119E14` は解説まで別問題
  （《処方箋》）のものだった。**`data-rate` と正解の肢は正しかった**ので、その2つが一致する
  問題をPDFから同定して設問文・選択肢・解説を復元した。同種の破損を探すときはこの2つを鍵にする。

### 正解肢（ok）が無いカードの修復（2026-07-28・過去問9件を完了）

`python _work/restore_missing_ok_flags.py`（冪等・`--dry-run` あり・**Git管理下**）。
選択肢はあるのに `ok` が1つも無い＝**何を選んでも黙って不正解**になる過去問カード9件を直した。
`node _work/test_calc_input.js` の「正解肢が無いカードは0件」がこれを守る（1件でも増えると落ちる）。

- **7件は正解ラベル(`ac`)ごと空**だった＝抽出時に「正解」ブロックを取り落としたもの。
  **9件中7件が「2つ選べ」**で、複数正解の問題ほど落ちやすい。
  PDFの「正解」ブロックを正本に、選択率が最大の肢・`data-rate` との一致で裏取りしてから入れる。
- **2件は計算問題に別の設問の選択肢が付いていた**（`117F71` `118C73`）。選択肢を外して
  `calc_input.js` の入力型に戻し、`node _work/normalize_calc_answers.js` で
  `計算答：6,5`→`計算答：65` に正規化した。**選択肢を持つカードは normalize の対象外**なので、
  この2件だけ旧カンマ形式が残り続けていた（バグが別のバグを隠していた例）。
  これで計算問題は48→**50問**。

### ⚠️ 未解決：連問（次の文を読み〜）のサブ設問が設問文と選択肢を共有している（147カード）

上の `117F71` を調べる過程で見つかった**別口の未解決問題**。過去問カード1825枚のうち
**147枚が「直前のカードと qt も選択肢も完全一致」**している（**2026-08-26 に数え直したら
2000枚中154枚**＝件数は増えているが状況は変わっていない）。連問（`次の文を読み、71〜73の問いに答えよ`）を
HTML化したときにサブ設問ごとに切り分けられず、グループ内の全カードが**同じ設問文と同じ1組の選択肢**を
持ってしまっている。正しいのはグループ内の1問だけで、残りは別の設問の選択肢で出題・採点される。

- 検出: 連続するカードで `qt` と `.ch2` の並びが完全一致するものを数える（147件・
  一致したものは**例外なく選択肢も一致**していた）。
- `ac`（正解ラベル）の肢の文字は正しいことが多いので、`.ch2.ok` の位置は合っていても
  **表示される選択肢の文言が別設問のもの**という壊れ方をする。件数チェックでは見つからない。
- 直すには PDF から**サブ設問ごとの設問文と選択肢**を取り直す必要がある（PDFでは
  `72　この患者で認める可能性の高い身体所見はどれか。ａ…ｅ…` のように番号始まりの独立ブロックに
  なっているので機械的に切り出せる見込み）。表が選択肢のサブ設問はスクリーンショット依頼になる。

検査は `python _work/pdf_audit.py {sid}` （PDFを正本に中身まで照合。`--no-image` で画像照合を省略）。
選択肢0個の問題は以前は無検査でスキップしていたが、正解データが解釈可能かを検査するようにした。
既存の `_work/audit_image_mismatch.py` はファイル名しか見ないため、ページのスクリーンショットが
正しい名前で貼られているケースを見逃す。`pdf_audit.py` はこれを知覚ハッシュで検出する。

## 弱点分析（2026-07-23〜）

「事実の抽出はJS、解釈だけAI」が方針。指標はローカルの決定論コードで計算し、AIには数字の解釈だけ聞く。
AIを呼ばなくても stats.html の弱点カルテとして単体で成立する。

```
attempts.js (mec_attempts_v1)  ← 解答イベントの生ログ（時刻・出題順・所要秒・選んだ肢）
qmeta.json  (build_qmeta.py)   ← 設問形式の自動分類
        ↓
stats.html「🩺 弱点カルテ」     ← 科目×設問形式ヒートマップ／本番正答率帯カーブ
        ↓
「AI相談用にコピー」のMarkdown  ← 集計済みの表だけを渡す（問題文は含めない）
```

- **正誤が記録されるのは試験モード＋SRSのみ**（`_recordMyRate`／`_logAttempt`・study_exam.js）。
  通常モードの「済」はSRSに正解扱いで入るだけで正誤は残らない。分析の母数はこれで確定。
- `questions_*.json` を更新したら `python _work/build_qmeta.py` を流して qmeta.json を作り直す。
- ヒートマップの配色は**アンバー単一色相の逐次ランプ**（強い＝面に沈む／弱い＝明るく浮く）。
  塗りだけに意味を負わせないよう全セルに％と受験回数を印字してある。赤緑は使わない。
- AIへ渡すのは uid・章・形式タグ・成績まで。**問題文・解説の全文は送らない**（MEC教材の著作物）。
  個別に深掘りする場合だけ対象を数問に絞って手で添付する。
- テスト: `node _work/test_attempts.js`（ログ）・`node _work/test_karte.js`（集計）・
  `node _work/test_merge_remote.js`（同期マージ）。いずれも実ソースを読み込むのでロジックの二重管理は無い。

## 疾患マインドマップ（2026-08-21 段A：1エンジン＋データ分離／2026-08-22 段C：全21科目のデータが揃った）

設計の正本は `_work/マインドマップ_設計.md`。テスト: `node _work/test_mindmap_layout.js`（248件）。

**役割は「直前期の俯瞰・想起」**（2026-08-21にユーザーが選択）。ここから何を載せないかが決まる。

| | |
|---|---|
| ページ | `mindmap.html`（`?sid={prefix}` で科目マップ・引数なしでハブ）1枚だけ |
| データ | `mindmap_data/index.js`（レジストリ）／`{sid}.js`（科目）／`_hub.js`（ハブ） |
| 現状 | **21科目 176章 804疾患 236関連＋ハブ21科目168疾患104関連**（2026-08-22に段Cが完了）。既存9科目は旧HTMLからの移行、残る12科目（tox/anes/rad/uro/ortho/ent/psy/derm/oph/peds/obg/kansen）はCLAUDE.mdの「章を貫く筋」記述と questions_*.json の章構成から起こした。⚠️ **2026-08-25 に足した公衆衛生 `ph` のマップだけ未作成**（レジストリは22件で `ph` が `ready:false`）。全19章がそろってから作るのが自然 |
| 生成器 | `node _work/extract_mindmap_data.js`（旧HTMLからの移行・再実行可）／`node _work/build_mindmap_index.js`（レジストリ） |

### ⚠️ 不変条件（破ると「重い・使いづらい」が再発する）

旧実装が重かった主因は**ノード数ではなく描画方式**だった（72ノード・約800要素はSVGとして軽量）。

1. **`infinite` アニメーションを1つも置かない。** 試験モードの Phase 4/5 は「読んでいる8〜15秒を
   埋める」ためのもので**目的が違う**。ここは眺めて思い出す道具で、動くものは視線を奪う。
   動くのはタップした瞬間の `.mm-pulse`（0.62秒・JSが要素ごと消す）だけ。
   旧実装は `flow`/`travelDot`/`orbitSpin`/`twinkle`/`pulse` が常時回っていた。
2. **SVGフィルタ（`filter:url(#…)`）を使わない。** glow は半透明の同心円を重ねて焼く。
   旧実装は `feGaussianBlur stdDeviation:55` を10枚重ね、拡縮のたびに再ブラーしていた。
3. **巨大SVGを CSS `transform:scale` で拡縮しない。拡縮は `viewBox` の書き換えで行う。**
   旧実装は 2000×2000 の SVG を scale 3 まで拡大でき、ドラッグ中は `will-change:transform` で
   レイヤー昇格して **6000×6000×4B ≒ 144MB のテクスチャ**になっていた（iPadが最初に落ちる場所）。
4. **章の色・角度をデータに持たない。** 色は `MM_PALETTE[章index % 11]`、角度は章数から自動計算。
   ⚠️ 旧データの章 `color` は「系統色」ではなく**隣接章を見分けるための回転パレット**だった
   （「内分泌の基本」が橙＝代謝系、「脂質代謝」が赤＝炎症系、「甲状腺」が緑＝自己免疫系）。
   `_work/mindmap_generation_prompt.md` の「系統でグループ化」は一度も守られていない。
   系統で塗り分けたくなったら、**意味のある軸を別に設計してから**入れること。
5. **走るアニメーションは常に1本**（`animToken` で世代を持つ）。旧 `panTo` は呼ばれるたびに
   新しい rAF ループを立てており、科目を連続タップすると**レンダラが応答しなくなった**
   （スクリーンショットが30秒タイムアウトした＝実測）。
6. **進捗オーバーレイ（済み率・正答率でノードを塗る）を入れない。** 章色という意味の色が既に
   載っている面に別軸の色を重ねると両方読めなくなる（Phase 4 の「筐体は真鍮固定／盤面は意味色」
   と同じ論理＝1つの面に2つの意味を持たせない）。弱点の可視化は stats.html の担当。
7. **疾患ノードの位置は `<g transform="translate(x,y)">` の提示属性が正本＝CSS で `transform` を
   動かさない。** CSS の transform は提示属性を丸ごと上書きするので、入場アニメで
   `transform: scale()` を当てた瞬間に**全ノードが原点（盤面の中央）へ折り重なり、病名が白文字の
   山になる**（2026-08-22に演出強化で実際に起きて、全科目のマップが使えなくなった）。
   動かすなら独立プロパティの `scale` / `translate` / `rotate`（translate と合成される）。
   ⚠️ **入場アニメで `opacity: 0` を使わないこと**——ベース規則でもキーフレームの 0% でも。
   非表示タブでは CSS アニメーションが1frameも進まず先頭で凍るので、ノードが見えないまま残る
   （stats.html の `armReveal` と同じ穴）。`scale` だけなら凍っても 0.6 倍で見えて押せる。
   `test_mindmap_layout.js` の §10 がこの2つを検査する。
8. **習熟度・正答率でノードを塗らない**（6 と同じ理由）。2026-08-22に `myrate_v1` を引いて
   ノードの縁を塗る実装が入ったが、**引いていたのは疾患ノードのid（`d01a`）で uid ではない**ため
   一度も一致せず、全ノードに同じ縁が付くだけだった。撤去済み。
9. **`fit()` は背景（`lBg`）と演出（`lFx`）を測らない。** 背景の目盛り円は疾患リングの大きさで
   描かれるので、何も展開していないハブだと「中身は中心の10ノードだけなのに外形は疾患リングぶん」
   になり、地図が画面中央で豆粒になる（実機で確認）。外形は `getBBox()` の実測で取る
   （レイアウトの計算値から見積もると縦に27%の余白が出た）。

### レイアウトは1つの関数で全部を賄う

`computeLayout(parents, isOpen)` が科目マップ（親＝章）とハブ（親＝科目）の両方を計算する。

```
R1   = max(250, N*(2*CH_R+CH_GAP)/2π)     親リング半径（Nから決まるので親は絶対に重ならない）
扇   = min(子が1リングに収まる幅, 隣に迷惑をかけない上限)
      閉じている隣の区画は丸ごと借りられる（閉じた親に子は無いので衝突しない）
入りきらない子は R2 + k*RING_STEP の外側リングへ積む
```

- ⚠️ **扇の `min(必要なぶん, 上限)` の `min` を外さないこと。** 外すと余裕のある科目マップでも
  扇が開閉のたびに伸び縮みして**ノードの位置が動く**＝「どこにあったか」の記憶が壊れる。
  テスト §5 が「1章を閉じても他章の疾患が1pxも動かない」を見張る。
- これで**感染症22章もハブ21科目も同じ関数で載る**。旧実装は1周を科目数で割るだけだったので、
  21科目にすると 17.1°間隔・fan 14°・弧173px となり、直径54pxの疾患ノードは3個で埋まって破綻した。
- ⚠️ **ハブに「すべて開く」を置かない。** 21科目×代表8疾患を同時に置くと、どの科目も隣の角度を
  借りられず必ず衝突する（幾何の要請であって好みではない）。同時展開は `HUB_MAX_OPEN = 3`。

### ハブは科目マップの射影ではない

`_hub.js` の72疾患（科目あたり8つ）は**独立に選抜された見取り図**で、科目マップの320疾患と
ラベルが対応するのは72件中55件、`keys` の粒度も違う（科目全体を俯瞰する高さで書かれている）。
段Cで新科目を足すときは、**科目データとハブの代表疾患8つを別々に書く**。これは重複ではなく
「その科目を定義する8つを選ぶ」という別の作業。

### 新しい科目のマップを足す手順（段C）

1. `mindmap_data/{sid}.js` を書く（章 → 疾患5〜8個 → `keys` 4行、`relations` は5〜12本）
2. `_hub.js` の `subjects` に代表疾患8つを足す
3. `node _work/build_mindmap_index.js`（`ready:true` になる）
4. `sw.js` の SHELL に `./mindmap_data/{sid}.js` を足す
5. `node _work/test_mindmap_layout.js` を通す

⚠️ **`keys` に書いてよいのは数値・略語・鑑別の分岐を含む具体的な記述だけ。**
「重要」「頻出」のような形容は1行使って何も伝えないので禁止。既存の水準を基準にする
（`小球性低色素性（MCV↓, MCHC↓）` `t(15;17) → PML-RARα融合遺伝子`）。

⚠️ **`{科目}/mindmap.html` を新しく作らないこと。** そこにあるのはリダイレクトのstubで、
エンジンを科目ごとに持つ形へ戻すと study_exam.js ⇔ chapter_exam.js と同じ乖離を21方向で踏む。

⚠️ **章の構成は questions_*.json をそのまま写さない科目が2つある。**
`kansen` は「第N章」の見出し行が0問で、問題は §N の節に入っているので**問題のある15節を章にした**。
`obg` の第13章「第120回国試問題」は年度別の問題集で疾患の章ではないため**マップには置いていない**
（内容は各章のノードに含まれる）。ここを機械的に揃えようとしないこと。

⚠️ **2026-08-22に追加した12科目の keys は、まだユーザーの医学的な検査を受けていない。**
出典はCLAUDE.mdに残したPDF照合済みの「軸」記述と一般的な国試知識で、PDF原文と1行ずつ
突き合わせたものではない（設計 §8-3 の手順5が未了）。誤りを見つけたら該当する `{sid}.js` の
`keys` を直す——**問題データ（questions_*.json）とは独立**なので、こちらを直しても採点には影響しない。

⚠️ **`_archive/mindmap_src/` を消さないこと。** `extract_mindmap_data.js` がここを読むので、
消すと移行をやり直して結果を突き合わせることができなくなる。

## テスト一覧

いずれも実ソースを読み込む（ロジックの二重管理をしない）。コミット前に全部通すこと。

```
node _work/test_attempts.js        解答イベントログ・今日の誤答 (18)
node _work/test_karte.js           弱点カルテの集計            (14)
node _work/test_merge_remote.js    Gist同期のマージ戦略        (64)
node _work/test_streak.js          連続日数と activity_v1      (9)
node _work/test_copy.js            クリップボード/2段階タップ  (17)
node _work/test_today_learning.js  ハブの「今日解いた問題」
node _work/test_srs_grade.js       SRSの自己採点3段階          (10)
node _work/test_subject_totals.js  科目別問題数の三者一致      (3)
node _work/test_card_render.js     カード描画（画像実寸・採点ボタン）(7)
node _work/test_calc_input.js      計算問題の桁入力・データ整合      (29)
node _work/test_missions.js        日次/週次ミッション          (39)
node _work/test_gamify_ceremony.js セレモニー/授与トレイ/スキップ (28)
node _work/test_exam_prog.js       試験の進捗バー・難問の可視化  (29)
node _work/test_exam_queue_scope.js 試験の出題範囲がキューに閉じているか (19)
node _work/test_daily_goal.js      ハブのゲージ・歯車の意匠      (36)
node _work/test_hero_cta.js        ハブのボタン・計器ベイの演出  (43)
node _work/test_fx_band.js         試験演出の可視帯(発火位置)    (15)
node _work/test_fx_additions.js    新エミッタ・tier7・難問/速答  (29)
node _work/test_stats_sections.js  統計の4タブ構成・弱点リスト統合 (26)
node _work/test_gist_sync.js       Gistの分割保存・切り詰めの復旧 (15)
node _work/test_exam_chassis.js    試験UIの筐体／盤面の分離      (25)
node _work/test_exam_reading.js    読んでいる間の演出            (34)
node _work/test_exam_brasswork.js  筐体の外へ広げた真鍮細工      (36)
node _work/test_mindmap_layout.js  マインドマップのレイアウト/データ (248)
node _work/test_sounds.js          効果音の一覧・音量・ランダム起動音  (28)
node _work/test_ui_theme.js        UIテーマ全8種（.qc への干渉・ネタバレ防止）(12)
node _work/check_effect_themes_sync.js  演出テーマのミラー整合

# UIテーマの「自律進化ループ」（2026-08-23〜24）が置いていった検査。粒度が細かく
# 個別の @keyframes 名を名指しするので、演出を作り直すとここが落ちる
node _work/test_step1_fx.js        (10)   node _work/test_step2_fx.js       (6)
node _work/test_step4_fx.js        (3)    node _work/test_new10_fx.js       (10)
node _work/test_next10_fx.js       (9)    node _work/test_dynamic_fx.js     (10)
node _work/test_concentric_fx.js   (4)
```

⚠️ **`node _work/check_themes.js`（ベース6テーマの配色検査）は 2026-08-26 時点で6テーマ全部が
不合格**。原因はテーマ側ではなく**放射線科 `rad` の科目色 `#475569`** で、カード面に対する
コントラストが 1.95〜2.03:1 と基準（2.5:1）に届かない。**科目色を足すときはこの検査を通すこと**
——通っていれば全科目色が基準を満たす設計になっている。他の検査はすべて green。

`test_subject_totals.js` は questions_*.json / `gamify.js`の`SUBJECTS` / `chapters_meta.js`
の3か所に散らばった問題数が一致しているかを見る。問題を増減したら必ずここが落ちる。

## stats.html の構成（2026-08-18 Phase 3 → 2026-08-23 に4タブへ）

設計の正本は `_work/演出強化_設計.md` §4。Phase 3 で**14本のセクションを10本**に畳み
4層に束ねたのが元の形で、**2026-08-23 の `f9c351a`（視認性・機能性改善 全20項目）が
その4層を4つのタブへ組み替えた**。「1画面に全部積まない」という趣旨は変わっていない。
テスト: `node _work/test_stats_sections.js`（26件）・`node _work/test_karte.js`（14件）。

| タブ（`data-tab`） | セクション（`data-sec-id`） |
|---|---|
| 📊 サマリー `summary` | 国試カウントダウンと必要ペース `hero` ／ 🔔 復習の見通し `srsForecast` |
| 🩺 弱点分析 `weakness` | 🩺 弱点カルテ `karte` ／ 🎯 弱点リスト `weakList` |
| 📈 進捗・推移 `progress` | 📚 科目別進捗 `subjTable` ／ 🗺 章別ヒートマップ `chHeatmap` ／ 📈 正答率の推移 `rateHistory` ／ ✅ 済みレベル分布 `levelDist` ／ 🗓 学習カレンダー `calendar` |
| 🤖 AIツール `ai` | 🤖 AI相談用データ ／ 一問一答ドリル ／ 取りこぼし対策（`#pane-ai`） |

### ⚠️ 2026-08-26 に書き直しで落ちた不変条件を戻した（`f9c351a` の巻き添え）

全面書き直しだったため、UIの意図とは関係なく次の4つが一緒に落ちていた。**タブ構成や
見た目は書き直し後のものを正とし、計算とテスト側だけを戻してある**。

| 落ちていたもの | 症状 | 戻し方 |
|---|---|---|
| `_mrByCh` / `_doneByCh` の索引 | ヒートマップが章ごとに `myrate`・`done` を**全件走査**（174章×全解答）。モード切替のたびに走り、実測 **1回197ms** | `hmStat(chapters, mode)` を復活させ索引を引くだけにした（**0.01ms**） |
| `cleanChTitle` | 章名の整形が `ch.title.replace(/^MEC.*?第\d+章\s*/,'')` の1本に痩せ、**末尾の「解答解説」が落ちない**＝174章中**85章**が「内分泌代謝の基本 解答解説」と表示 | `cleanChTitle(title, subjName)` を復活（**末尾の「解答解説」を先に落とす**のが要点） |
| 弱点リストの並び | ソートから**②本番との差**が抜け、本番正答率の無い問題が差を持つ問題より前に出た | `weakUnified()` を描画から切り離して復活。並びは ①タグ数 ②本番との差 ③自分の正答率 ④受験回数 |
| ドリルの出題範囲 | `weakChapters` / `weakCells` ごと消え、**章名で範囲を指定できなくなった**うえプロンプトのルールが痩せた（「次の問題を絶対に書かない」等） | 旧 `buildDrillPrompt` を復元（`_karte` → `_karteDoc` に読み替え） |

⚠️ **`HM_MIN_N`（=3）・`WK_GAP_PT`（=40）・`WK_LIMIT`（=30）は名前付きの定数に戻すこと。**
書き直しでは `d.total >= 3` / `gap >= 40` / `.slice(0, 30)` と数値が直に埋まっており、
**同じ「3」がカルテの表とヒートマップの2か所にあって片方だけ変わりうる**状態だった。
（`WK_LIMIT` は Phase 3 当時 24 だったが、書き直し後の 30 を正とした）

⚠️ **判定と描画は分けたまま置くこと**（`weakUnified` / `hmStat` / `wkPicks` が描画から独立）。
テストは実ソースの当該関数をそのまま eval する＝ロジックを二重に持たないための構造で、
`renderWeakList` の中に判定を畳み込むと**テストが関数を掴めなくなって丸ごと死ぬ**
（2026-08-23 から 2026-08-26 まで、実際に2本とも起動すらしていなかった）。

### ⚠️ 📐「本番との差」（科目単位）を作り直さないこと

`renderGap()` は**廃止した**。計算が

```
diff = (自分が試験モードで解いた問題の正答率) − (その科目の全問の本番正答率の平均)
```

で、**引き算の左右で分母が違う**。難問フィルターで解いていればそれだけで差はマイナスへ、
易問ばかりならプラスへ振れる＝数字が何を意味するのか原理的に読めない。

正しい形（**同じ uid どうしの比較**）は弱点リストの **🔻本番差 タグ**と、各行の ▾ を開いた
「本番 N% / 自分 M%」の対比バーとして残してある。`test_stats_sections.js` が
`renderGap` と `gapSec` の復活、および見出しへの「本番との差」の復活を検出する。

### 弱点リスト（統合）

旧・📐本番との差 / 💡取りこぼし問題 / 🎯苦手問題ランキング / 🔍誤答選択肢パターン の4本を
1本に畳んだ。**同じ問題は必ず1行**で、挙がった理由をタグで示す（`weakUnified`）。

| タグ | 条件 |
|---|---|
| 💡取りこぼし | 本番正答率 ≥80% なのに自分は <50% |
| 🔻本番差 | 同じ uid で本番を `WK_GAP_PT`(=40) pt 以上下回る |
| 🔁反復ミス | 試験モードで3回以上受けて <50% |

- 並びは **①タグ数 → ②本番との差 → ③自分の正答率 → ④受験回数**。
  本番正答率が無い問題は差を比べられないので、差を持つ問題の後ろへ回す。
- 上部のチップで理由ごとに絞り込める。**表示は上位 `WK_LIMIT`(=30) 件**で、全件数は下に出す。
- **旧「🔍誤答選択肢パターン」の a〜e 分布は行の ▾（`wkPicks`）へ移した**。
  全体の分布は「どこを直すか」に繋がらないので、セクションではなく末尾の1行に留めてある
  （AI相談Markdownの「誤答選択肢の傾向」は従来どおり出す）。

### 🗺 章別ヒートマップは2段（科目 → 章）

旧実装は **174章を一度に**並べ、どの章なのかは15pxの升目を1つずつタップして
読み取り行（`#hmReadout`）で読むしかなかった。1段目を科目タイルにすると一望する対象が
科目数まで減り、**2段目に章名と正答率をそのまま印字できる**ので読み取り行は廃止した。

- ⚠️ **`hmStat` は章prefixの索引（`_mrByCh` / `_doneByCh`）を引くこと**。章ごとに `myrate`
  を全件走査すると 174章（`chapters_meta.js` の全章）× 全解答の二乗になり、モード切替の
  たびに走る。テストが検査する（2026-08-23 に一度これが落ちて実測 197ms になった）。
- 升目は `.hm-cell` を使い回す＝**ランプの対応表（a1〜a5 / p1〜p5 / n0）を2箇所に持たない**。
- ⚠️ `cleanChTitle` は**「第N章」と「解答解説」で挟んで中身を取る**。講座名は科目名と一致
  しない（内分泌 → 「MEC内分泌代謝」）ので、科目名を頭から削るだけでは「代謝」が残る。
  末尾の「解答解説」は**先に**落とすこと（1つの正規表現に混ぜると、章名の無い title で
  「解答解説」自体が章名として残る）。

### ⚠️ セクションを `display:none` から出入りさせないこと

旧実装は `chartSec` / `gapSec` / `missSec` が**データの有無で**出入りしており、
**日によってページの形が変わって**「あの表はどこだっけ」の原因になっていた。
常設して**中身だけ**を空の状態（`.sec-empty`）に差し替える。

⚠️ **タブ自身の `display:none` はこれに当たらない**（`.tab-pane` は選択で切り替わるだけで、
データの有無では動かない）。禁じているのは「今日はデータが無いから消える」セクション。
畳んだ状態は `.sec.is-collapsed`（`mec_stats_collapse_v1` に `data-sec-id` ごと永続）で、
これも**ユーザーが畳んだときだけ**閉じる。

⚠️ **裏のタブに canvas を置いたら、そのタブを開いたときに描き直すこと。**
`display:none` の中では `offsetWidth` が 0 なので、`renderUnifiedChart()` は
`cv.parentElement.offsetWidth || 300` の**フォールバック 300px で焼き付いてしまう**。
`switchTab('progress')` が再描画を呼ぶのはこのため（消すと推移グラフだけ幅が合わなくなる）。

### 演出は今このページに1つも無い（戻すときの条件）

⚠️ **`f9c351a`（2026-08-23）で入場演出とカウントアップは丸ごと消えた。**
`armReveal` / `.rv-on` / `.rvs` / `mec_stats_revealed_v1`（sessionStorage）/ `countUp` /
達成リングの rAF は**1つも残っていない**。現物は `@keyframes` 0件・`animation:` 0件・
`requestAnimationFrame` 0件で、動くのは `transition:` 13本（タブの切替とホバー）だけ。
数字は最初から確定値で描かれる＝**非表示タブで0のまま凍る穴は構造的に存在しない**。

以下は撤去の経緯と、**動きを足すときに必ず添える条件**。無いものの手順書ではないので、
ここを読んで `armReveal` を探しに行かないこと。

- Phase 3 以前にも `armReveal()` があり、**14セクション全部**を動かして「ページが落ち着かない」
  ため一度撤去された（つまり2回撤去されている）。戻すなら ①対象を減らしてから載せる
  ②箱だけ（中身は既存の動きに任せる）③**ブラウザのセッション内で1回だけ**、の3条件を守る。
- ⚠️ **隠すのは JS が付けたクラスの中でだけ**（旧 `.rv-on .rvs{opacity:0}` の作り）。
  CSS 側だけで `.rvs{opacity:0}` にすると、JS が落ちた日に**ページが丸ごと真っ白**になる。
  `test_stats_sections.js` は実装が無い今も「CSS だけで本文を隠していない」を見張っている。
- ⚠️ **隠すのをやめる口は1つに寄せること**。保険で `.in` を付けて回る実装は駄目だった——
  **CSSアニメーションは非表示タブでは進まない**ので `animation-fill-mode:forwards` が適用されず、
  `.in` が付いても `opacity:0` のまま残る（実機で踏んだ。rAF が止まるのと同じ失敗の仕方）。
- ⚠️ **rAF で数字を動かすなら `setTimeout(finish, dur+400)` の落とし所を必ず添える**
  （非表示タブでは rAF が1フレームも来ない）。ハブの `_tweenNum`（index.html）と同じ作り。
  テストは**rAF を使っているのに落とし所が無いときだけ**落ちる＝使わない今は無条件で通る。
- ⚠️ **`prefers-reduced-motion` のブロックが現物に1つも無い**。`@keyframes` が無いので今は
  実害が出ていないだけで、**動きを1つでも足すなら同じコミットで止める指定も置くこと**。
- 「今日」のセルは `.cal-cell.is-today{outline:1.5px solid var(--yl)}` の**静的な印だけ**で、
  呼吸のアニメーションは今は無い（12週＝84セルの中で今日の位置が毎日ずれるので印は要る）。
  ⚠️ **呼吸を足すときも `outline` は残すこと**——reduced-motion で動きが止まっても位置が
  わかる二重化で、テストが「outline があること」と「呼吸があるなら reduced-motion で
  止まること」を対で検査する。

## 筐体（真鍮固定）と盤面（テーマ可変）の分離（2026-08-19・Phase 4・D1〜D9）

設計の正本は `_work/演出強化_設計.md` §5。**study.css だけで完結する意匠**で、JS を触るのは
稼働灯（D9）の `_updateExamFocus` と cleanup だけ。**`vars.css` / `study.html` / `index.html` には
1行も触れていない**。テスト: `node _work/test_exam_chassis.js`（25件）。

衝突していた前提は2つ——ハブ（index.html）は**真鍮・銅で固定**、試験モードは**演出7テーマで
配色がランダムに変わる**。片方に寄せるともう片方の意味が壊れるので、層を分けた。

| 層 | 色 | 中身 |
|---|---|---|
| **筐体**（シャーシ） | **真鍮固定**。ベース6テーマにも演出7テーマにも振らない | レール（`.st-hdr::before`）・溝（`.exam-prog-track`）・2つのモーダルの額縁と稜線とリベット |
| **盤面**（表示） | 可変。`--or` と意味色 `--yl`/`--gr`/`--rd`/`--blu` | 進捗の塗り・目盛り・難問印・数字・スコアリング・粒子 |

「同じ機械の中にいる」は筐体が、「今どうなっているか」は盤面が担う。**ハブのゲージが既に採っている
分け方**（真鍮の歯車 ⇔ `--or`／`--gr` の弧）と同じなので新しい規約は増えていない。

- ⚠️ **トークン名は「`--exam-` 接頭辞 かつ vars.css に同名が無い」の2条件を必須にすること。**
  `--exam-` は既に共有名前空間で、**vars.css が `--exam-modal-a` / `--exam-modal-b` を持つ**。
  接頭辞を付けただけでは安全ではない（2026-08-14 に `--cb` が vars.css のカード背景色を継承で拾い、
  `animation` ショートハンドが色を duration に食ってプロパティごと `unset` になった事故がある）。
- ⚠️ **`--exam-bevel-lo`（無彩色の暗線）を「見えないから」と省かないこと。** 明線だけだと
  「地が少し明るい帯」に見えるだけで、**明線と暗線の対**になって初めて金属の縁に見える。黒は
  6テーマすべてで中立なので対の片側が常にテーマに依存しない。省くと暖色テーマ（琥珀・薔薇）で
  意匠が消える。実測では**一番弱いのは暖色ではなく既定のインディゴ**（2.09:1・他は2.0〜2.1）で、
  散らばりが 0.19 しかなかったので**テーマ別の作り分けは1つも書いていない**。
- ⚠️ **`.ep-tick` / `.ep-last` を真鍮にしないこと（2026-08-19 に撤回済み）。** 真鍮 `#E0C25E`(L=.5525)
  は `--yl` `#FFB830`(L=.5575) と**輝度がほぼ同一＝1.008:1** で、目盛りは塗りの上にも載るので
  **通過済み区間で完全に消える**。白 .85＋黒縁なら同じ塗りの上で 1.59:1 あり、現に読めている。
  **`.exam-prog` の床（`rgba(0,0,0,.5)`）も黒のまま**——無彩色の方がその上のチャネルとレールが立つ。
- ⚠️ **モーダルの筐体を疑似要素で描かないこと。** `.exam-start-box` と `.exam-modal` は
  どちらも `overflow-y:auto` なので、`position:absolute` の疑似要素は**内容と一緒にスクロールして
  流れる**（章チップを下まで送るとリベットと稜線だけ上へ消える）。`background-image` と
  `box-shadow:inset` はボーダーボックスに固定されるのでこの2つで描く。副産物として**入場アニメも
  不要**（箱ごと `examBoxIn` でスケールインするので筐体も一緒に入る）。
  ⚠️ 2つのモーダルは**必ず1つのルールブロック**（`.exam-start-box,.exam-modal{...}`）で書くこと。
  分けると片方だけ直されて乖離する。面のグラデだけは各自の `--exam-face` に持たせてある。
- ⚠️ **`.qc` には何も足さない。** 層が満杯（`::before`=`exam-scar` / `::after`=`data-recap` と
  `exam-multi-correct` の2人）で、しかも最大594枚DOMに載る。カードは「問題」であって計器ではない。
- ⚠️ **ヘッダの高さを1pxも増やさないこと。** `_fxBand()` が `.st-hdr` の下端を演出の焦点の基準に
  している。触ってよいのは `background` / `box-shadow` / `border-*color*` の3つだけ。
  ⚠️ **`test_fx_band.js` は `.st-hdr` の高さをスタブしていて CSS を1バイトも読まないのでこの不変
  条件を守らない。** 守るのは `test_exam_chassis.js` の項目3（文字列一致）。
- ⚠️ **`body.exam-effect-*`（演出7テーマ）に筐体の上書きを足さないこと。** 「ネオンのときは筐体も
  光らせたい」は必ず出る発想だが、やると筐体が可変になり Phase 4 の前提そのものが消える。
- ▼で畳んだ状態（`hdr-collapsed`）でも筐体は全部残る。**レールと稼働灯は必ず `.st-hdr` 本体の
  疑似要素に置くこと**（`.st-filter-panel` の中に入れると畳んだ瞬間に消える）。
  ⚠️ **`.st-hdr.hdr-collapsed` のルールは3本のまま**にする（`.exam-prog` を隠すと計器ごと飛ぶ）。

### 動くのは稼働灯（D9）1つだけ

筐体は原則として静止し、自前の `animation` を持つのは **`.st-hdr::after` の稼働灯ただ1つ**。
カードに向かっている間だけレールの上を光が走り、**答えた瞬間に消えて `EXAM_IDLE_BEAT_MS`(1200ms)
の一息を置いてから次のカードで点き直る**。50問セッションでは動く／止まるが50回繰り返され、
**ユーザー自身の解答が刻む拍**になる（Phase 4 で唯一「時間」を持つ演出）。

- **フックは `_updateExamFocus()` の1か所だけ。** ⚠️ `_afterCorrectFx` は**複数選択の経路を
  通らない**し `_tallyQuestion` は3箇所に散る。`_updateExamFocus` は開始・スクロール・解答直後の
  3経路すべてを含む9箇所から呼ばれており、**1関数で全経路を覆えて取りこぼしが構造的に起きない**。
- ⚠️ **タイマー（`_examIdleTimer`）は常に1本。張り直す前に必ず `clearTimeout` する。**
  `_updateExamFocus` はスクロールのたびに走る＝この経路で一番呼ばれる関数なので、
  守らないと確実に多重発火する（`_armHold`・`_startGaugeAmbient` で同じ型の前科が2件ある）。
- ⚠️ **消灯は `opacity` だけで受ける。** クラスで `animation` ごと外すと `transform` が基準値へ
  戻って光が始点へ飛び、`animation-play-state:paused` だと途中で凍って「固まった」に見える。
  `animation` は掛けっぱなしにする。
- ⚠️ **レールは `::before`・稼働灯は `::after`。** 逆にすると後から描かれる光がレールの下に隠れる。
- ⚠️⚠️ **光は「箱の外へ出る要素」ではなく「箱いっぱいの要素の中を動く `background`」で作ること。**
  初版は `width:26%` の疑似要素を `translateX(-110%→395%)` で走らせており、**右端が包含ブロックの
  128%（1905px 幅で右へ約547px）まではみ出していた**。デスクトップ Chrome は文書のスクロール領域を
  広げないので気づけないが、**iOS Safari は右へあふれた内容に合わせてレイアウトビューポートを広げる**
  ため、`width=device-width` の下で**ページの縮尺が1.4秒周期で勝手に伸び縮みし続けた**
  （「試験中に勝手にピンチアウトして元に戻るのが終わらない」＝2026-08-19 に実機で発生）。
  `left:0;right:0` で箱に固定し `background-position` を動かせば geometry は1pxも動かない。
  `position:sticky` の疑似要素に無限の `transform` を掛けない、という意味でも安全になる。
  `test_exam_chassis.js` の項目15b が「`width` を持たない・`transform` を使わない・端で光が
  見切れない振り幅がある」を検査する。
- ⚠️ **cleanup（`exitExam`）で点灯クラスとタイマーの両方を落とすこと。** 残ると通常閲覧のヘッダで
  光が走り続ける。
- **周期 `--exam-idle-cycle` は 1.4s。長くしないこと**——問題文を読んでいる空白は8〜15秒しか
  ないので、4〜6秒の「呼吸」だと1周も回りきらずに毎回途中で止まる＝ちらつきに見える。
- `EXAM_IDLE_DELAY_MS`（点灯までの遅延）の**既定は 0**。倒す先の候補は `FAST_TIER_MS[2]`（2026-08-25に 7000→12000）
  （速答3段の「速答ではない」の境目）で、そうすると**褒める区間は静かで褒めない区間だけ機械が
  回る**という筋が通り新しい定数も要らない。⚠️ 値は推測せず `mec_attempts_v1` の所要秒から決める。
- reduced-motion では `_fxOff()` が点灯クラスを一度も付けない（vars.css の `@media` でも二重に
  止まる）。**光が消えてもレール・稜線・リベット・チャネルは残る**ので「消えるだけ」にはならない。
- 逃げ道: ラストスパートと重なって「うるさい」場合は
  `body.exam-mode.exam-sprint .st-hdr::after{opacity:0;}` の1行で消せる。

### ⚠️ 試験開始の直後だけ `.exam-key-focus` が付かない（Phase 4 以前からの既存の穴）

`startExam` の `requestAnimationFrame(_updateExamFocus)` は**カードが出そろう前に1度走るだけ**で、
そのあと再実行の機会が無い（次に走るのは最初のスクロールか解答）。そのため試験を開始した直後は
`.exam-key-focus` が誰にも付いておらず、`_markCardSeen` も呼ばれない。

- **Phase 4 が作った不具合ではない**（`_getExamTargetCard` と焦点クラスを立てる3行は HEAD と
  バイト単位で同一。2026-08-19 に確認済み）。ただし**稼働灯がこの状態にぶら下がっているので、
  1問目をスクロールせずに解くと稼働灯が一度も点かない**という形で初めて表面化した。
- **2026-08-19 にユーザーが「許容できる」と判断した＝直さない。** 稼働灯は情報を持たない付加物で、
  これが点かなくても読めなくなる情報は1つも無い（レール・稜線・リベット・チャネルは静的に残る）。
  気が変わったときのために書いておくと、直すなら `startExam` の rAF を「カウントダウンが明けた
  後にもう一度」呼ぶ形が素直。**手を出すのはそのときだけにすること**——見かけの穴に見えるので
  「ついでに直す」が起きやすいが、Phase 4 の範囲外であり、判断は済んでいる。

## 読んでいる間の演出（2026-08-19・Phase 5・R1〜R13）

設計の正本は `_work/演出強化_設計.md` §11。**解答するまでの 8〜15 秒**を埋める層で、
それまで存在したのは「3px の静止した焦点枠」と「1px の稼働灯」だけだった。
テスト: `node _work/test_exam_reading.js`（34件）。

⚠️ **読書中の演出は、他のどの演出とも性質が違う——読解の邪魔になりうる唯一の演出である。**
正解時の演出は読み終わった後なので何をしてもよいが、ここは5つの原則で縛る:
①中心視野に置かない（周辺視野だけ）②文字の上に何も重ねない ③周期を一定にする
④**解答に影響する情報を出さない**（正答率の予告＝バイアス）⑤**急かさない**（経過秒数・
カウントダウンは有害。国試は1問1分の配分）。**却下済み: R4 焦点外を沈める／R12 検査値の装飾／
経過秒数の表示／難易度の予告／読書中の環境音。R11「セッションが進むと筐体が温まる」は保留**
（Phase 4 の「筐体は真鍮固定」と衝突するので、採るなら約束を明示的に緩める判断が要る。
**ついでに実装しないこと**）。

| 記号 | 演出 | 実装 |
|---|---|---|
| R13 | 焦点カードを持ち上げる | `.qc.exam-key-focus` の `box-shadow`（study.css） |
| R5 | 真鍮のクランプが閉じて「装填」する | `.exam-key-focus:not(.exam-revealed)::before` |
| R3 | 連続正解を焦点枠の色に残す | `_syncFocusStreakColor()`（`--exam-focus-c` / `-glow`） |
| R1 | 圧が溜まり、解答で放出する（蒸気） | `_examPressureBuilt` / `_examPuffSteam` / `_examSteamTick` |
| R8 | スクロールに機械が応答する | `_machineAnims` / `_setMachineRate` / `_examScrollPulse` |
| R10 | 離席でスリープ、動くと起動 | `_armExamSleep` / `_examWake`（`.exam-asleep` / `.exam-waking`） |
| R2 | 計器ベイの歯車がアイドル回転する | `.ep-gear`（study.html の SVG ＋ study.css） |
| R7 | 画像が読影灯（シャウカステン）に掛かる | `_examLightbox`（`.qimg-lit`） |
| R9 | 「読む」→「決める」の相転移 | `_examPhaseDecide`（`.exam-deciding` / `.exam-phase-decide`） |
| R6 | 決断フェーズで選択肢がわずかに起き上がる | `.exam-deciding .ch2`。**R9 とセットでのみ意味を持つ**。⚠️水平の線を作らない（下記） |

### ⚠️ `.qc::before` を R5 が使えるのは「層が空いた」からではない

CLAUDE.md は長く「`.qc` の層は満杯」と書いてきたが、正確には**状態で排他**になっている。
`.exam-scar`（C5 の傷）が付くのは `exam-revealed` のカードだけで、焦点は
**`_getExamTargetCard()` が `!exam-revealed` で絞った未解答カード**にしか付かない。
⚠️ **この排他を担保しているのは `_getExamTargetCard()` のフィルタ**なので、あそこを触るときは
必ずここを思い出すこと。CSS 側も `:not(.exam-revealed)` を明示して二重に守っている。
`::after` は B5 の成績と `exam-multi-correct` が使ったままで、こちらは**依然として満杯**。

### ⚠️ 試験開始直後に焦点が付かない穴は塞いだ（2026-08-19 に判断を覆した）

`startExam` の `requestAnimationFrame(_updateExamFocus)` はカードが出そろう前に1度走るだけで、
次に走るのは最初のスクロールか解答だった。**稼働灯が点かないだけ**として一度は許容したが、
**R3・R5・R13 が全部この状態にぶら下がる**ので1問目だけ演出が丸ごと抜ける。
カウントダウン明けの `setTimeout`（B6/B7 が1問目を立ち上げる場所）で `_updateExamFocus()` を
呼ぶ形にした。⚠️ **`_getExamTargetCard()` の条件は触らないこと**（解答直後の焦点移動が壊れる）。

### 段2 の要点

- ⚠️ **R1 のしきい値に新しい定数を作らない**。`FAST_TIER_MS[2]`（A3 の「速答ではない」の
  境目・2026-08-25に 7000 → **12000**）を再利用すると「**A3 が褒める区間は静かで、褒めない
  区間だけ機械が動く**」という筋が通る。**速答の帯を緩めれば蒸気も同じだけ遅れて出る**
  ——この連動が壊れるので定数を分けないこと。
  経過時刻も `_examCardSeenAt`（速答判定の起点）が正本＝**帳簿を新設しない**。
- ⚠️⚠️ **R1 は噴気（読書中）と放出（解答後）で掛かる制約がまるで違う**。噴気は読んでいる最中に
  出るので原則1・2に縛られて薄さで止めるが、**放出は読解がもう終わっている瞬間**なので制約は
  掛からず、正解／誤答の演出と同じ土俵で大きく出してよい。2026-08-19 に放出量を噴気の延長で
  決めていたのを「小さすぎる」と指摘され、**放出側だけ約2.5倍**にした（両弁あわせて14→36粒・
  最大44→84px）。**噴気は据え置き**。
  ⚠️ **`rise` を上げて大きく見せようとしないこと**——発生源はレール（試験ヘッダの下端＝実測
  y≒209）で画面上端までの余地がそれしかなく、初速を上げると寿命1.0〜1.9秒の半分以上を画面外で
  使って「速く抜ける細い噴射」になる。**大きさは速度ではなく滞留時間と粒径（`grow`）で出す**。
  ⚠️ **正誤で量も色も変えない**。量っているのは正誤ではなく**費やした思考**で、「機械は判定
  しない、ただ圧を抜くだけ」という性格が誤答時に効く（誤答は罰さない方針）。
  ⚠️ 2026-08-19 に**放出をさらに拡大**した（ユーザー要望「画面中央まで吹き出して左右が重なってよい」）。
  **左右の弁からそれぞれ内側へ4段の小噴出（計8点・64粒）を撒き、中央で重なる**。
  ⚠️⚠️ **到達距離は必ず画面幅に比例させる**（`STEAM_SPAN = .42` ＝弁から内側へ画面幅の42%）。
  固定 px にすると iPad(820〜1024) では中央を大きく越え、デスクトップ(1920) では全く届かない。
  ⚠️ **`MecFX.steam` の引数や既定値は変えない**（エミッタは純増の約束＝7テーマ全部に波及する）。
  横へ伸ばすのは**同じ弁から x をずらして複数回呼ぶ**ことで作る。`steam` の `w` は左右対称に
  撒くので、弁1点で `w` を広げると画面外へ半分捨てることになる。
  ⚠️ **横へ届かせるのに速度を上げないこと**。`drag` は `Math.pow(drag, dt*60)` ＝ `.985` なら
  1秒で速度が約40%まで落ちるので、距離は伸びにくい。届かせるのは**発生位置の分散**で作る。
- **放出に合わせて歯車が3.2倍に膨らみ、噛み合いから火花が飛ぶ**（`_examGearBlow`・520ms）。
  ⚠️⚠️ **拡大は `scale` プロパティで行う。`transform:scale()` は使えない**——歯車は
  `animation:epGearSpin` が `transform` を占有しており、`transform` を別途宣言しても走行中の
  アニメーションに上書きされて**何も起きない**（黙って死ぬ型の失敗）。`scale` は独立プロパティ
  なので回転と合成される。しかも**レイアウトに影響しない**＝ヘッダの高さが1pxも動かない（実測確認済み）。
  ⚠️ **倍率を上げるときは「右の歯車から画面右端までの約110px」を超えないこと**（タイマーと終了
  ボタンのぶんで画面幅に依らない）。超えると iOS Safari がレイアウトビューポートを広げ、
  2026-08-19 のページ縮尺の振動が再発する。13px×3.2＝中心から20.8px なので現状は余裕がある。
  ⚠️ 火花は `MecFX.burst` に **`additive:false` を必ず渡す**（既定は加算合成＝光の玉になって
  金属片に見えない）。`shapes:['shard','square']`＋`gravity:1500` で弧を描いて落ちる。
  ⚠️ 火花を大きくするときは **`tier` ではなく `scale`** を上げる。`tier` は `maxSz` だけでなく
  `speed` の既定と `ttl` も動かすので、上げると「大きく」ではなく「遠くまで長く飛ぶ」になる。
- ⚠️⚠️ **膨らんでいる間は `animation-play-state:running` を強制すること**。解答した瞬間は D9 が
  `exam-idle-lit` を外して歯車を止める（1拍止まる）ので、これが無いと**膨らんだ歯車は停止した
  まま**で、`playbackRate` をいくら上げても1frameも回らない。速さは CSS の duration ではなく
  **`playbackRate`（`_machSurge`）で付ける**——位相が飛ばないため。
- ⚠️⚠️ **`getAnimations()` は CSS transition も返す**。名前で絞らずに `playbackRate` を書き換えると
  歯車の `scale`/`opacity` の transition まで加速され、**膨らみのバネ(.16s)が14倍速で潰れて瞬間
  移動に見える**（2026-08-19 に実機で確認）。速さを変えてよいのは**回り続けているもの＝
  `animationName` を持つ `CSSAnimation` だけ**（`_isNamedAnim`）。
- ⚠️ **`playbackRate` の持ち主は `_machSurge` 1つ**に寄せること。スクロール応答(R8)と歯車の
  吹き上がり(R2)が別々のタイマー列を持つと、片方の減速がもう片方の加速を打ち消して
  「たまに速くならない」という追えない挙動になる。
- ⚠️ **煙を濃くするときは粒数と粒径ではなく `alpha` と色で稼ぐ**。`steam` の1粒は
  `max*(1+grow)` 角のスプライトを毎フレーム `drawImage` するので、**描画面積は粒径の2乗**で効く
  （現在 130×4.2＝546px角 × 88粒 ＝ 約26Mpx/frame）。色は**弁元（核）を暗く外側を明るく**すると
  厚みが出る（`STEAM_TONES`）。⚠️ 色をランダムにしないこと——`glowSprite` は色ごとにキャッシュ
  するので、毎回新しいスプライトを焼くことになる。
  `test_exam_reading.js` の項目28〜34 がこれらを守る（描画面積の上限 30Mpx/frame も見張る）。
- ⚠️ **R1 の放出を `_afterCorrectFx` に置かないこと**。あそこは複数選択の経路を通らない。
  `_updateExamFocus` の「直前の焦点が `exam-revealed` になった」分岐が**単一・複数・計算・
  採点除外の全経路**を1か所で捉える唯一の場所（D9 と同じ理由）。
- ⚠️⚠️ **R8 で `animation-duration` を書き換えないこと**。走行中に変えると進捗率が不連続に飛び
  **光がワープする**。`getAnimations({subtree:true})` で掴んで **`playbackRate`** を変える
  （位相を保つ）。疑似要素のアニメは `element.animate()` では作れないので `subtree:true` が要る。
  掴めなければ**何もしない**（機能低下で済ませ、例外を投げない）。
- ⚠️ **歯車は `animation-play-state` で止める**。`animation` ごと外すと角度が 0 へ戻ってワープする。
  D9 の稼働灯で `paused` を禁じたのは「光が途中で凍ると"固まった"に見える」からで、
  **歯車は逆＝途中で止まるのが正しい絵**（答えた瞬間に1拍止まる）。
- ⚠️ **歯車は `.exam-idle-lit` に連動させる**。これにより「筐体で動くのは稼働灯の状態1つだけ」
  という Phase 4 §5-6 の趣旨が保たれる（歯車はその状態の機械的な表現であって別軸ではない）。
- ⚠️ **歯車 13px はこの行が既に持つ高さ（終了ボタンで約23px）に収まる寸法**。
  **ヘッダを1pxも増やさない**（`_fxBand()` の焦点が動く）。寸法を変えるなら実測すること。
- ⚠️ **R10 のスリープ番は D9 の `_examIdleTimer` とは別に1本**持ち、張り直す前に必ず
  `clearTimeout` する（`_armHold`・`_startGaugeAmbient`・D9 で同じ型の前科が3件ある）。
  「放置＝叱られている」に見せないこと＝暗くするだけで印は消さない。

### 段3 の要点

- ⚠️ **IntersectionObserver はこのプロジェクト初導入で、1本だけ作って R7/R9/R6 で共有する**。
  ⚠️⚠️ **全カードを observe してはいけない**（最大594枚）。焦点カードが変わったら前のカードの
  対象を `unobserve` し、新しいカードの `.qimg` と最初の `.ch2` だけを `observe` する＝常に数個。
  ⚠️ `rootMargin` は 0 のまま（広げると `content-visibility:auto` のカードの描画を強制する）。
  ⚠️ `exitExam` で必ず `disconnect()`。
- ⚠️ **R7 は素の画像を暗くしない**。CSS で暗くして JS で戻す形にすると**JS が落ちた日に画像が
  読めなくなる**（stats.html の `armReveal` で踏んだ失敗の型）。クラスが付いたときだけ
  「暗→明」が一度走る形にしてあるので、失敗しても情報は1つも失われない。
  ⚠️ 一度点けた画像は二度と点け直さない（往復のたびに光ると鬱陶しい）。
  ⚠️ 当たり判定を作らない＝`.qimg` の拡大表示（`zoom-in`）と競合させない。
- ⚠️ **稼働灯を消す経路は R9（相転移）と R10（スリープ）で分ける**。同じ `exam-idle-lit` を
  2箇所から落とすと、どちらが消したのか追えなくなる。**専用クラスで表す**。
- ⚠️ **R6 は R9 とセットでのみ意味を持つ**（単体では静的で弱く、意味も持たない）。
  ⚠️ `.ch2` の `padding` / `margin` を変えないこと（負マージンで詰めてあり、`.exam-selected` と
  `.exam-instant-correct` の背景の当たりがずれる）。⚠️ **`.exam-deciding .ch2.exam-selected` の
  再宣言を消さないこと**——この規則は `.exam-selected` より後ろにあるので、書かないと
  複数選択の「選択中」の青が `background` ごと上書きされて消える。
  ⚠️ 計算問題には `.ch2` が無い＝**何も出ないのが正しい**。
- ⚠️⚠️ **選択肢に水平の「線」を1本も作らないこと**（2026-08-19 にユーザー報告で撤去）。
  初版は各肢の上端に明線・下端に暗線・直下に硬い銅の側壁の**3本**を置いていたが、
  肢は負マージンで詰めて積んであるので**各行に罫線が引かれた表**に見え、とくに銅の側壁が
  **下線そのもの**に見えた。これは見た目の好みの問題では済まない——**下線部はこの教材では
  意味を持つ記号**で、「本文中の下線部①〜⑤が選択肢そのもの」という問題が実在する
  （整形外科 NO.167・171・142／放射線科 NO.2）。装飾の線がそこに紛れると設問の読解を壊す。
  キーの立体感は**硬い線ではなく拡散した影＋わずかな浮き**だけで出す
  （負の spread で影を要素より内側へ縮め、左右の端で線状に伸びるのを防ぐ）。
  `test_exam_reading.js` の項目27 が「縦オフセットがあるのに blur が 0」の層と `inset` を禁じる。

### ⚠️ Phase 4 の不変条件を緩めた履歴（`test_exam_chassis.js` を更新済み）

| 旧 | 新 | いつ |
|---|---|---|
| `.qc` に真鍮が1つも現れない | **R5 のクランプと R6 のキーキャップ**、および **S5 の当て板**（`.exam-scar` / `.exam-plate-fix`）だけ許す。カード面そのものは据え置き | Phase 5 / Phase 7 |
| 筐体で `animation` を持つのは `.st-hdr::after` だけ | **常時アニメ（`infinite`）を持てるのは `.exam-idle-lit` に連動するものだけ**（稼働灯・歯車・S11 の熾火）。一度きりの入場・起動アニメと**起動オーバーレイ（`.cd-*`）**は別扱い | Phase 5 / Phase 7 |
| `.qc` の疑似要素に真鍮が入らない | `.exam-key-focus:not(.exam-revealed)::before`（R5）と `.exam-scar::before` / `.exam-plate-fix::before`（S5）だけ許す。**どちらも状態で排他**なので同時には出ない | Phase 5 / Phase 7 |
| D6 の禁止リストに結果画面の中身が入っていた | **`.exam-detail-item` と4つのボタンを外した**。読解が終わった後の面で、しかもタイルとボタンは「読み値」ではなく「物」だから。**リング（`.exam-pct-ring` / `.exam-pct`）と試験中に見えるもの・開始モーダルは据え置き** | Phase 7 |

## スチームパンクの語彙を筐体の外へ（2026-08-21・Phase 7・S1〜S13）

設計の正本は `_work/演出強化_設計.md` §12。**触ったのは `study.css` / `study.html` /
`study_exam.js` の3つだけ**で、`vars.css`・`index.html`・`gamify.js`・`chapter_exam.js` には
1行も触れていない（`test_exam_brasswork.js` の項目33 が守る）。
テスト: `node _work/test_exam_brasswork.js`（36件）。

| 記号 | 内容 | 実装 |
|---|---|---|
| S6 | 選択肢に「押した」手応え | `body.exam-mode .qc:not(.exam-revealed) .ch2:active`（study.css） |
| S5 | 誤答の傷 → 真鍮の補強プレート | `.qc.exam-scar::before` ／ 克服光 `_polishPlate`（study_exam.js） |
| S10 | 結果画面を計器盤にする | `.exam-detail-item` ／ ボタン群 ／ `tubes-lit`（`showExamSummary`） |
| S8 | 起動シーケンスに蒸気機関 | `EXAM_BOOT_STYLES` に `'steam'`／`.cd-boiler`／`.cd-bz` |
| S9 | レールを管の断面にする | `.st-hdr::before` を 1px → 2px（明線と暗線の対） |
| S11 | 炉のヴィネット | `body.exam-mode::before`（縁）／`::after`（熾火） |
| S4 | 軸光（歯車の軸穴が灯る） | `.ep-gear` の `background` ／ `--exam-axle` |
| S1・S1' | 排圧計（針は答えた後にしか動かない） | `.ep-relief` / `.ep-needle` ／ `_reliefKick` |
| S12 | ヘッダの数字を管にする | `#examProgTxt` / `#examTimer` ／ `_updateExamProg` |
| S13 | トーストに打撃の1フレーム | `_showStreakEffect` の `translate` アニメ |

### ⚠️ 沈み込みは `translate`・回転は `rotate`・拡大は `scale`（`transform` は黙って死ぬ）

S6 の肢は R6 の `examKeycapUp` が `both` で `transform:translateY(-1px)` を保持し続けており、
S10 のボタンは `:hover{transform:translateY(-1px)}` と `:active{transform:scale(.98)}` が既にあり、
S13 のトーストは入場アニメが `transform` を占有している。**独立プロパティなら合成される**。
§11-5-4'（歯車の拡大を `scale` へ逃がした）とまったく同じ罠が Phase 7 で4回出た。

### ⚠️ 配色の裁定 — 「物は真鍮／読み値は意味色」（2026-08-21 にユーザーが判断）

設計の原文は S10 で「タイルも数字も琥珀のニキシー管」だったが、4タイルは
**緑=正解／赤=不正解／青=回答／紫=時間 で意味を色分けしている**ので琥珀で統一すると
その情報が消える。Phase 4 の「筐体は真鍮固定／盤面はテーマ可変」と、ハブのゲージ
（真鍮の歯車 ⇔ `--or`/`--gr` の弧）と同じ分け方を当て、**枠・鋲・ボタン＝真鍮／
数字＝意味色のまま（等幅＋暗いガラスの窪み＋自分の色のグロー）**とした。
⚠️ 同じ理由で S4 の軸光は**琥珀固定**で、tier では明るさしか変えない。

### ⚠️ S12 は正解の報酬信号を消していない（2段）

旧実装の進捗テキストの脈打ちは `if (isCorrect)` の中にあり**正解のときだけ光った**。
S12 は「この数字が量っているのは正誤ではなく進んだこと」なので中立化を推奨していたが、
**現在ある報酬信号を1つ消す変更**なのでユーザーに確認し、**2段**に落ち着いた——
**正解＝従来どおり強く緑に光る／それ以外の更新＝弱く琥珀にともる**。
⚠️ `#examTimer` には脈を入れない。1秒ごとに変わるので 1Hz の常時フリッカーになる。

### ⚠️ S5 の克服光は「一時的に打って、消す」

当て板（`exam-scar`）は誤答で付き `exitExam` が外す**セッション中だけの印**なので、
リマッチが始まる時点では既に消えている＝「板が付いたまま克服する」状況は存在しない。
そこで克服光は**別クラス `exam-plate-fix` を 1.15 秒だけ付けて消す**形にした
（前に落とした問題を正解し直した瞬間＝`_lastAnswerPrior.wasWrong`）。
⚠️ 残してはいけない——板が居座ると「この回で落とした」と「克服した」が同じ絵で並ぶ。
⚠️ 付けるのは `_afterCorrectFx`（正解の2経路の合流点）。片方に書くと計算問題50問で抜ける。

### ⚠️ 起動オーバーレイ（`.cd-*`）は筐体ではない

S8 の歯車は `infinite` で回るが、`#examCountdown` は約2.5秒で `kill()` される一度きりの
演出で、ずっと画面に居る筐体ではない（cyber 様式のリングも同じく `infinite`。真鍮を
使っていなかったから検査に掛かっていなかっただけ）。**この例外を `.cd-` 以上に広げないこと。**
⚠️ S8 の歯車は study.html の `.ep-gear` を **`cloneNode` で複製**する。path を書き写すと
歯車の実装が3本目になる（Phase 4 の判断）。
⚠️ 起動の尺は3様式で完全に同じ。**1msも増やさないこと**——起動演出は待ち時間で、
長い演出は2回目から邪魔になる。ブートログの文体は変えても**出題数と科目名は落とさない**。

### ⚠️ S11 の実測ゲート（§12-3）— 1つだけ未実施

390 / 820 / 1920 の3幅で実測し、**ヘッダ高・計器行の高さ・横あふれは Phase 7 の全改修を
通して1pxも動いていない**（hdr 241.87/208.79/208.79・overflow -14）。トラック幅だけは
S1 の針のぶん縮む（148.84→121.35 / 578.84→551.35 / 1678.84→1651.35）。
⚠️ **ゲート6（長い章でのスクロールの重さ）は未実施**。Chrome 拡張から開くタブは
`document.hidden=true` で rAF が止まるため実測できない（神経622問で確認）。実機での確認が要る。
構造上は `position:fixed` の合成レイヤー2枚で、動くのは `opacity` だけ。

### ⚠️ Phase 7 で持ち越さなかったもの

- **R11「セッションが進むと筐体が温まる」** — 保留のまま。S4 の軸光も S11 の熾火も
  **tier / 稼働灯の連動であって時間・進捗の連動ではない**。「ついでに進捗でも温めよう」と
  しないこと——採るには「筐体は真鍮固定」という約束を明示的に緩める判断が要る。
- **章別試験（`chapter_exam.js`）への展開** — Phase 4・5 と同じく意図的に乖離させたまま。

## 試験経路の演出（2026-08-18・B1〜B8）

設計の正本は `_work/演出強化_設計.md`。**study 側だけに入れてあり、`chapter_exam.js`
（年度別の過去問ビューア）とは意図的に乖離させている**——ユーザーが通らない経路なので
優先しないという判断。⚠️ `_work/check_effect_themes_sync.js` は study 側だけの `fx` キーを
除外するのでこの乖離を検出しない。「なぜ過去問だけ演出が古いのか」を後から思い出せるよう、
乖離は意図的なものだとここに残す。

テスト: `node _work/test_exam_prog.js`（29件）。

| 記号 | 演出 | 実装 |
|---|---|---|
| B1 | 科目の読み込み（チップの脈・カードの波状着地・完了の走査） | `_subjLoadStart` / `_subjLoadDone`（study.html） |
| B2 | 進捗バーの目盛り・難問印・ラストスパート | `_examProgLayout` / `_renderExamProgMarks` / `_syncExamProgMarks` |
| B3 | 結果画面の「難問 N問中 M問正解」 | `_examHardStat` ／ `#sumHardNote` |
| B4 | 開始モーダルの「この条件で N問（うち難問 M問）」 | `_renderExamPredict` ／ `#examPredict` |
| B5 | 閉じる時の幕＋戻った先に成績が残る | `closeExamSummary` / `_applyRecapChips` |
| B6 | 1問目だけ選択肢のシャッフルを見せる | `_shuffleChoices(card, wantFlip)` → `_revealShuffleFx` |
| B7 | 1問目のカード入場 | `_firstCardEntrance`（`_examCountdown` の戻り値に合わせる） |
| B8 | 誤答再試験＝リマッチ | `_rematchPending` / `_examIsRematch` / `_examRematchLines` |

### ⚠️ 進捗バーの節目は「祝わない」

跨いだ瞬間に光が走るだけで、**音も粒子も出さない**。連続正解（tier）と別軸で祝う演出を足すと
tier 演出とぶつかり画面が騒がしくなる。`test_exam_prog.js` が `_syncExamProgMarks` の中に
音・`MecFX`・ストリーク演出の呼び出しが無いことを検査する。ラストスパート（残り5問からの
色温度）は連続正解と独立した軸なので tier と競合しない。

- **難問の判定は `_isHardCard`（`data-rate < EXAM_HARD_RATE = 60`）1本**。B4の予告 → B2の道中の印
  → B3の結果、が同じ関数を共有する。⚠️ `data-rate` が無い問題は難問に数えない。
  閾値は study.html のフィルタ「難問(<60%)」・`gamify.js` の `HARD_RATE` と**3か所で一致**させる。
- ⚠️ **採点除外にはバーの印を置かない**。バーが進まない区間なので、置くと以降の位置が全部ずれる。
- ⚠️ **出題数を数える式を2つ書かないこと**。`_examCandidateCards()` が正本で、`startExam` と
  B4の予告が共有する。別々に書くと「開始を押したら予告と違う問題数だった」が起きる。
- ⚠️ **バーの印は「進んだ側（アンバーの塗り）」と「まだの側（暗いトラック）」の両方に載る**。
  片方だけ見て色を決めるともう片方で消える。芯と縁で明暗を対にすること（通過した難問は芯が白、
  未通過は芯が暗くアンバーの縁）。
- ⚠️ **`fill` を付けない `animation` は終わると基準値へ戻る**。`.ep-sweep::after` は静止位置
  （`transform:translateX(100%)`）を明示しないと、走り終えた光の帯がバーの真ん中に居座る
  （実機で確認して直した）。

### ⚠️ 集計は `_tallyQuestion`（`_afterCorrectFx` ではない）

難問の成績とセッションの正誤は **`_tallyChapter` の隣で `_tallyQuestion(card, isCorrect)`**
が拾う。採点経路は **3つ**（複数選択・単一選択・計算問題）あり、`examAnswered++` と同じ場所が
唯一の真実点。⚠️ **`_afterCorrectFx` / `_afterWrongFx` は複数選択の経路を通っていない**ので、
そこに集計を載せると「2つ選べ」の問題が丸ごと数え落とされる。
`test_exam_prog.js` が呼び出し数（3箇所）を検査する。

### ⚠️ B5 の成績表示は C5 の傷と別物

`.qc[data-recap]`（結果画面を閉じた後もカードに残る成績）は **`::after`** で描く。
`::before` は C5 の `.exam-scar`（セッション中だけの傷）が使っている。
`.qc` の `box-shadow` を上書きしないこと——影と内側ハイライトを持っており、上書きすると
カードが平らになるうえ `.qc:hover` でも消える。

### ⚠️ 完了の合図を requestAnimationFrame だけに預けない

B1 の完了（`_subjLoadDone`）は `_finish` の rAF にぶら下がるが、**非表示タブでは rAF が
1フレームも来ない**（実測。ハブの `_tweenNum` が数字を0で凍らせたのと同じ原因）。
`_subjLoadStart` が `SUBJ_LOAD_MAX_MS` の `setTimeout` を落とし所として必ず張る。

## 試験モードの演出エフェクト仕様

試験モード（🎓）で選択肢を選んだ瞬間に発火する視覚エフェクトの仕様。実装は `study_exam.js`（統合study.html用）と `chapter_exam.js`（章別過去問用・同一配色をミラー）。CSSアニメの一部は `study.css`。パーティクル描画は `fx_engine.js`（`window.MecFX`）。

### 効果音は sounds/ の3フォルダ ＋ 生成された一覧が正本（2026-08-21に再編）

```
sounds/正解音/   sounds/起動音/   sounds/選択音/   ← 音の実体（ユーザーが置く）
sounds/meta.json                                  ← 台帳（key / label / vol / peak）※人が編集する唯一の場所
        ↓ node _work/build_sounds_index.js
sounds_index.js（window.MecSounds・派生物）        ← ファイル名・キー・音量の唯一の正本
        ↓
study.html(study_exam.js) ／ index.html ／ chapter_exam.js  ← 3つとも「これを読むだけ」
```

**音を足す手順は3つだけ**——①フォルダに置く ②`sounds/meta.json` に1行足す
③`node _work/build_sounds_index.js`。**コードもHTMLも1文字も触らない**（設定画面のボタンは
`_renderStudySoundGrid` / `_renderHubSoundGrid` が一覧から生成する）。あとは `sw.js` の
`SHELL_VERSION` を bump して push。テスト: `node _work/test_sounds.js`（25件）。

⚠️⚠️ **ファイル名の表を2本目に書かないこと。** 2026-08-21 まで `study_exam.js` の
`CORRECT_WAVS`/`BOOT_WAV`・`index.html` の `HUB_CORRECT_WAVS`/`HUB_BOOT_WAV`・
`chapter_exam.js` の `ceBootSound` に**同じ表が3本**あり、ハブと過去問ビューアは
`study_exam.js` を読まないので片方だけ増やすと黙って乖離した（`check_effect_themes_sync.js`
はこれを検出しない）。`test_sounds.js` の項目3が、一覧に載っているファイル名がこの3ファイルに
直書きされていないことと、旧ミラーの名前が復活していないことを見張る。
⚠️ 過去問ビューアは `国家試験過去問/` の下にあるので、`sounds_index.js` を**自分で
`scriptBase` 付きで動的ロード**する（27ページの HTML には `<script>` を足していない）。
パスにも `scriptBase` を前置すること。

| 設定キー | 既定 | 中身 |
|---|---|---|
| `mec_correct_sound_v1` | 一覧の先頭（`custom`＝正解音） | `sounds/正解音/`（現在11種）＋`off` |
| `mec_select_sound_v1` | 一覧の先頭（`mp3`＝選択） | `sounds/選択音/`（現在4種）＋`off` |
| `mec_result_sound_v1` | 一覧の先頭（`fanfare`＝勝利のファンファーレ） | `sounds/結果画面/` の2種＋`off` |
| `mec_boot_sound_v1` | `on` | **鳴らす／鳴らさないだけ**（下記） |

- ⚠️ **起動音は設定で選ばせず、試験開始のたびにランダムで1つ鳴る**（2026-08-21〜）。
  抽選は `_pickBootSpec()`。**抽選も prepare も `startExam` の中＝開始をタップしたその手の中で
  済ませること**（iOS の自動再生制限を通せる唯一の機会。`_playBootSound` で選び直すと遅い）。
  旧値（`'ms'` 等＝ファイルを指していた頃の設定）は `'off'` 以外なので鳴らす側へ落ちる。
- ⚠️ **起動音の尺はカウントダウンより長くてよい＝鳴らし切る**（2026-08-21 にユーザーが選択）。
  MS起動 4.73秒／アカツキ起動 4.85秒 に対しカウントダウンは 2.535〜2.745秒なので、明けて
  1問目に入っても音だけ2秒ほど続く。**`_examCountdown` の尺は1msも増やさない**
  （起動演出は待ち時間で、長い演出は2回目から邪魔になる）。
- ⚠️ **正解音・選択音・コンボ音等の合成音（ping/chime/pop…）は全廃した**（ユーザーの判断＝
  「自分が置いた音以外は要らない」、コンボ音も廃止）。**戻さないこと。**
  保存済みの旧キーは `_sndResolve()` が一覧の先頭へ落とす（**localStorage は書き換えない**——
  別端末の設定を同期で壊さないため、解決は読む側で行う）。
- ⚠️ **キーは localStorage にそのまま入るので改名しないこと**（`custom`＝correct.wav、
  `mp3`＝選択.mp3 は既存ユーザーの設定が刺さっている）。ファイルをフォルダへ移しても key は据え置く。
  **起動音の key だけは localStorage に入らないので改名してよい**（毎回ランダムなので）。
- ⚠️ **音量 `vol` は「ピーク × vol」を揃える**（正解音・起動音は ≒0.5／選択音は頻繁に鳴るので ≒0.7）。
  揃っていないと**選び替えただけで体感の音量が2倍以上変わる**。`test_sounds.js` の項目2が
  台帳の `peak` と突き合わせて見張る。wav は python の `wave`+numpy で測れるが、**mp3 は
  デコーダが要る**（ブラウザの `decodeAudioData` で実測した）。
- ⚠️⚠️ **`vol>1` は GainNode でしか効かない**（`<audio>` の `volume` は 1 で頭打ち）。
  MHF_クエスト開始BGM はピーク .117＝**vol 4.3**、アカツキ起動はピーク .051＝**vol 9.8** なので、
  `<audio>` へ落ちるとほぼ無音になる。だから `_playWavSound` は**デコードが済んでいなければ
  `<audio>` へ落とさず、バッファの完成を待ってから鳴らす**（`slot.waiting` で待機中の重複要求は
  捨てる＝連打で同じ音が積み上がらない）。ハブのプレビューだけは `<audio>` なので本番より小さい。
- **起動音は演出と一蓮托生**——`_examCountdown()` は `_fxOff()`（reduced-motion 等）で何も出さずに
  返るので、そのときは音も鳴らない。
- ⚠️ **過去問ビューアの旧キー `chExamCorrectSound` / `chExamSelectSound` は 2026-08-21 に廃止した**
  ——書き込む UI がどこにも無く常に既定値のままだった（＝効かない設定）。いまは study/ハブと
  同じ共有キーを見るので、そこで選んだ音が過去問でも鳴る。
- ⚠️ sw.js の SHELL には `sounds_index.js` だけを入れてある。**`sounds/` の音そのものは入っていない**
  （従来どおり＝オフラインでは鳴らない・約2.7MB）。入れるなら `CACHE` ごと bump が要る。

### ⚠️ 発火位置は「可視帯」が正本（2026-08-04〜）

演出の焦点は **`_fxBand()`（study_exam.js）/ `ceBand()`（chapter_exam.js）** が返す帯の中心。
`window.innerHeight * 0.4` のような**画面基準の座標を新しく書かないこと**。

- 帯 = **sticky ヘッダーの下端（`.st-hdr` / 過去問は `.sn`・`.sn2`）〜 可視域の下端**（上下16pxのパディング）。
  可視域は `visualViewport` があればそれが正本（Safariのツールバー出入り・分割表示・ピンチ・
  ソフトキーボードに追従する）。無ければ `innerWidth/innerHeight`。
- 直した不具合: iPadはヘッダーが約180pxあり、トーストの `top:68px` は**ヘッダーの裏**、
  粒子・特大×nの焦点 `0.44×画面高` も上に寄って、上向きの粒子と数字が上端で切れていた。
  トースト・シグネチャ・TIER UP・全画面×n は**発火時にインラインで**帯の座標を入れる
  （CSSの `top:68px` / `top:112px` / `top:38%` / `inset:0` はフォールバック）。
- ⚠️ **`document.body` を transform してはいけない**。transform された要素は `position:fixed` の
  包含ブロックになるため、揺れている間だけ全演出がページ先頭基準になって画面外へ飛ぶ。
  画面を揺らす演出は `_shakeFxLayers()` / `ceShakeFxLayers()`（canvas＋ヴィネットだけを揺らす）を使う。
- ⚠️ 試験終了時に `opacity:0!important` を張る要素（`examStreakToast`・`streakFullscreen`・
  `examStreakBorder`）は、**次の発火で `removeProperty('opacity')` すること**。
  `!important` は WAAPI アニメより強いので、外さないと同じページの2回目以降の試験で一度も出ない。
- テスト: `node _work/test_fx_band.js`（帯の幾何・両ファイルのミラー整合・上の回帰ガード）

### 演出セット（テーマ）
- 全7セット: `classic` / `neon` / `ink` / `ecg` / `space` / `retro` / `luxury`（`EXAM_EFFECT_SETS`）。定義本体は `EXAM_EFFECT_THEMES`（study_exam.js）。
- **試験開始ごとにランダム選択**（ユーザー選択UIは無い・localStorage永続もしない）。`EXAM_EFFECT_POOL` は classic を1票・他を各2票の重み付き（classicは他の半分の確率）。
- セットは正解／連続正解エフェクトの見た目（配色パレット・絵文字・ラベル・雷/紙吹雪/花火などのON/OFF）を丸ごと切り替える。各テーマは `burstPalettes` `labels(n)` `comboLabel(n)` `comboColors` `fullscreenCols/Glow` `flashColors` `borderColors` `meterGrads` `floaterGlyphs` `correctEmoji` `use*`（useConfetti/useFireworks/useLightning/useGlitch 等のフラグ）を持つ。
- ⚠️ `body.exam-effect-*` クラスのCSS定義は `neon`/`ink` のみ（`study.css`）。他セット（ecg/space/retro/luxury）はJS（`EXAM_EFFECT_THEMES` + `MecFX`）だけで描画される。classicは `body` クラス無し。

### 正解／誤答の追加演出は「合流点」を通す（2026-08-14〜）

正解の経路は**2つ**ある（`revealAnswer`＝選択肢／`_revealCalcAnswer`＝計算問題の桁入力）。
新しい演出は必ず **`_afterCorrectFx(card, fxEl)` / `_afterWrongFx(card, fxEl, brokeStreak)`**
（study_exam.js）と **`ceAfterCorrectFx` / `ceAfterWrongFx`**（chapter_exam.js）へ足すこと。
片方の経路に直接書くと**計算問題50問だけ演出が抜ける**。`test_fx_additions.js` が呼び出し数を検査する。

現在この合流点が持つもの:

| 記号 | 演出 | 発火条件 |
|---|---|---|
| A1 | 難問クリア（刻印＋粒子・**無音**） | `data-rate < 60`。⚠️ 正答率なしは難問に数えない。⚠️ **低音の「ドン」は2026-08-18に廃止**（正解音に重なるうえ音の設定から切れなかった）。音を戻さないこと |
| A2 | 初見突破 ／ リベンジ達成 | myrate_v1 の**加算前**の値（`_lastAnswerPrior`）。⚠️ 2026-08-25に「初見は易問(≥80%)では出さない」制限を撤廃（`EXAM_EASY_RATE` ごと削除） |
| A3 | 速答3段（一閃/速答/まずまず） | `FAST_TIER_MS = [3000,6000,12000]`（2026-08-25に 2/4/7 秒から緩和）。`theme.fastLabels` は**強い順**。⚠️ 経路側で `_triggerFastBonus` を直接呼ばないこと（合流点と二重に飛ぶ） |
| A4 | 正解肢 → 解答ブロック(.ab) へのリボン | 常時（視線誘導を兼ねる） |
| A5 | 選ばなかった肢が沈む | 常時。**1.1秒で必ずクラスを外す**（解説が読めなくなる） |
| C1 | コンボメーター崩落 | 途切れた連続数が4以上。⚠️ `examStreak` を**0にする前**に控える |
| C2 | 立て直し（RECOVER） | 誤答の次の1問を正解。`_examRecoverPending` は examStreak と**別に持つ** |
| C3 | 「前にも同じ肢」 | `mec_choice_v1` の肢別誤答回数が2以上。母集団の選択率は手元に無いので**自分の履歴**で出す |
| C4 | フラットライン → 再拍動 | `theme.useFlatline`（**ecg テーマのみ**） |
| C5 | 誤答カードの傷 | 常時。**cleanup で必ず外す**（通常閲覧へ持ち越さない） |

⚠️ **ラベルの排他は2026-08-25に撤廃した**（旧: 難問 ＞ リベンジ ＞ 初見 の1つだけ）。
該当するものを全部出す——難問は肢の中央・リベンジ／初見は肢の右下と**発火位置が違う**ので
重ならず、同時に出るときはマーク側を 150ms → 420ms へずらしてある。
**リベンジ（`wasWrong`）と初見（`fresh`）だけは定義上どちらか一方しか立たない**
（fresh は total=0）ので、そこは else-if のまま。

### 正解時（単発・連続数に関係なく毎回）
`_triggerChoiceCorrectPop()`:
- 選んだ選択肢を pop（scale+brightness、420ms）＋カードを bounce（480ms）＋ `popOverlay` 色オーバーレイをカードに重ねてフェード。
- フローティングコンボ `_spawnFloatingCombo()`: カード上に浮かぶ数字。連続1（＝単発）は `+1`、2以上は `comboLabel(n)`（例 classic=`×n COMBO!`）。上へ飛んで消える（900ms）。
- classic以外は `correctEmoji`（例 neon=`⚡️💠🔷`）を6個バースト（`MecFX.glyphBurst`）。

### 連続正解時（ストリーク）
連続数 `examStreak` は正解で+1、**不正解で0にリセット**。ティア判定 `_examTier(n)`:

| tier | 連続数 n |
|---|---|
| 1 | 2 |
| 2 | 3〜4 |
| 3 | 5〜6 |
| 4 | 7〜9 |
| 5 | 10〜13 |
| 6 | 14〜19 |
| 7 | 20〜（2026-08-14に追加・天井） |

⚠️ **2026-08-25に段の敷居を前倒しした**（旧 4/7/10/15/20/30 → 新 3/5/7/10/14/20）。
50問セッションで tier5 以上へ一度も届かないことが多く、上段の演出（絵文字群・グリッチ・
墨スワイプ）が事実上死んでいたため。**この梯子を変えたら `_updateComboMeter` の
`starts`/`ends`（「あと N で TIER」の表示）と `_setAwaken` の閾値も必ず揃えること**。
`test_fx_additions.js` はソースから梯子を読んで3者の一致を検査する（数字は書き写さない）。

⚠️ **tier で配列・マップを引くときは `_tIdx(tier, o)` / `ceTIdx(tier, o)` を使うこと**。
テーマ側の配列は index 7 まで拡張済みだが、演出関数の中には index 6 までのローカル配列
（粒子数の段など）が混ざる。`_tIdx` は配列なら `length-1`、マップなら 7 で丸めるので、
どちらを渡しても範囲外にならない。`Math.min(tier, 6)` を新しく書かないこと。

`_showStreakEffect(n)` は **n≥2 でのみ発火**し、tierに応じて段階的に増える
（**2026-08-25に各段を1つずつ前倒しした**）:
- **全tier**: 上部トースト（`t1`〜`t7`、`labels[tier]` 例 classic=`🔥 n連続！！`）＋ 背景ブレス（`_triggerBgBreath`）＋ 上端コンボメーター（`_updateComboMeter`）
- **tier≥1**: 画面中央に特大 `×n`（`_triggerFullscreenCombo`）＋ 全画面フラッシュ（`flashColors`）＋ ストリーク粒子（`_spawnStreakParticles`）
- **tier≥2**: 画面シェイク（`_triggerScreenShake`）
- **tier≥3**: ゾーン突入（`_zoneStart`）＋ タイムストップ暗転（`_triggerTimeStop`）＋ 画面外周ボーダーグロー（`_triggerBorderGlow`）＋ フラッシュが複数回パルス
- **tier≥4**: 絵文字フローター群（`floaterGlyphs`）＋ グリッチ（`useGlitch`）or 墨スワイプ（`useBrushSwipe`）＋ dust（luxury/space）
- **覚醒モード**（`_setAwaken`）は tier6 の入口 ＝ **14連続**から

### ⚠️ 「昇格フレームだけフル演出」の山谷設計は撤回した（2026-08-25）

旧実装は `promoted`（tierが上がった瞬間）だけをフル演出にし、同ティア内の継続は
`quiet`（トーストの尺52%・`_spawnLightStreakFx` の軽量バーストのみ）へ落としていた。
**同ティア継続でもフル演出を出す**（`STREAK_FULL_EVERY_TIME` / `CE_STREAK_FULL_EVERY_TIME`）。

- **`promoted` に残った仕事は TIER UP スタンプだけ**——昇格していないのに `TIER UP` は嘘になる。
- `_spawnLightStreakFx` は消さずに、**同ティア継続のフレームへ上乗せ**する（UIテーマ固有の
  粒＝金継ぎの砂・氷の粉などがフル演出側に無いため、消すとテーマの手触りが失われる）。
- 戻したくなったらフラグを `false` にする（1行）。`test_fx_additions.js` が
  `if (promoted && tier >= …)` の復活を検出する。
- 個別フラグで花火・雷・紙吹雪・CRT・ECGスイープ・ブラックホール等がテーマごとに追加（`use*`）。

### 章別（chapter_exam.js）との関係
過去問ビューア側は `CE_EFFECT_THEMES` / `CE_EFFECT_POOL` として同一配色をミラー実装。片方の配色・ラベルを変えたら**もう片方も合わせる**こと。乖離は `node _work/check_effect_themes_sync.js` で自動検出でき、pre-commitフックが study_exam.js / chapter_exam.js のステージ時に自動実行する（study側のみの `fx` キーは除外。フックはGit管理外＝別マシンでは要再設定）。

## 科目選択は単一選択（2026-07-20〜）

study.html の科目チップは**1科目だけ選べる**。「全科目」ボタンは廃止した。

- **理由**: 全科目選択は最大5487問（約22万ノード）をDOMに載せ、iPad/iPhone がメモリ退避で
  タブを強制リロードする主因だった。実運用でも複数科目を同時に開く場面が無かった。
- **効果**: DOMは常に1科目ぶん（最大594問・神経）に固定される。
- `toggleSubjectChip` が「前の科目を `_unloadSubjectCards` で捨ててから次を読む」を担保する。
  同じチップの再タップで未選択に戻れる（`#mecNoSubj` の案内が出る）。
- `applyFilters` は元々 `selectedSubjects` に限定されているため、難易度・状態・検索・
  🎯苦手・🔔復習の**すべてが選択中の1科目内**になる。これは仕様。
- 科目横断が必要な用途には既に代替がある:
  - 苦手 → stats.html の苦手ランキング（科目横断）から `study.html?sid=X&filter=weak`
  - 復習 → SRS復習モード（🔔ボタン / `?mode=srs_review`）は科目横断で動く
  - 用語の横断検索 → knowledge.html

### 将来の検討事項: 全科目ミックスの総合演習（未実装）

本番は全科目ミックスだが、単一選択化により study.html 上での総合演習はできなくなった。
現状は **実力試験Ⅰ（jitsu1・160問）** と **章別試験** でカバーしている。

もし実装するなら、**全科目をDOMに載せる方式に戻してはいけない**（上記の理由で却下済み）。
**SRS復習モードと同じ「必要な問題だけをDOMに起こす」方式**を流用するのが筋:

- `_renderDueCardsForReview` / `srsq` 単品キャッシュ（IndexedDB）/ `_srsHostShow` の仕組みが
  そのまま使える。出題uidを決めてから、そのuidぶんだけホストへ描画する。
- 出題数は上限を設ける（SRS復習は `SRS_SESSION_LIMIT = 50`）。100問程度が現実的。
- 科目配分は本番の出題比率に寄せるか、`myrate_v1` の弱点重み付けにするかを決める必要がある。
- 起動経路は `?mode=mock` 等を新設し、`_srsLaunch` と同様に全科目初期化をスキップする。
- 終了後の復帰は `_srsRestoreAfterReview()` と同じ考え方が要る（解放した科目の読み直し）。

## 複数デバイス同期

GitHub Gist API で進捗を保存。`index.html` の「同期設定」から PAT と Gist ID を登録。
マージ戦略：done はunion（周回数は大きい方）。

### ⚠️ 1つの Gist ファイルに全部を詰めない（2026-08-13〜）

**Gist API の応答は `content` を 900KiB(921,600B) で切り詰め、`file.truncated` を立てる**。
2026-08-13に進捗が 963,716B まで育ち、`Unterminated string in JSON at position 920360` で
同期が止まった。

⚠️ **この 900KiB はファイル単位ではなく応答全体の合計**。実測で4ファイルに分けても
合計 content が 921,600B ちょうどで頭打ちになり、最後のファイル（`mec_srs.json`）が切られた。
**つまりファイル分割では切り詰めを避けられない**。本体の対策は raw からの取り直しの方:

- **読む側（本体の対策）** — `truncated`（および content が壊れているとき）は `raw_url` から
  全文を取り直す（`_readGistFile`）。**truncated なファイルは複数同時に出るので全部拾う**。
  ⚠️ **`raw_url` に `Authorization` を付けてはいけない**。`gist.githubusercontent.com` は
  プリフライトを通さないので `Failed to fetch` になる。secret gist の raw_url はリビジョンの
  sha入りで推測できないためヘッダ無しで取ってよい（実測で 200・全文が返る）。
- **書く側（`GIST_SHARDS`）** — 大きいキーを別ファイルへ分ける。現在は
  `mec_srs.json`（srs）／`mec_attempts.json`（attempts）／`mec_rate.json`（myrate＋choice）／
  残り全部が `mec_progress.json`。狙いは切り詰め回避ではなく、**1ファイルの肥大で書き込み側の
  上限に当たるのを防ぐこと**と、**取り直す raw の量をそのファイルぶんに抑えること**。
  読む側は**全ファイルを浅くマージして1つの payload に戻す**ので `_mergeRemote` は分割を知らない。
  旧形式（全キーが `mec_progress.json`）もそのまま読める。混在時は `mec_progress.json` が勝つ。

⚠️ **`pushToGist` の read-modify-write の事前取得が失敗したら push を中止すること**。
以前ここは `catch {}` で握り潰して push を続行しており、**リモートを読めないまま
ローカル状態で上書きして他端末の進捗を消す経路**だった（切り詰めで parse が落ちていた間、
まさにここを通っていた）。例外は「リモートが本当に壊れている(`kind==='parse'`)」ときだけで、
これはマージのしようがないので上書きで修復する。

テスト: `node _work/test_gist_sync.js`（14件・実ソースを vm で読み込み fetch だけスタブ）

## 大量ファイル変更時の注意

章ファイルは78個ある（9科目）。共通パターンの変更はPythonスクリプトで一括処理すること。
変更後は必ず数ファイルで動作確認してからコミットする。

## データソースの方針（2026-07-04〜）

- **解説・問題文の編集は必ず `questions_{prefix}.json` を対象にすること。** 章別HTML(`{科目}/ch*.html`)は編集対象ではない（study.htmlから参照されないため、直しても画面に反映されない）。
- 過去に神経・血液で「HTMLだけ強化してJSONに未反映」「科目ごとにcls命名がバラバラ(`em` vs `eem`)」という事故が発生済み。新しく解説を追加する科目でも同じ命名規則を使うこと。
- `eg`配列の`cls`命名規則（共通）: `ep`=病態, `ee`=鑑別, `ept`=国試ポイント, `em`=選択肢別解説（またはニーモニック）, `ec`=計算, `ei`=画像所見。`study.css`内のCSS(`.ep` `.ee` `.ept` `.em` `.ec` `.ei`)で色分けされるため、独自クラス名を作らない（旧: study.html内インライン。2026-07-05にstudy.cssへ外出し）。
- `questions_*.json` の整形はファイルごとにバラバラ（compact＝jitsu1 / indent=1+CRLF＝resp / 手書き混在＝custom）。**書き戻しで整形を変えないこと**。1問直しただけで全行が差分になりレビュー不能になる。`_work/fix_missing_bi_badges.py` が「往復で再現できるならjson.dumps、できなければ行単位パッチ」の実装例。
- 画像を追加・差し替えたら `python _work/build_image_dims.py` で `image_dims.json` を作り直す（`<img width height>` の材料。忘れるとその画像だけレイアウトシフトが戻る）。
- 画像は `python _work/compress_images.py`（長辺1200px・JPEG q85・**ファイル名は不変**）を通す。2026-07-24に全2544枚で461MB→228MBにした。パスが変わらないのでJSON/HTML/sw.jsの書き換えは不要。
- ⚠️ pre-commitフックは`.git/hooks/pre-commit`にありGit管理外（マシンローカル）。別マシンでcloneした際は再設定が必要。現在は「演出テーマ乖離チェック」と「画像整合性チェック」の2本。
- 章別HTMLをJSONへ完全移行し終えた科目から `_archive/{科目}/` へ`git mv`する。移行未完了（HTML側にのみ存在する解説がある）科目は先にJSONへマージしてから移動する。
